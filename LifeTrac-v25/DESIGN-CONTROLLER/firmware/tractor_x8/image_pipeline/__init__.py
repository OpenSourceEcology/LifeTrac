"""LifeTrac v25 tractor-side image pipeline package.

Splits the monolithic camera_service.py into focused stages so each can be
unit-tested independently and benchmarked against the V1..V10 gates from
IMAGE_PIPELINE.md §9.
"""
