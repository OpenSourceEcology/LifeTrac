# Minimal mosquitto config for the tractor-vtest bench stack.
# Anonymous listener on 1883; no auth, no TLS. Container network only —
# the compose file does not publish ports to the host.
listener 1883 0.0.0.0
allow_anonymous true
persistence false
