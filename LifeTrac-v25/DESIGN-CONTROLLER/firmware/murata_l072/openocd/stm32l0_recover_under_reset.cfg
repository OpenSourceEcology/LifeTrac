# STM32L072 under-reset recovery configuration for bad option-byte states.
set CHIPNAME stm32l072cz

source [find target/stm32l0.cfg]

# Slow SWD and assert NRST during connect for long jumper/CN2 recovery wiring.
adapter speed 400
reset_config srst_only srst_nogate connect_assert_srst