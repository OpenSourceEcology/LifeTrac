# Base-station web UI wiring review — 2026-07-26

Static multi-agent code review (23 agents, ~1.69M tokens, 451 tool calls). Boards powered off;
every finding is file:line-evidenced and every high/medium finding below was independently
adversarially re-verified (verdict CONFIRMED unless marked otherwise). Scope: base_station/web/*,
web_ui.py, image_rx_daemon.py, lora_proto.py, image_pipeline/*, firmware/tractor_x8/image_tx_daemon.py,
camera_service.py, plus cross-checks into lora_bridge.py, link_monitor.py, params_service, state_publisher.py.

See the chat/commit summary for the executive verdict. This file is the complete findings dump.

---
## End-to-end trace: Is the multi-method / quality image-encoding control chain active, and is it working correctly end to end?
**Active:** True | **Working:** partial

ACTIVE: yes. The encode-mode chain is fully wired end to end in current code, with an explicit confirmation loop added 2026-07-25. Path: settings.html buttons / main-view encoder pill (app.js:456-614) -> POST /api/settings/encode_mode or /api/encode_mode/cycle (web_ui.py:1915, 1974) -> retained MQTT {\"mode\":\"<name>\"} on lifetrac/v25/control/encode_mode_override (web_ui.py:1964) -> image_rx_daemon._on_encode_mode_msg maps the name through lora_proto.EncodeMode (FULL=0 ... MONO_G4=6) and registers an ack-driven retried 0xFB opcode 0x63 frame (image_rx_daemon.py:682-701, 450-463) -> tractor image_tx_daemon dispatches 0x63 both between trains and MID-BURST (image_tx_daemon.py:590-608, 1008-1019) and republishes {\"mode\":<int>} retained on the tractor-local topic -> camera_service._apply_encode_mode clamps, sets the global, forces a keyframe, and publishes a retained requested-vs-effective ack (camera_service.py:1253-1270, 1026-1069) -> image_tx_daemon forwards that ack as 0x68 (image_tx_daemon.py:643-649) -> base clears the retry and republishes retained on lifetrac/v25/status/encode_mode (image_rx_daemon.py:596-609) -> web_ui caches it (web_ui.py:819-826) and serves it via /api/encode_mode/current, which app.js polls every 5 s and renders a mismatch pill when the tractor clamped (app.js:522-543). Value mappings are consistent at every hop: the six UI strings equal the web_ui regex, equal lora_proto.EncodeMode names/values, equal camera_service.ENCODE_MODE_NAMES indices — no renumbering drift found. WORKING CORRECTLY: partially. (a) Four of six modes genuinely change the encoder: full (color WebP), y_only (luma WebP), motion_only (luma capped at quality 30), mono_g4 (1-bit dither + zlib, own wire codec, decodable at the base via image_pipeline/codec_decode.py). (b) btc4_per_tile and btc4_per_frame have no encoder yet — the tractor demotes them to y_only; the operator selecting BTC4 will see grayscale video and the pill showing e.g. 'enc: BTC4 / tile -> Y-only WebP' flagged as a mismatch, which is intended behavior but means those two menu entries are placebo codecs. (c) The QUALITY half of the original 'methods and quality' goal is NOT operator-controllable in the current LoRa-only control plane: no quality opcode exists in the 0xFB command set, the legacy CMD_CAMERA_QUALITY back-channel is bypassed in bridge mode, and the /api/params image.encode_quality route dead-ends on the base broker with no bridge to the tractor. Quality today is a tractor-local env setting (LIFETRAC_WEBP_QUALITY, default 55) with per-mode caps and an automatic degrade-to-fit loop. So: mode selection is active and correct (with two advertised-but-unimplemented codecs that clamp visibly); remote quality control is inactive.

**Hops:**
- [OK] 1. UI control — `base_station/web/settings.html:249-294; base_station/web/app.js:456-614` — Two operator surfaces. Settings page builds buttons from the server catalogue and POSTs {mode} to /api/settings/encode_mode (settings.html:268-276). Main-view encoder pill short-click POSTs /api/encode_mode/cycle (app.js:571); long-press >=800 ms jumps to /settings#encmode (app.js:596-599). UI mode strings: full, y_only, motion_only, btc4_per_tile, btc4_per_frame, mono_g4.
- [OK] 2. HTTP route in web_ui.py — `base_station/web_ui.py:1915-1926 (POST /api/settings/encode_mode), 1974-1995 (POST /api/encode_mode/cycle), 95-109 (choices + EncodeModeBody regex)` — Pydantic regex accepts exactly the six UI strings. Settings POST persists to .encode_mode_override then publishes; cycle POST publishes without persisting (session-only). Both funnel through _set_encode_mode_override (1943-1971). wireframe deliberately excluded from UI but still legal on-wire.
- [OK] 3. MQTT publish — `base_station/web_ui.py:1963-1964 (topic const at 220)` — Publishes JSON {"mode": "<name>"} (string form) on lifetrac/v25/control/encode_mode_override, retain=True, qos=0. Also republished retained on web_ui startup from the persisted file (530-540). Retained pin — same pattern that caused the radio_profile stale-retain incident; a cycle press leaves a retained value that differs from the persisted settings value until web_ui restarts and re-pins the persisted one.
- [OK] 4. Base daemon -> 0xFB 0x63 frame — `base_station/image_rx_daemon.py:682-701 (_on_encode_mode_msg), 726 (callback add), 1021 (subscribe qos=1); name->byte map base_station/lora_proto.py:80-91; opcode const lora_proto.py:957` — String name mapped via EncodeMode[requested.upper()].value: FULL=0, Y_ONLY=1, MOTION_ONLY=2, WIREFRAME=3, BTC4_PER_TILE=4, BTC4_PER_FRAME=5, MONO_G4=6, ADAPTIVE=7 — matches UI names exactly. Sends pack_command_frame(0x63, bytes([mode])) via the RS-1.5 ack-driven retry table _set_pending (10 s deadline, max 20 attempts, one retry per idle pass; image_rx_daemon.py:450-463, 497-507). 'auto' payloads are ignored (688).
- [OK] 5. Tractor RX dispatch (incl. mid-burst) — `firmware/tractor_x8/image_tx_daemon.py:590-608 (_dispatch_command 0x63), 1008-1019 (mid-burst RX_FRAME dispatch inside the pipelined TX loop), 468-482 and 856-857 and 1031-1032 (inter-frame/gap drains)` — On 0x63 with >=1 arg byte, logs and republishes JSON {"mode": <int>} retained qos=0 on tractor-LOCAL topic lifetrac/v25/tractor/encode_mode_override (topic const at 234). Mid-burst path explicitly fixed 2026-07-25: RX_FRAME URCs arriving during a fragment burst are parsed and dispatched instead of dropped (comment cites 1/12 delivery in runs D/F before the fix).
- [OK] 6. camera_service application (mode AND quality) — `firmware/tractor_x8/camera_service.py:1253-1270 (MQTT override handler), 1026-1069 (_apply_encode_mode), 447-460 (_clamp_encode_mode), 437-444 (_ENCODE_MODE_IMPLEMENTED), 555-609 (_encode_tile), 516-524 (quality caps), 62-65 (WEBP_QUALITY env)` — Tractor-local transport is retained MQTT on the tractor broker (subscribe qos=1 at 1278 so a restarted camera_service re-picks the pin). Handler accepts int or name (ENCODE_MODE_NAMES at 430-433 — indices identical to lora_proto values). _apply_encode_mode clamps to implemented set {full 0, y_only 1, motion_only 2, wireframe 3, mono_g4 6, rawstream 8}; btc4_per_tile(4), btc4_per_frame(5), adaptive(7) are NOT implemented and silently demote to Y_ONLY(1), then sets global ENCODE_MODE and forces a keyframe. Encoder honours mode at 572-589 (mono_g4 branch 573-574 -> _encode_tile_mono_g4 at 528; grayscale convert for non-FULL at 575-581) and wire codec byte via _codec_for_mode (502-511, used at 977). QUALITY is NOT operator-commanded over this chain: effective quality = env LIFETRAC_WEBP_QUALITY (default 55, clamped 20-100) capped per-mode (motion_only<=30, wireframe<=20) with ROI split (670-673) and a -10-step degrade loop to fit TILE_BYTES_MAX (591-599).
- [OK] 7. 0x68 ENCODE_MODE_ACK over the air — `camera_service.py:1052-1066 (retained ack publish on lifetrac/v25/status/encode_mode, local); image_tx_daemon.py:1082-1090 (subscribes, latches _enc_ack_out), 643-649 (_service_control_plane sends pack_command_frame(0x68, ack[:200]), copies=1)` — Ack JSON carries {requested, effective, effective_name, clamped, codec, source, ts} — requested-vs-effective is exactly what exposes the btc4 clamp. Latest-wins single-copy TX; if the copy is lost, the base's RS-1.5 retry of 0x63 re-triggers the tractor's retained republish -> fresh ack, so convergence is retry-driven. Boot ack also published (camera_service.py:1284) so the base never renders unknown.
- [OK] 8. Base ack handling -> MQTT status — `base_station/image_rx_daemon.py:596-609` — On opcode 0x68: _clear_pending(0x63) stops the retry loop, then the camera_service JSON is republished as-is, retained, on lifetrac/v25/status/encode_mode (const at 125) to BOTH mqtt clients — designed for zero-LAN-to-tractor operation.
- [OK] 9. Operator reflection — `base_station/web_ui.py:819-826 (status cache), 894-895 (subscribes status/#), 1894-1912 (GET /api/encode_mode/current returns tractor ack + rx_codec_name); base_station/web/app.js:552-564 (5 s poll), 522-543 (_renderEncPill mismatch)` — web_ui caches the retained ack in _encode_mode_ack and serves it as 'tractor' plus the codec of the last reassembled frame (implicit over-the-air proof). app.js polls every 5 s and, when ack.clamped or effective_name != pinned mode, renders 'enc: <pinned> -> <effective>' with a mismatch style and an explanatory tooltip ('clamped — encoder not implemented'). map.js:204-207 and diagnostics.js:203-204 also display link.encode_mode. Base-side decode of every implemented codec exists (image_pipeline/codec_decode.py:45,56-119 incl. CODEC_MONO_G4).

**Gaps:**
- btc4_per_tile (4) and btc4_per_frame (5) are offered in the UI but have no tractor encoder: camera_service.py:437-444 excludes them from _ENCODE_MODE_IMPLEMENTED and _clamp_encode_mode (447-460) silently demotes both to Y_ONLY. The chain is honest about it (ack clamped:true, pill mismatch, tooltip text in app.js:466-467 even pre-announces the fallback), but 2 of the 6 operator choices do not deliver their advertised codec.
- Operator quality control has NO working transport. The 0xFB command set (lora_proto.py:962-964) contains no quality opcode; CMD_CAMERA_QUALITY 0x04 exists only in the legacy M7 KISS back-channel dispatcher (camera_service.py:1094-1099), which is bypassed in LoRa-bridge mode (main() skips IpcWriter, camera_service.py:1145-1148). The web_ui allow-listed key image.encode_quality (web_ui.py:49) publishes to lifetrac/v25/params/set on the BASE broker (web_ui.py:2109), while params_service subscribes on the tractor-local broker (firmware/tractor_x8/params_service.py:184) — with the tractor control plane LoRa-only and no LAN bridge, this path dead-ends. Quality is therefore env-fixed on the tractor (LIFETRAC_WEBP_QUALITY, per-mode caps).
- lifetrac/v25/control/encode_mode_override is a retained pin (web_ui.py:1964) republished on web_ui startup (530-540) — same retained-topic pattern as the radio_profile incident. A pill 'cycle' publishes retained without persisting, so after a web_ui restart the persisted settings value is re-pinned over the last cycled mode; a restarted image_rx_daemon will also replay whatever mode is retained.
- ENCODE_MODE_ACK is sent with copies=1 (image_tx_daemon.py:649) versus copies=2 for other command frames; convergence relies on the base's 0x63 retry loop (10 s / 20 attempts) re-eliciting a fresh ack. If all copies fail inside the window, the base logs 'GAVE UP' while the tractor has already switched — the UI would still self-correct via the retained ack on the next successful forward, but the operator can see a stale pill for that interval.

---
## End-to-end trace: Are all buttons and selection mechanisms in the base-station UI properly wired into the wireless (LoRa) system: (A) radio-profile selector incl. auto policy and CONF/revert feedback, (B) keyframe request incl. the LIFETRAC_KF_REQUEST_DISABLE gate, (C) hydraulic/drive controls incl. deadman and link-down behavior?
**Active:** True | **Working:** partial

The three chains split cleanly into one well-closed loop, one gated automatic mechanism, and one open-loop safety-critical path. (A) RADIO PROFILE is properly wired end to end with real feedback: settings.html buttons (363-394) -> POST /api/settings/radio_profile (web_ui.py:2025-2065) -> retained publish (305-313) -> image_rx_daemon two-phase switch (0x65 cmd, 12 s ACK wait, local switch on 0x66, CONF 0x67 at proof-of-life, 45 s revert watchdog; image_rx_daemon.py:363-429/541-650) -> tractor ACK-on-old-grid + independent revert (image_tx_daemon.py:609-669). The UI DOES learn the outcome: ok:true is published retained on status/radio_profile/rx only at proof-of-life and ok:false with 'reverted: link silent' on revert; web_ui caches both (828-838) and settings.html renders '(FAILED)' plus mismatch coloring on a 5 s poll. Caveats: the synthesized tx ack is ok:true before the switch is proven, the main console has no profile indicator, and the incident-class retained hazard survives — the daemon never checks msg.retain (667-680), so a stale retained {\"profile\":N} still replays on reconnect. (B) KEYFRAME has NO UI button at all (grep of base_station/web finds only CSS @keyframes); the mechanism is automatic (web_ui.py:965-972 canvas recovery + the daemon's self-heal poke). LIFETRAC_KF_REQUEST_DISABLE=1 silently swallows every request at the subscription choke point (image_rx_daemon.py:711-712) — nothing is published, and although needs_keyframe rides every /ws/state snapshot, no frontend module consumes it, so the answer to 'would a button silently no-op' is yes, and it is not surfaced. (C) HYDRAULIC/DRIVE is connected in code but NOT via the 0xFB LoRa-only plane the commit message implies: the 0xFB opcode set has no drive opcode (lora_proto.py:956-961) and image_rx_daemon ignores cmd/control. Drive goes index.html/app.js 20 Hz -> /ws/control -> web_ui pack_control -> MQTT cmd/control qos=0 (web_ui.py:1208-1304) -> lora_bridge.py (header still 'DRAFT FOR REVIEW. Not run yet.', run by docker-compose.yml:54) -> KISS/AES FT_CONTROL on a separate SX1276 -> tractor_h7.ino -> Opta Modbus. Deadman layering is solid (150 ms server zero-frames + halt-on-close, 200 ms tractor neutral, latched E-stop), so the system fails SAFE — but it fails SILENT: control frames and E-stop have no ack path, /api/estop returns ok unconditionally, the E-stop click handler ignores the response, and the 'link' pill only reflects the browser-to-server WebSocket. With the radio down the UI neither shows failure nor success. Properly wired: radio-profile selector (full confirmation loop), encode-mode pill (ack + rx-codec loop), camera select (waits for tractor echo, app.js:196-207). Orphaned or lying: the nonexistent manual keyframe button, the silent KF-disable gate, and the drive/E-stop path's total absence of delivery feedback.

**Hops:**
- [OK] A1: UI radio-profile buttons -> POST /api/settings/radio_profile — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/settings.html:363-394` — Buttons injected from server catalogue; POST {profile}; status line shows 'commanded — daemons reconfigure (~2 s outage)'. Settings page only — the main operator console (index.html) has no radio-profile indicator.
- [OK] A2: web_ui persists + publishes retained {"profile":N} on lifetrac/v25/control/radio_profile — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:2025-2065 and 305-313` — retain=True qos=0 at web_ui.py:308-309. 'delivered' in the POST response reflects only the local broker publish rc, not air delivery. Startup republish at 546-556.
- [OK] A2b: auto policy (AutoRadioPolicy + 5 s worker) -> same retained publish — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:316-372, 559-605` — Driven by image_rx_daemon link_stats via /video/link_stats (web_ui.py:806-813). Degrades 2->1 on stale/lossy link, promotes 1->2 after 60 s health; never picks bench profile 0. Publishes retained with source="auto".
- [OK] A3: image_rx_daemon consumes control/radio_profile (RETAINED-FLAG RISK) — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:667-680, 722-728` — _on_radio_profile_msg never checks msg.retain — a stale retained {"profile":N} is replayed on every daemon reconnect/restart and enqueued as a fresh switch. Only guard is target==active suppression at line 421. The incident-class hazard is still present in current code; it is masked (not fixed) by web_ui republishing its persisted selection at startup.
- [OK] A4: base two-phase switch: 0x65 cmd on old grid, await 0x66 ACK (12 s), switch, proof-of-life, 0x67 CONF, 45 s revert watchdog — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:363-429, 541-595, 611-650 (timeouts at 134-135)` — PROFILE_ACK_TIMEOUT_S=12, PROFILE_REVERT_TIMEOUT_S=45. Includes LATE-ACK split-heal (577-595) and RXCONT re-arm after every TX (431-448, run-31 fix).
- [OK] A5: tractor side: ACK on old grid before retune, revert unless CONF within window — `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:609-626, 643-669` — 0x66 ACK queued and flushed on the OLD profile, then switch; CONF (0x67) clears its revert deadline; no CONF -> reverts independently so both ends heal on the old grid.
- [OK] A6: outcome feedback to UI: retained acks on status/radio_profile/{tx,rx} -> web_ui cache -> GET -> settings poll — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:544-560 (tx, synthesized), 652-665 (rx, ok/error); web_ui.py:828-838, 1999-2022; settings.html:338-357, 396-398` — YES — the UI learns success and reversion: ok:true is published ONLY at proof-of-life (image_rx_daemon.py:621), and revert publishes ok:false error='reverted: link silent' (415-416) / 'no tractor ack' (397-398); settings.html renders '(FAILED)' and orange mismatch, polling every 5 s. Caveat: the tx-side ack is synthesized ok:true at ACK time (550-552), BEFORE the switch is proven, so tx can read confirmed while rx later shows FAILED.
- [BROKEN] B1: UI keyframe button — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web (no match; only CSS @keyframes at index.html:291)` — NO manual keyframe button exists anywhere in the web UI. The KeyframeRequester docstring referencing 'web_ui's manual button' (image_rx_daemon.py:160-162) is doc drift. The mechanism is automatic-only.
- [OK] B2: automatic requesters -> MQTT lifetrac/v25/cmd/req_keyframe — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:965-972 (grid-mismatch/canvas recovery, qos=1); image_rx_daemon.py:159-186 poke on reassembly timeout (called at 762)` — Two publishers: web_ui's canvas recovery and the daemon's own self-heal poke (5 s min interval). Poke is gated by LIFETRAC_KF_REQUEST_DISABLE at line 175.
- [OK] B3: image_rx_daemon subscription -> 0x60 LoRa cmd, GATED by LIFETRAC_KF_REQUEST_DISABLE — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:703-720 (gate at 711-712), retry-until-keyframe 885-887` — SILENT NO-OP CONFIRMED: with the env set, _on_req_keyframe_msg returns before doing anything — the choke point ALL request sources pass through. Nothing publishes a 'disabled' status; link_stats does not carry it; and although StatePublisher snapshots include needs_keyframe (image_pipeline/state_publisher.py:95), NO web/img module consumes it. The operator would just see a stale/black canvas with no explanation. If a manual button were added it would silently no-op the same way. Additional 10 s rate limit at 713-716 also drops requests silently.
- [OK] B4: tractor: 0x60 -> lifetrac/v25/tractor/req_keyframe -> camera_service forces keyframe — `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:596-599; camera_service.py:105, 1250, 1275` — Tractor-scoped topic (renamed in 3b1fb347 so a shared bench broker cannot bypass the radio). camera_service subscribes both legacy and tractor topics.
- [OK] C1: index.html joysticks/gamepad/buttons -> app.js 20 Hz JSON over /ws/control — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:44, 181-185, 274-381, 628-690; index.html:397-398 (E-STOP)` — Virtual pads + USB gamepad; controlsLocked zeroes state when handheld/autonomy owns arbitration (app.js:140-163). The 'link' pill (app.js:45-59) reflects ONLY the browser<->web_ui WebSocket, not the radio.
- [OK] C2: web_ui /ws/control -> pack_control -> MQTT lifetrac/v25/cmd/control qos=0; 150 ms deadman + halt-on-close — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1208-1304 (deadman 1235-1252, close-halt 1287-1303, arbitration gate 766-768)` — Deadman: 150 ms without a browser frame -> repeated all-zero halt frames; socket close -> final halt frame. Both gated on _base_controls_allowed so base never fights handheld/autonomy.
- [OK] C3: MQTT -> lora_bridge -> KISS+AES-GCM FT_CONTROL / CMD_ESTOP over its own SX1276 serial radio — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/lora_bridge.py:3 ('DRAFT FOR REVIEW. Not run yet.'), 467-493; deployed via docker-compose.yml:31-54` — KEY FINDING: hydraulic/drive is NOT on the 0xFB LoRa-only control plane. The 0xFB opcode set is only 0x60/0x63/0x65-0x68 (lora_proto.py:956-961) — no drive opcode — and image_rx_daemon never subscribes cmd/control. Drive rides the LEGACY lora_bridge path (CRC check, nonce restamp, encrypt, TX) on a SEPARATE radio from the image link. The file still carries a 'DRAFT... Not run yet' header though docker-compose runs it; commit 3b1fb347's 'control plane' migration covered only image-plane selectors.
- [OK] C4: tractor_h7 M7 firmware: FT_CONTROL RX -> 50 Hz arbitration -> Opta Modbus; 200 ms stale -> neutral; CMD_ESTOP latched — `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_h7/tractor_h7.ino:513 (CONTROL_TIMEOUT_MS=200), 516-518 + 1092 (g_estop_latched), 918-923 (Opta regs)` — Tractor-side deadman: stale ControlFrame -> neutral; E-stop latched, cleared only by CMD_CLEAR_ESTOP. Fails safe independently of the base.
- [BROKEN] C5: link-down / failure surfacing back to the operator — `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:383-385, 666-668 (estop fetch, no response handling); web_ui.py:1307-1310 (/api/estop returns ok unconditionally)` — THE UI NEITHER SHOWS FAILURE NOR SUCCESS for drive/E-stop. Control frames are qos=0 fire-and-forget; no ack path exists (app.js:64-66 'Control socket receives no messages currently'); /api/estop returns {ok:true} regardless of whether lora_bridge is even running; the E-stop click handler ignores the response entirely and renders nothing. If the bridge or radio is down, the operator's only clue is telemetry going stale — and the telemetry sidebar has no staleness marker (only the image link_stats panel has a 'stale' class, app.js:269). Safety is preserved by the tractor's 200 ms neutral deadman, but silently.

**Gaps:**
- C: No operator feedback for drive/E-stop delivery. E-stop button fires fetch('/api/estop') and ignores the result (app.js:383-385); the server returns ok unconditionally (web_ui.py:1307-1310); control frames are qos=0 with no ack. A dead lora_bridge or dead control radio is invisible — the UI pretends normal operation (the 'link' pill only covers the browser-to-server WebSocket).
- C: Architecture claim vs code: hydraulic/drive does NOT travel as 0xFB frames. No drive opcode exists in the 0xFB set (lora_proto.py:956-961) and image_rx_daemon does not subscribe cmd/control. Drive depends on lora_bridge.py, whose header still reads 'DRAFT FOR REVIEW. Not run yet.' (lora_bridge.py:3), on a second radio, with no health cross-check from web_ui.
- B: LIFETRAC_KF_REQUEST_DISABLE=1 makes ALL keyframe requests (including web_ui's automatic canvas recovery) silently no-op at image_rx_daemon.py:711-712 with zero status published and no UI surface — needs_keyframe is in every /ws/state snapshot (state_publisher.py:95) but no frontend module reads it.
- B: No manual keyframe button exists in the web UI despite image_rx_daemon.py:160-162 referencing 'web_ui's manual button' (doc drift).
- A: Retained-replay hazard from the real incident is still present: image_rx_daemon._on_radio_profile_msg (667-680) never checks msg.retain, so a stale retained {"profile":N} is re-applied on every daemon reconnect; only the target==active guard (421) limits it.
- A: tx-side ack is synthesized ok:true at ACK time (image_rx_daemon.py:548-560), before proof-of-life — settings can show tx confirmed for a switch that later reverts (rx side does show FAILED).
- A: Radio-profile state (incl. auto-policy demotions and reverts) is visible only on settings.html via 5 s polling; the main operator console has no indicator, so a mid-run auto demotion or revert is invisible while driving.
- A: POST response field 'delivered' reflects only the local mosquitto publish rc (web_ui.py:305-313), not radio delivery — misleadingly named but corrected by the ack display.

---
## Layer review: frontend-main

Reviewed the main-page frontend (index.html, app.js, all 8 img/ modules) against web_ui.py, lora_bridge.py and image_rx_daemon.py. All referenced element IDs exist and every visible control has a wired handler; the encode-mode pill's 6-mode list exactly matches the server's choices/cycle order, and its full chain (POST /api/encode_mode/cycle -> retained control/encode_mode_override -> image_rx_daemon 0xFB opcode 0x63 -> tractor ack 0x68 -> status/encode_mode -> 5s poll of /api/encode_mode/current) is verified; there is NO quality control on the main page (image.encode_quality/tile_quality exist only behind the unused /api/params). Two high-severity defects: (1) E-STOP (click and gamepad START) is fire-and-forget with no error handling — after a web_ui restart wipes the in-memory session store, E-stop 401s in total silence; (2) button releases swallowed during controls-locked transitions latch screenButtons/screenFlags, so a later innocent button press silently re-transmits take-control (buttons bit7 + flags 0x01). Notable mediums: the sidebar link-stats rows listen on '/status/link'//source_active dicts while the bridge actually publishes control/link_airtime (rows permanently dead); /ws/telemetry has no reconnect so the SOURCE banner/lock state freezes after one drop; /ws/control's 4401 auth-close loops forever without prompting re-login; camera-select ignores HTTP errors and offers cameras the build config filtered out; the encode pill has a cycle-vs-poll race plus silent-failure stale state, and its 'loading' overlay is dismissed by old-stream tiles. Also flagged: raw-mode CSS defeats the badge fail-closed blackout, t-rssi is displayed but never written, 'Pkt loss' is fed airtime utilization, ws:// is hardcoded, the 5s poll defeats the 30-min idle session timeout, and delivery of cmd/control//cmd/estop//cmd/camera_select beyond MQTT is unverified because image_rx_daemon (the stated radio owner) does not subscribe to those topics — only lora_bridge does, on its own serial port.

### Controls / surface inventory
- **Left virtual joystick (#pad-left, LH drive)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:393 (element); app.js:334-350 (pointer handlers)
  - action: pointerdown/move/up/cancel update state.lhx/lhy (-127..127); 20 Hz setInterval (app.js:181) sends JSON over WebSocket when not controlsLocked and no gamepad active
  - backend: WS ws://host/ws/control -> web_ui.py:1208 packs ControlFrame -> MQTT lifetrac/v25/cmd/control (qos0) -> lora_bridge.py:468 serial TX
  - note: Element exists, handler wired, ranges match ControlMsg validation (web_ui.py:62-67). Server enforces 20 Hz, 150 ms deadman halt, and session-cookie auth on the WS.
- **Right virtual joystick (#pad-right, RH boom/aux)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:400; app.js:334-350
  - action: same as left pad, updates state.rhx/rhy
  - backend: WS /ws/control -> MQTT lifetrac/v25/cmd/control
  - note: Same pipeline as left pad.
- **CURL button (data-btn=0)** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:326; app.js:361-381
  - action: pointerdown/up/leave set/clear bit0 of state.buttons (hold-to-actuate)
  - backend: WS /ws/control buttons bitmap -> MQTT lifetrac/v25/cmd/control
  - note: Affected by the stuck-bit latch across controls-locked transitions (finding 2): pointerup during handheld/autonomy lock is swallowed, leaving screenButtons latched.
- **DUMP button (data-btn=1)** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:327; app.js:361-381
  - action: bit1 of state.buttons while held
  - backend: WS /ws/control -> MQTT lifetrac/v25/cmd/control
  - note: Same stuck-bit latch exposure as CURL.
- **REQ CTRL / take-control button (data-btn=7)** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:328; app.js:361-381 (flags logic 369-371)
  - action: while held sets buttons bit7 AND flags bit0 (takectl_held); server latch is upstream
  - backend: WS /ws/control -> MQTT lifetrac/v25/cmd/control
  - note: Worst case of the stuck-bit latch: a release swallowed while controls are locked leaves flags bit0 set in screenFlags; the next unlocked button press silently re-transmits take-control (finding 2).
- **AUX 1 button (data-btn=2, settings drawer)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:350; app.js:361-381
  - action: bit2 of state.buttons while held
  - backend: WS /ws/control -> MQTT lifetrac/v25/cmd/control
  - note: Lives in the gear drawer; drawer must be open to press. Same latch caveat in principle.
- **AUX 2 button (data-btn=3, settings drawer)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:351; app.js:361-381
  - action: bit3 of state.buttons while held
  - backend: WS /ws/control -> MQTT lifetrac/v25/cmd/control
  - note: Same as AUX 1.
- **E-STOP button (#estop)** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:398; app.js:383-385
  - action: click -> fetch POST /api/estop, fire-and-forget (no .then/.catch)
  - backend: POST /api/estop (web_ui.py:1307, session-gated) -> MQTT lifetrac/v25/cmd/estop empty payload qos1
  - note: Handler wired and endpoint exists, but zero response/error handling: a 401 (in-memory session store wiped by web_ui restart) or network error is completely silent (finding 1). Correctly NOT gated by controlsLocked, so E-stop stays clickable during handheld/autonomy control.
- **USB gamepad axes + A/B/X/Y/RB** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:628-691 (pollGamepad, 50 Hz)
  - action: axes[0..3] -> lhx/lhy/rhx/rhy with 0.07 deadzone; A/B/X/Y -> buttons bits 0-3; RB -> bit7 + flags bit0; merged with on-screen buttons
  - backend: WS /ws/control -> MQTT lifetrac/v25/cmd/control
  - note: Preempts on-screen sticks; zeroed and visuals synced when controls are locked. Footer legend (index.html:410) matches the actual mapping. gamepadButtonsMask (app.js:29,664) is written but never read - dead variable.
- **Gamepad START (button 9) -> E-stop** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:666-668
  - action: rising-edge only -> fetch POST /api/estop (fire-and-forget)
  - backend: POST /api/estop -> MQTT lifetrac/v25/cmd/estop
  - note: Same silent-failure exposure as the on-screen E-STOP (finding 1). Edge detection correctly prevents 50 Hz re-fire. Fires even when controlsLocked (intentional for E-stop).
- **Gamepad BACK/SELECT (button 8) -> encode-mode cycle** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:673-675
  - action: rising-edge -> cycleEncodeMode() (same path as pill click)
  - backend: POST /api/encode_mode/cycle (web_ui.py:1974) -> retained MQTT lifetrac/v25/control/encode_mode_override -> image_rx_daemon -> 0xFB opcode 0x63 ENCODE_MODE -> tractor; ack 0x68 -> retained status/encode_mode
  - note: Runs BEFORE the controlsLocked check (app.js:676), so encode cycling works while handheld/autonomy is active - probably acceptable (image plane, not motion) but inconsistent with every other button. Inherits the cycle/poll race (finding 7).
- **Camera switch FRONT/REAR/IMPL/AUTO (#camera-switch)** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:356-361; app.js:392-402
  - action: click -> POST /api/camera/select {camera:name}; active highlight deliberately driven only by tractor echo on topic .../active_camera (app.js:196-208)
  - backend: POST /api/camera/select (web_ui.py:1743, session-gated, filtered by BuildConfig) -> MQTT lifetrac/v25/cmd/camera_select 1-byte -> lora_bridge CMD_CAMERA_SELECT over LoRa; confirmation echo: lifetrac/v25/video/active_camera -> /ws/telemetry
  - note: Response entirely ignored, no .catch: HTTP 400 (camera absent from BuildConfig - HTML always shows all four buttons regardless of _CAMERA_IDS filtering, web_ui.py:1424-1442), 401, or network failure all look identical to 'tractor ignored me' (finding 6). Echo-driven highlight also freezes if /ws/telemetry dies (finding 4).
- **RADIO ON / RADIO OFF bench toggles (data-radio wake/sleep)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:366-367; app.js:406-454
  - action: click -> async POST /api/bench/radio {mode}; status text updated with detailed success/failure reason (401/403/rc/output_tail)
  - backend: POST /api/bench/radio (web_ui.py:1383, session-gated, env-gated by LIFETRAC_ENABLE_BENCH_RADIO) -> subprocess helper scripts in firmware/x8_lora_bootloader_helper + audit publish on MQTT control/bench_radio
  - note: The best-behaved control on the page: full error surfacing to #radio-bench-status plus console. 180 s subprocess timeout on the server; button not disabled during flight so double-clicks can run two helpers concurrently (minor).
- **Radio bench status label (#radio-bench-status)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:368
  - action: disabled <button> used as a status line; written by the bench-radio handler
  - backend: NONE
  - note: No handler by design (disabled, no data-radio attribute so the selector skips it).
- **Encode-mode pill short click (#encode-mode-pill)** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:301; app.js:461-614 (cycle fn 565-586, press wiring 587-614)
  - action: mouse/touch press <800 ms -> cycleEncodeMode(): shows 'switching...' overlay, POST cycle, on success clears canvas + 'loading: <mode>' overlay until next lifetrac-tile-painted event; pill re-rendered with tractor ack mismatch (requested->effective) from 5 s poll
  - backend: POST /api/encode_mode/cycle -> retained MQTT lifetrac/v25/control/encode_mode_override {mode} -> image_rx_daemon._on_encode_mode_msg -> 0xFB opcode 0x63 -> tractor camera_service; ack 0x68 -> retained lifetrac/v25/status/encode_mode -> web_ui cache -> GET /api/encode_mode/current poll
  - note: The control the user asked about. Wiring is complete end-to-end and the 6-mode UI list (app.js encMeta 462-469) exactly matches server _ENCODE_MODE_UI_CHOICES and _ENCODE_MODE_CYCLE_ORDER (web_ui.py:95-102,1933-1940). Defects: cycle/poll race can briefly revert the pill (finding 7), failed cycle is silent leaving stale pill (finding 7), and the 'loading' overlay is cleared by tiles of the OLD stream (finding 8). There is NO quality control on this page: image.encode_quality / image.tile_quality exist only behind /api/params (web_ui.py:47-53), which nothing on index.html calls.
- **Encode-mode pill long-press (>=800 ms)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:592-599
  - action: setTimeout 800 ms -> window.location = '/settings#encmode'
  - backend: GET /settings (web_ui.py:1020, session-gated redirect to /login when expired)
  - note: Route and settings.html exist. mouseleave cancels the timer without firing a click - correct.
- **Settings gear toggle (#settings-toggle)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:341, inline script 444-468
  - action: click toggles .open on #settings-drawer + aria-expanded
  - backend: NONE (pure client)
  - note: Both IDs exist; wired via bindToggle.
- **Telemetry sidebar toggle (#telemetry-toggle)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:330, inline script 444-468
  - action: click toggles .open on #telemetry-panel
  - backend: NONE (pure client)
  - note: Wired. Sidebar content itself has dead rows (findings 3, 10, 11).
- **Escape key binding** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:458-467
  - action: keydown Escape closes settings drawer and telemetry panel
  - backend: NONE
  - note: Only keyboard binding on the page. Notably there is NO keyboard E-stop; E-stop requires mouse/touch or gamepad START.
- **RAW MODE toggle (dynamically created #raw-mode-toggle)** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/img/raw_mode_toggle.js:18-62
  - action: click toggles body.raw-mode (CSS hides all overlay canvases), persists to localStorage, POSTs audit ping
  - backend: POST /api/audit/view_mode (web_ui.py:1192, unauthenticated) -> MQTT lifetrac/v25/control/view_mode qos1
  - note: Works, but raw-mode CSS (index.html:427-430) also hides #image-badges, which is where badge_renderer paints its fail-closed blackout of invalid-badge tiles - raw mode defeats the 'MUST refuse to display' contract (finding 9).
- **Footer links: audit log / settings** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:411-412
  - action: navigate to /audit and /settings
  - backend: GET /audit (web_ui.py:1028), GET /settings (web_ui.py:1020); audit.html and settings.html exist in web/
  - note: Both routes session-gated with redirect to /login.
- **Telemetry sidebar rows (display state, no handler)** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:376-388; app.js:188-229 (wsTele message handler)
  - action: populated from /ws/telemetry {topic,data} fan-out of MQTT topics
  - backend: WS /ws/telemetry (web_ui.py:1110, unauthenticated); topics: telemetry/engine|battery|hydraulics|gps|crop_health, video/active_camera, control/source_active
  - note: t-rpm/t-oil/t-bat/t-gps are self-described placeholders (raw msg.data, hex string for binary payloads). t-rssi is NEVER written by any code (finding 10). t-link-use/t-img-use/t-loss/t-encode listen on topics ('/status/link', dict-form '/source_active') that nothing publishes - the real publisher uses control/link_airtime (finding 3). No wsTele reconnect (finding 4).
- **Background: /ws/state image stream + overlays (no user handler)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/img/canvas_renderer.js:122-149 (WS + reconnect); badge_renderer.js:51 (POST /api/health/refusal)
  - action: paints tiles, dispatches lifetrac-state / lifetrac-tile-painted; badge refusals POSTed to server
  - backend: WS /ws/state (web_ui.py:1146, unauthenticated, 2 Hz snapshots); POST /api/health/refusal (web_ui.py:1173) -> MQTT control/badge_refusal
  - note: canvas_renderer has proper exponential-backoff reconnect (unlike wsTele) and is wss-aware (unlike app.js). Dead code: computeSyntheticScore/REAL_HOLD_MS/lastNonSyntheticTs computed but never acted on (finding 14); source_guard.js provides the actual banner.
- **Delivery beyond MQTT (cmd/control, cmd/estop, cmd/camera_select)** [`unverified`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/lora_bridge.py:193-195 (subscriber) vs base_station/image_rx_daemon.py:1020-1022 (subscribes only radio_profile, encode_mode_override, req_keyframe)
  - action: web_ui publishes drive/estop/camera commands to MQTT; only lora_bridge (own serial port) converts them to air frames - image_rx_daemon, which per the 3b1fb347 LoRa-only architecture owns the Murata radio, does NOT subscribe to cmd/control, cmd/estop or cmd/camera_select
  - backend: MQTT lifetrac/v25/cmd/{control,estop,camera_select}
  - note: Static review cannot confirm both processes run with separate radios. If only image_rx_daemon owns the single L072, every joystick frame, E-STOP and camera select terminates at the broker. Flagging for the system-level reviewer; encode_mode/radio_profile/req_keyframe are the only topics with a verified 0xFB path.

### Verified findings
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:384` — E-STOP (button click and gamepad START) is fire-and-forget with zero response or error handling
  - detail: fetch('/api/estop', {method:'POST'}) at app.js:384 and app.js:667 has no .then, no .catch, no UI feedback. The endpoint requires a valid session cookie (web_ui.py:1307 Depends(_require_session)) and sessions live only in the in-memory _sessions dict (web_ui.py:388), so any web_ui restart invalidates the open page's cookie. A 401, 5xx, or network error produces no visual change whatsoever, plus an unhandled promise rejection on network failure. Every other failure mode on this page at least changes some text; the single most safety-critical software control changes nothing.
  - failure: web_ui restarts while the operator console is open (sessions wiped). Operator sees the canvas still updating (unauthenticated /ws/state reconnects fine), hits E-STOP during an incident -> POST returns 401 -> no MQTT cmd/estop is published, no error is shown, operator believes the tractor was stopped.
  - verifier correction: Claim is accurate with two line-reference corrections and one strengthening addition. Corrections: the in-memory session store `_sessions: dict[str, float]` is defined at web_ui.py:374 (line 388 is the lookup inside `_session_valid`), and `Depends(_require_session)` is on web_ui.py:1308 (the route decorator is 1307). Addition: SESSION_TTL_S = 30 min idle timeout (web_ui.py:205) creates the same silent-401 E-STOP failure without any restart — and in that variant even the link-status pill still reads 'link: connected' because /ws/control validates the cookie only at connect time (web_ui.py:1219-1220), so there is no visible anomaly whatsoever. In the restart variant the link pill would show 'link: DISCONNECTED' (app.js:49-57), a weak ambient cue not tied to the E-STOP action.
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:366` — Button release swallowed while controls are locked leaves screenButtons/screenFlags latched, later re-transmitting take-control without operator intent
  - detail: set() at app.js:365-377 early-returns when controlsLocked, so a pointerup arriving during a handheld/autonomy lock never clears the bit in screenButtons/screenFlags. setSource (app.js:150-156) zeroes state.buttons/state.flags on lock but NOT the screenButtons/screenFlags accumulators. After unlock, the next press of ANY data-btn button recomputes state.buttons = screenButtons and state.flags = screenFlags (app.js:372-374), resurrecting the stale bit7 + flags bit0 (takectl_held). The gamepad-disconnect path (app.js:639-640) restores the same stale masks.
  - failure: Operator holds REQ CTRL when the handheld takes over -> controlsLocked=true -> operator releases the button but set(false) is discarded. Handheld later releases control; operator taps CURL -> the frame sent carries buttons bit7 AND flags 0x01 (take-control held), silently re-asserting base take-control and curling simultaneously.
  - verifier correction: All load-bearing references are accurate. One trivial imprecision: setSource spans app.js:140-163 (lock-zeroing block at 150-155), not 150-156 as cited. Additionally, the claim understates severity in two ways: (a) lines 686-687 also OR the stale screenButtons/screenFlags into every frame while a gamepad is active and unlocked, a third resurrection path; (b) the resurrected bit7/flags 0x01 persist across all subsequent frames (releasing the other button clears only its own bit), not just the single frame described.
- **[CONFIRMED] (medium)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:216` — Link-utilization telemetry rows listen on topics nobody publishes; the real publisher uses control/link_airtime
  - detail: setLinkStatus fires only for topics ending '/status/link' or dict-form '/source_active' (app.js:216-218). Repo-wide, nothing publishes lifetrac/v25/status/link, and lora_bridge deliberately publishes the airtime triple + encode_mode on the sibling retained topic lifetrac/v25/control/link_airtime (lora_bridge.py:685-692) specifically to avoid clobbering the tractor's 1-byte source_active. '/link_airtime' matches neither suffix, so t-link-use, t-img-use, t-loss and t-encode are permanently '—'. (app.js also only reads lowercase u_* keys, while the 0x10 decoder documents legacy U_* aliases.)
  - failure: Bridge publishes real airtime data every cycle; operator opens the telemetry sidebar to check P0 headroom before commanding a heavy image mode and sees dashes forever, concluding stats are simply not implemented — while U_total may actually be in alarm.

### Significant findings NOT adversarially verified (verify-cap overflow)
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:69` — /ws/telemetry has no reconnect logic — source banner, controls-lock state and camera echo freeze silently after one drop
  - detail: wsTele is constructed once at app.js:69. wsCtrl got a full auto-reconnect wrapper (app.js:42-61) but wsTele has none: after a server restart or transient drop, the telemetry socket stays closed for the life of the page. Everything it feeds freezes at the last value: SOURCE banner, controlsLocked (drives whether control frames are even sent), camera active-button echo, and all sidebar rows. The 5 s 'ping' interval (app.js:230-232) silently no-ops on a closed socket.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:49` — Auth-close 4401 on /ws/control is indistinguishable from an outage; page never prompts re-login
  - detail: web_ui closes an unauthenticated control socket with code 4401 (web_ui.py:1219-1222). app.js's close handler ignores ev.code and just backs off 1->10 s forever showing 'link: DISCONNECTED'. Since sessions are in-memory and the SPA never checks /api/session or redirects to /login, an expired/invalidated session turns into an infinite reconnect loop that can only be fixed by a manual page reload.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:396` — Camera-select ignores the HTTP response and the HTML shows cameras the build config may have filtered out
  - detail: The click handler (app.js:392-402) has no .then/.catch. Server-side, _CAMERA_IDS is filtered by BuildConfig (web_ui.py:1424-1442) and unknown names get HTTP 400 (web_ui.py:1753-1755), but index.html:356-361 hardcodes FRONT/REAR/IMPL/AUTO unconditionally. A 400/401/network error is indistinguishable from 'command sent, waiting for tractor echo' because the active highlight only ever comes from the /active_camera echo.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:571` — Encode-mode pill: cycle POST races the 5 s poll, and a failed cycle leaves stale pill state with no error
  - detail: refreshEncodeMode (app.js:552-564) and cycleEncodeMode (app.js:565-586) both write encCurrentMode with no in-flight guard. A poll response dispatched just before a cycle click can resolve after the cycle response and overwrite the new mode with the pre-cycle value (pill shows the old mode, possibly with a bogus mismatch arrow, until the next poll up to 5 s later). Separately, when the cycle POST fails (401 after restart, 500, network), the code path just hides the overlay: `r.ok ? r.json() : null` -> null -> silent; the pill keeps claiming the old mode with no indication the click did nothing.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:517` — 'loading: <mode>' overlay is cleared by any painted tile, including tiles of the OLD stream
  - detail: waitingForModeFrame is cleared by the first lifetrac-tile-painted event (app.js:517-521) with no check that the tile belongs to the new codec. After a mode switch, in-flight fragments of the previous stream (plus the server-side snapshot re-pushed at 2 Hz on /ws/state, which still contains the old canvas tiles) arrive and immediately dismiss the overlay, contradicting the stated contract ('until the first tile of the new stream is painted'). clearImageForModeSwitch also clears only the visible canvas, which the next 2 Hz snapshot promptly repaints with old-mode tiles.

### Low-severity notes
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:429` — raw-mode hides #image-badges, defeating the fail-closed blackout of invalid-badge tiles — badge_renderer.js:74-77 paints its mandatory blackout ('the browser MUST refuse to display' per IMAGE_PIPELINE §6) onto the #image-badges overlay canvas. The raw-mode CSS block (index.html:427-430) display:none's that same canvas, so with RAW MODE on, tiles with missing/malformed badges are shown as normal pixels. Refusals are still POSTed to /api/health/refusal, but the operator-facing enforcement disappears.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/index.html:380` — Telemetry sidebar RSSI row (t-rssi) is never written by any code — grep across base_station/web finds #t-rssi only in index.html. app.js's telemetry handler never touches it; the per-source rssi_dbm in the 0x10 JSON sidecar (topic_0x10_decoder.py sources map) is never read by the main page. RSSI does appear in the image link-speed line (from link_stats), but the sidebar row the label promises stays '—' forever.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:173` — 'Pkt loss' row is fed telemetry airtime utilization (u_telemetry), not packet loss — setLinkStatus writes `telem <pct(u_telemetry)>` into #t-loss whose visible label is 'Pkt loss' (index.html:381). u_telemetry is an airtime-utilization fraction from the bridge ledger, not a loss rate; the actual loss_rate lives in the 0x10 sidecar per-source map which is never read. Currently moot because the feeding topics are dead (see the link_airtime mismatch), but once that is fixed this row will actively mislead.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:44` — Control and telemetry WebSockets hardcode ws://, breaking behind any future TLS deployment — app.js:44 and app.js:69 use `ws://${location.host}/...` unconditionally, while canvas_renderer.js:23 correctly selects wss under https. web_ui.py's login-cookie comment explicitly anticipates a future nginx+TLS front (web_ui.py:1782-1784); if that happens the page loads but mixed-content blocking kills the control and telemetry sockets while the image keeps streaming.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:564` — 5 s encode-mode poll defeats the 30-minute idle session timeout — _session_valid implements a sliding TTL — every successful check refreshes the timestamp (web_ui.py:395). refreshEncodeMode polls the session-gated /api/encode_mode/current every 5 s (app.js:564) for as long as the tab exists, so an unattended open console keeps its PIN session alive indefinitely, nullifying SESSION_TTL_S = 30 min idle timeout (web_ui.py:205).
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/img/canvas_renderer.js:96` — Synthetic-content suppression in canvas_renderer is dead code — score computed, never acted on — SYNTHETIC_THRESHOLD, REAL_HOLD_MS and lastNonSyntheticTs (canvas_renderer.js:33-35) support a hold-last-real-frame feature the direct-paint rewrite explicitly abandoned; the getImageData sampling at lines 96-102 now runs a full-canvas readback after EVERY snapshot purely to update a timestamp no code reads. source_guard.js independently duplicates the same detection with an actual banner. Cost without function on every frame.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/app.js:673` — Gamepad encode-cycle (button 8) fires while controls are locked by handheld/autonomy — pressed(gp,8) -> cycleEncodeMode() at app.js:673-675 executes before the controlsLocked early-return at app.js:676. E-stop intentionally bypasses the lock; the encode cycle bypassing it looks accidental (every on-screen equivalent is gated: the pill is not, either, but the pill at least requires deliberate aiming; the shoulder-adjacent BACK button is easy to bump while the handheld operator is driving).

---
## Layer review: frontend-secondary

Static review of the 8 secondary web-UI pages (settings, config, diagnostics, map, audit, login) against web_ui.py, image_rx_daemon.py, camera_service.py, params_service.py, planner.py and state_publisher.py. Headline answers to the three focus questions: (a) ENCODE MODE selection IS active and correctly wired end-to-end (settings buttons -> POST /api/settings/encode_mode -> retained MQTT lifetrac/v25/control/encode_mode_override {"mode":name} -> image_rx_daemon -> 0xFB opcode 0x63 with the correct EncodeMode wire value), BUT two of the six advertised modes (btc4_per_tile=4, btc4_per_frame=5) are silently clamped to y_only by the tractor's _clamp_encode_mode and the settings page never surfaces the tractor ACK, so the pill shows a green lie; there is NO quality selector anywhere in the UI — image.encode_quality/image.tile_quality exist only in the params allow-list, whose transport is dead (see below). (b) RADIO PROFILE selector is correct: POST {"profile":"auto"|"0"|"1"|"2"} matches RadioProfileBody; concrete picks publish retained {"profile":N,"source":...} on lifetrac/v25/control/radio_profile (retain=True, qos=0) and the daemon acks retained on status/radio_profile/{tx,rx} with {ok,profile,ts} which the page renders with mismatch highlighting. The retained-flag incident risk RECURS in auto mode: a web_ui restart while selection=="auto" seeds AutoRadioPolicy from the module-default active profile 2 and immediately re-publishes retained {"profile":2} ("auto-startup"), re-commanding DTS BW500 even if auto had degraded to FHSS. (c) Settings persistence: accel toggle, PIN, encode mode and radio profile all round-trip correctly (file stores + re-GET). The "Tractor parameters" editor round-trip is IMPOSSIBLE three ways: web_ui's allow-list (flat image.*/ui.* keys) and the tractor params_service schema (nested hydraulic/joystick/link/key sections) reject each other's payloads in both directions, and the transport (base-broker lifetrac/v25/params/set -> tractor-localhost broker) does not exist under the LoRa-only control plane (no 0xFB opcode carries params). Two page-killing bugs found: the map page loads /static/vendor/leaflet/leaflet.js which exists nowhere in the repo (no nginx config ships it either), so L is undefined, map.js throws at line 23 and every autonomy control is left with no handler; and /ws/state's snapshot (state_publisher.py) contains no `link` or `gps` keys while diagnostics.js and map.js consume state.link.*/state.gps exclusively, so the entire diagnostics page and the map GPS/RSSI sidebar can never populate (RSSI/SNR data actually ships under state.link_stats, unread). config.html, audit.html and login.html are correctly wired to existing endpoints with matching payload shapes.

### Controls / surface inventory
- **Accel 'Re-scan hardware' button (#accel-refresh)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/settings.html:31 (handler :86)
  - action: click -> fetch POST
  - backend: POST /api/settings/accel/refresh (web_ui.py:1872) -> AcceleratorSelect.refresh().to_dict()
  - note: Response keys {present,usable,enabled,kind,last_error,active} match paint(); 401 not handled on this call (silently no-op).
- **Accel 'Use Coral when available' checkbox (#accel-toggle)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/settings.html:36 (handler :77)
  - action: change -> fetch POST {enabled:bool} then re-GET
  - backend: POST /api/settings/accel (web_ui.py:1857) -> settings_store image.coral_enabled
  - note: Round-trip verified: load() re-GETs /api/settings/accel which reflects the persisted flag.
- **Params 'Reload from tractor' button (#params-reload)** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/settings.html:108 (handler :129)
  - action: click -> fetch GET, fills #params-editor
  - backend: GET /api/params (web_ui.py:2069) <- cache fed by retained MQTT lifetrac/v25/params/changed (web_ui.py:900,989)
  - note: Tractor params_service publishes 'changed' only to the tractor-localhost broker (params_service.py:183); under the LoRa-only plane (commit 3b1fb347) nothing bridges it, so the cache stays empty forever — or worse, serves a stale pre-cutover retained snapshot as if live.
- **Params 'Send patch' button (#params-save)** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/settings.html:109 (handler :131)
  - action: click -> JSON.parse editor -> fetch POST whole object
  - backend: POST /api/params (web_ui.py:2081) -> publish lifetrac/v25/params/set qos=1 on the BASE broker
  - note: Triple-broken: (1) web_ui allow-list is flat keys image.coral_enabled/image.encode_quality/image.tile_quality/ui.theme/ui.language (web_ui.py:47) so re-sending the tractor snapshot (hydraulic/joystick/link/key sections) -> HTTP 400 'unknown parameter'; (2) even allow-listed keys are rejected by params_service.update() as unknown sections (params_service.py:125); (3) topic never reaches the tractor — no LAN MQTT, no 0xFB params opcode.
- **'Change PIN' button (#pin-save) + 3 password inputs** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/settings.html:163 (handler :174)
  - action: click -> client-validates \d{4,6} + confirm match -> fetch POST {current_pin,new_pin}
  - backend: POST /api/settings/pin (web_ui.py:1805) -> PIN_STORE_PATH, invalidates other sessions
  - note: Shapes match ChangePinBody; 401 detail ('invalid current PIN') surfaced via d.detail as coded.
- **Encode-mode button group (6 server-driven buttons in #encmode-buttons)** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/settings.html:206 (built :249-266, sender :268)
  - action: click -> POST {"mode":name}; GET on load builds buttons from data.choices
  - backend: POST /api/settings/encode_mode (web_ui.py:1915) -> file .encode_mode_override + retained MQTT lifetrac/v25/control/encode_mode_override {"mode":name} -> image_rx_daemon._on_encode_mode_msg (image_rx_daemon.py:682) -> 0xFB opcode 0x63 ENCODE_MODE, payload byte = EncodeMode[name].value (lora_proto.py:80: full=0,y_only=1,motion_only=2,btc4_per_tile=4,btc4_per_frame=5,mono_g4=6)
  - note: ACTIVE and wire-correct for full/y_only/motion_only/mono_g4. btc4_per_tile and btc4_per_frame are advertised in the catalogue and painted green when selected, but camera_service._clamp_encode_mode demotes them to y_only (camera_service.py:437-460); the tractor's clamp ACK is only exposed on /api/encode_mode/current which settings.html never reads. Also GET reads the persisted file, so the 'Current pin' pill diverges from the live mode after console/gamepad cycling (persist=False, web_ui.py:1993). No image QUALITY selector exists on any page.
- **Radio-profile button group (auto/0/1/2 in #radioprof-buttons)** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/settings.html:303 (built :359-373, sender :375, 5 s ack poll :398)
  - action: click -> POST {"profile":"auto"|"0"|"1"|"2"} (string); page polls GET every 5 s for acks
  - backend: POST /api/settings/radio_profile (web_ui.py:2025) -> file .radio_profile_override; concrete pick publishes RETAINED {"profile":N,"source":"operator"} qos=0 on lifetrac/v25/control/radio_profile (web_ui.py:305); 'auto' starts AutoRadioPolicy and immediately publishes its seed profile retained. image_rx_daemon._on_radio_profile_msg (image_rx_daemon.py:667) runs the two-phase 0xFB 0x65/0x66/0x67 switch and publishes retained acks {ok,profile,ts} on status/radio_profile/{tx,rx}
  - note: Payload shape matches contract (daemon reads only body['profile']; extra 'source' key tolerated). Ack rendering (tx/rx, FAILED, mismatch-orange) matches the daemon's ack schema. Caveat = finding 4: retained re-pin on web_ui restart in auto mode.
- **Config 'Preview diff' button (#preview-btn)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/config.html:69 (handler :183, fn :128)
  - action: click -> POST editor text as text/plain
  - backend: POST /api/build_config/preview-diff (web_ui.py:1640) -> {ok,changed_leaves,classes,worst_reload_class}
  - note: Shapes match; on expired session the 401 JSON is misrendered as 'Schema error: unknown' instead of redirecting.
- **Config 'Save' button (#upload-btn)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/config.html:70 (handler :184, fn :166)
  - action: click -> POST editor text as text/plain
  - backend: POST /api/build_config/upload (web_ui.py:1674) -> schema-validate, refuse firmware_required + unit_id mismatch, atomic write via installer_daemon, audit
  - note: Response {ok,sha256,worst_reload_class} matches; same missing-401-redirect nit as preview.
- **Config 'Reload from disk' button (#reset-btn)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/config.html:71 (handler :185)
  - action: click -> re-GET source + state
  - backend: GET /api/build_config/source (web_ui.py:1622) + GET /api/build_config/state (web_ui.py:1522)
  - note: State payload {unit_id,schema_version,sha256,source_path,restart_pending,last_event,last_event_reason} matches refreshState(); minor: meta-sha set via textContent so '&hellip;' renders literally (config.html:104).
- **'download installer bundle' link** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/config.html:38
  - action: navigate
  - backend: GET /config/download (web_ui.py:1452) -> config_bundle attachment + audit record
  - note: PIN-gated; 503 if build_config not loaded.
- **Diagnostics page data binding (no interactive controls; nav links only)** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/diagnostics.js:178 (applyState)
  - action: WebSocket /ws/state -> reads state.link.{image_util,telemetry_util,total_util,sf_rung,candidate_mode,encode_mode,fhss,link_loss}
  - backend: WS /ws/state (web_ui.py:1146) -> StatePublisher.snapshot()
  - note: snapshot() (state_publisher.py:81-102) has NO 'link' key at all — every panel (airtime, SF rung, FHSS heatmap, link-loss) and every stat row stays empty/'—' forever. Server-side data exists under state.link_stats (rssi/snr/timeouts) but is never read. Additionally snapshot ts_ms is time.monotonic-based while the FHSS heatmap buckets against Date.now(), so even a future 'link.fhss' feed would render nothing.
- **Map 'Start Drawing' button (#btn-draw)** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/map.html:98 (handler map.js:298)
  - action: click -> toggles drawModeActive; map clicks (map.js:310) append geofence vertices
  - backend: NONE (client-side only)
  - note: Handler never attaches: map.html:117 loads /static/vendor/leaflet/leaflet.js which does not exist anywhere in the repo (StaticFiles serves web/ only, web_ui.py:495; no nginx config ships vendor assets) -> L undefined -> map.js IIFE throws at line 23 before any addEventListener.
- **Map swath-width number input (#swath-width)** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/map.html:105 (read at map.js:342)
  - action: value read on Generate Sweep; parseFloat fallback 2.5
  - backend: feeds swath_width_m in POST /api/autonomy/calculate-sweep
  - note: Dead for the same leaflet-crash reason; latent boundary bug once fixed: HTML allows min=0.5/max=30.0 but GeofencePlanRequest uses exclusive gt=0.5/lt=30.0 (web_ui.py:2121) -> 422 at exactly 0.5 or 30.0, and map.js turns the 422 detail array into a garbled error message.
- **Map 'Generate Sweep' button (#btn-calc)** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/map.html:108 (handler map.js:340, fetch :350)
  - action: click -> POST {polygon:[[lat,lon]...], swath_width_m} -> draws returned waypoints
  - backend: POST /api/autonomy/calculate-sweep (web_ui.py:2123) -> planner.generate_sweep_coverage -> {ok,swath_width_m,waypoints:[{lat,lon,command}]}
  - note: Endpoint + payload shapes verified correct (planner.py:17,44), but the handler never attaches (leaflet crash). Even when fixed, the sweep is display-only: no endpoint transmits the plan to the tractor, yet the status text says 'Plan loaded!'.
- **Map 'Clear Plan' button (#btn-clear)** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/map.html:111 (handler map.js:330)
  - action: click -> removes layers, resets drawnPoints
  - backend: NONE (client-side only)
  - note: Handler never attaches (leaflet crash).
- **Map GPS/link sidebar + LINK up/down pills** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/map.js:164 (applyState)
  - action: WebSocket /ws/state -> reads state.gps, state.link.rssi_dbm/snr_db/encode_mode, state.link_power
  - backend: WS /ws/state (web_ui.py:1146)
  - note: Doubly dead: (1) leaflet crash prevents connect(); (2) snapshot() has no 'gps' or 'link' keys, so GPS rows and RSSI/SNR/range would stay '—' regardless. Only state.link_power (the S7.1 pills) actually exists in the snapshot.
- **Audit 'Last N lines' input (#limit)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/audit.html:35
  - action: value appended to fetch URL on load/refresh
  - backend: GET /api/audit?limit=N (web_ui.py:1069), server clamps 1..5000
  - note: Input max=5000 matches server clamp.
- **Audit 'Filter' text input (#filter)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/audit.html:39 (handler :103)
  - action: input event -> full re-fetch + client-side substring filter over JSON blob
  - backend: GET /api/audit (same endpoint; filtering is client-side)
  - note: Refetches on every keystroke — wasteful but functional.
- **Audit 'Refresh' button (#refresh)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/audit.html:41 (handler :102)
  - action: click -> load()
  - backend: GET /api/audit (web_ui.py:1069) -> {path,lines,error?}
  - note: Response shape matches renderer; 401 redirects to /login. Minor: r.event and non-numeric r.ts are interpolated into innerHTML unescaped (audit.html:79-84) — only the fields JSON goes through escapeHtml.
- **Audit 'auto (5 s)' checkbox (#autorefresh)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/audit.html:42 (handler :104)
  - action: change -> setInterval(load,5000)/clearInterval
  - backend: GET /api/audit every 5 s while checked
  - note: Timer correctly cleared on uncheck.
- **Login form (#login-form, PIN input, Sign in submit)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/login.html:33 (handler :47)
  - action: submit -> preventDefault -> fetch POST {pin}
  - backend: POST /api/login (web_ui.py:1762) -> HttpOnly SameSite=strict session cookie, 401 invalid, 429 lockout (5 fails/60 s per IP), 503 if no PIN configured
  - note: Shape matches LoginBody; 429 and generic-error branches handled; button re-enabled in finally.
- **Encode-mode cycle endpoint (referenced by settings docstring; consumed by operator console, not these pages)** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1974
  - action: POST advances runtime mode through the 6-mode cycle WITHOUT persisting
  - backend: POST /api/encode_mode/cycle -> retained MQTT encode_mode_override
  - note: Listed because it silently desynchronizes the settings page's 'Current pin' pill (GET /api/settings/encode_mode reads the file, not the runtime value).

### Verified findings
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/map.html:117` — Map page loads /static/vendor/leaflet/leaflet.js which exists nowhere in the repo, so map.js crashes on 'L' and every autonomy control is left without a handler
  - detail: web_ui mounts /static directly onto base_station/web/ (web_ui.py:495) and web/ contains no vendor/ directory; a repo-wide glob for leaflet* returns nothing, and no nginx config exists in the repo to supply it (the only deployment unit is systemd/lifetrac-base.service). leaflet.css and leaflet.js therefore 404. map.js line 23 executes L.map(...) at module scope inside the IIFE, throws ReferenceError, and none of the subsequent code runs: no WS connect, no GPS sidebar updates, and btn-draw/btn-calc/btn-clear/swath-width are never wired.
  - failure: Operator opens /map -> blank dark page with a dead sidebar of em-dashes; clicking 'Start Drawing' or 'Generate Sweep' does nothing at all, silently.
  - verifier correction: Minor refinement only: the deployment unit is systemd/lifetrac-base.service -> docker compose (docker-compose.yml + Dockerfile), not the service file alone; that chain likewise contains no nginx service and no Leaflet fetch step, so the built image cannot contain /static/vendor/leaflet/ and both leaflet.css and leaflet.js 404 as claimed.
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_pipeline/state_publisher.py:81` — /ws/state snapshot has no 'link' or 'gps' keys, but diagnostics.js and map.js consume state.link.* and state.gps exclusively — the whole diagnostics page and the map GPS/RSSI sidebar can never populate
  - detail: StatePublisher.snapshot() emits {ts_ms,grid,tiles,accel_status,encode_mode,detections,safety_verdict,needs_keyframe,last_keyframe_reason,link_power,link_stats}. diagnostics.js applyState (line 180) reads only state.link.{image_util,telemetry_util,total_util,sf_rung,sf_history,fhss,link_loss,encode_mode,candidate_mode}; map.js (lines 166-207) reads state.gps and state.link.{rssi_dbm,snr_db,encode_mode}. None of those keys exist. The RSSI/SNR data the pages want actually arrives as state.link_stats (fed from lifetrac/v25/video/link_stats at web_ui.py:806) but neither page reads it. Secondary defect: snapshot ts_ms is time.monotonic-based while diagnostics' FHSS heatmap buckets events against Date.now()/1000, so the frontends and backend also disagree on the timebase.
  - failure: Operator opens /diagnostics during a live run to check airtime before a profile switch -> all four canvases stay empty and every stat row reads '—' even though link_stats is flowing; on /map the RSSI/est-range rows never move, so the coarse range readout the page advertises is never available.
  - verifier correction: Claim is accurate except the word 'exclusively' for map.js: map.js:215 also consumes state.link_power, which IS present in the snapshot (fed at web_ui.py:871-877), so the map page's LINK pill row (link-pill-up/link-pill-dn/link-reason) does populate. The dead elements are precisely: the entire diagnostics page (all four canvases and every state.link.*-driven stat row), and on /map the GPS fix/lat/lon/hdop/sats/speed rows, marker/trail, and the link-rssi/link-snr/link-range/link-mode rows. RSSI/SNR exist in snapshot.link_stats but neither page reads that key. Secondary timebase defect (monotonic ts_ms vs Date.now() bucketing in the FHSS heatmap, and likewise map.js gps-age sweep) is confirmed as stated.
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:2109` — Settings 'Tractor parameters' editor round-trip is impossible: web_ui's allow-list and the tractor params_service schema reject each other's payloads in both directions, and the params/set topic has no transport to the tractor under the LoRa-only control plane
  - detail: POST /api/params validates against _ALLOWED_PARAM_KEYS = flat dotted keys {image.coral_enabled,image.encode_quality,image.tile_quality,ui.theme,ui.language} (web_ui.py:47-53), then publishes to lifetrac/v25/params/set on the BASE broker. The tractor's params_service.update() (params_service.py:118-127) accepts only nested top-level sections {hydraulic,joystick,link,key} — every allow-listed key would be rejected as an unknown section. Conversely, reloading the tractor snapshot into the editor and re-sending it fails web_ui validation with 400 'unknown parameter hydraulic'. And the transport itself is gone: params_service connects to localhost on the tractor X8 (params_service.py:183), there is no LAN MQTT to the tractor (commit 3b1fb347), and no 0xFB opcode carries params. The GET side is equally dead (retained params/changed never reaches the base broker) or, worse, serves a stale pre-cutover retained snapshot as if live.
  - failure: Operator edits joystick.deadband_pct in the editor and clicks 'Send patch' -> HTTP 400 'unknown parameter'; if they instead type image.tile_quality (the only quality knob in the system) the POST returns 200 'patch sent — waiting for confirm…' but the patch lands on the base broker, is consumed by nobody, and the 750 ms reload shows the unchanged (empty or stale) snapshot.
  - verifier correction: Two trivial phrasing nuances only: the HTTP 400 detail is the stringified pydantic ValidationError (which contains "Value error, unknown parameter 'hydraulic'"), not the bare string 'unknown parameter hydraulic'; and the browser status line shows only 'send failed: HTTP 400' because settings.html does not render the response detail. Additionally the incompatibility is even stronger than stated: even if a flat allow-listed key somehow reached the tractor, params_service would silently drop it (LOG.warning, no NACK), so no confirm could ever arrive.

### Significant findings NOT adversarially verified (verify-cap overflow)
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:553` — With radio profile 'auto' selected, a web_ui restart re-publishes retained {"profile":2} seeded from the module default instead of the last commanded profile — recurrence of the retained-pin incident class
  - detail: Startup restore (web_ui.py:546-556) runs at import time, before any retained ack or link_stats can arrive, so _radio_profile_active still holds its initializer value 2 (web_ui.py:281). AutoRadioPolicy(initial_profile=2) is created and _publish_radio_profile(2,'auto-startup') immediately publishes retained {"profile":2} on lifetrac/v25/control/radio_profile. If the auto policy had previously degraded the link to FHSS(1) because of interference, the restart force-recommands DTS BW500 mid-run and the daemons execute a live two-phase switch back onto the profile that was failing; the policy then needs a fresh degradation cycle (min-dwell 60 s) to recover.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/settings.html:236` — Encode-mode selector advertises btc4_per_tile and btc4_per_frame, which the tractor silently clamps to y_only, and the settings page never surfaces the tractor's clamp ACK
  - detail: The server catalogue (_ENCODE_MODE_UI_CHOICES, web_ui.py:95) includes both BTC4 modes and the wire path delivers them correctly (0xFB 0x63, values 4/5), but camera_service._ENCODE_MODE_IMPLEMENTED (camera_service.py:437-444) excludes them and _clamp_encode_mode demotes to ENCODE_MODE_Y_ONLY. The clamp is reported in the tractor's retained status/encode_mode ack and exposed via GET /api/encode_mode/current (web_ui.py:1894, fields tractor/rx_codec_name), but settings.html only reads GET /api/settings/encode_mode which carries no ack — the button paints green and the pill shows 'BTC4 / tile' as if active.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1889` — Settings page 'Current pin' can silently diverge from the live encode mode after console/gamepad cycling because GET reads the persisted file while /api/encode_mode/cycle changes only the runtime override
  - detail: POST /api/encode_mode/cycle (web_ui.py:1974) calls _set_encode_mode_override(persist=False): it publishes the retained MQTT override (so the tractor really switches) but does not write .encode_mode_override. GET /api/settings/encode_mode returns _load_encode_mode_override() from the file. The settings page therefore highlights a mode that is not on the air. A later web_ui restart republishes the stale file value (web_ui.py:531-537), reverting the operator's cycled choice.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:49` — No image-quality selector exists anywhere in the UI; the only quality knobs (image.encode_quality, image.tile_quality) are reachable solely through the dead params path
  - detail: The user asked whether quality selection is active: it is not. The settings page has method (encode mode) buttons only. image.encode_quality/image.tile_quality appear in _ALLOWED_PARAM_KEYS but that POST path never reaches the tractor (see the params finding), and the tractor's actual quality ceilings are env vars (LIFETRAC_MOTION_ONLY_QUALITY etc., camera_service.py:463) plus hardcoded WEBP_QUALITY. There is no MQTT topic, LoRa opcode, or endpoint that changes encode quality at runtime.

### Low-severity notes
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:2121` — Sweep swath bounds are exclusive on the server (gt=0.5, lt=30.0) but inclusive in the map UI (min=0.5, max=30.0), and the resulting 422 detail renders as a garbled error — GeofencePlanRequest.swath_width_m uses gt/lt so exactly 0.5 or 30.0 — values the number input explicitly permits — fail validation with a FastAPI 422 whose detail is a list of dicts; map.js:363 does new Error(errorData.detail) turning it into '[object Object]'.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/map.js:404` — 'Generate Sweep' claims 'Plan loaded!' but the plan is display-only — no endpoint transmits the waypoints to the tractor — The calculate-sweep response is only drawn as a polyline plus start/end markers; there is no follow-up POST, MQTT publish, or 0xFB frame that delivers the waypoint list to the vehicle, and no such opcode exists in the LoRa command contract.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/audit.html:79` — Audit table interpolates r.event and non-numeric r.ts into innerHTML without escaping — only the fields JSON goes through escapeHtml — tr.innerHTML embeds fmtTs(r.ts) (returns String(ts) raw when ts is not a number) and ${ev} raw; the CSS-class copy is sanitized but the visible text is not. Audit lines are locally generated JSONL today, but audit_log records event names and fields derived from MQTT traffic, so a crafted event string in a log line would execute as HTML in the operator's authenticated session.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/config.html:104` — SHA-256 ellipsis rendered as the literal text '&hellip;' because it is assigned via textContent — $("meta-sha").textContent = d.sha256.slice(0,16) + "&hellip;" — textContent does not decode entities; every other entity on the page is inside HTML markup or innerHTML and renders fine.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web/config.html:135` — Preview/Save do not handle session expiry: a 401 response is misreported as 'Schema error: unknown' / 'Upload rejected: unknown' instead of redirecting to /login — refreshState() and loadEditor() check r.status===401 and redirect, but previewDiff() and upload() parse the 401 JSON body ({detail:'PIN required'}) as a diff result; d.ok is undefined so the error banner shows 'unknown'.

---
## Layer review: server

Full inventory of base_station/web_ui.py (all 2137 lines read): 30 HTTP routes + 4 WebSockets, cross-checked against every fetch()/WebSocket call in base_station/web/ and against the MQTT subscribers in image_rx_daemon.py, lora_bridge.py, and firmware/tractor_x8/params_service.py. (a) ENCODE MODE: POST /api/settings/encode_mode and /api/encode_mode/cycle publish {"mode":"<name>"} RETAINED qos 0 on lifetrac/v25/control/encode_mode_override; tractor ack (retained status/encode_mode, incl. clamp info) is cached server-side but surfaced only via GET /api/encode_mode/current (polled by index, not by the settings page, whose "pinned" message is driven by paho's local qos-0 enqueue rc); cycle publishes retained WITHOUT persisting, so broker pin and disk store diverge across restarts. Image QUALITY (image.encode_quality/tile_quality) rides POST /api/params -> lifetrac/v25/params/set qos 1 non-retained, which has NO base-side consumer post LoRa-only migration - a dead endpoint pair (GET cache never populates). (b) RADIO PROFILE: POST /api/settings/radio_profile publishes {"profile":N,"source":...} RETAINED qos 0 on lifetrac/v25/control/radio_profile; the server NEVER clears the retained pin, "auto" immediately publishes a retained concrete seed and every auto switch publishes retained; on every boot web_ui overwrites the broker's retained value from disk, and in auto mode the seed is the unpersisted module default 2 - a restart while auto had degraded to profile 1 re-commands profile 2 mid-run, reproducing the prior stale-retained-pin incident class (high finding). (c) DRIVE CONTROL: /ws/control is cookie-authed, pydantic-validated, 512 B capped, 20 Hz rate-limited, with a 150 ms deadman halt and halt-on-disconnect publishing packed frames to lifetrac/v25/cmd/control qos 0 - but up to 4 sockets publish concurrently with no single-writer arbitration (every tab auto-connects and streams zeros; background tabs become halt-frame generators), and the deadman lives on an event loop that POST /api/bench/radio can freeze for 180 s via an in-handler subprocess.run (default-enabled in production). (d) ORPHANS/MISSING: no frontend call targets a missing route; orphan routes are /ws/image, /api/logout, /api/session, plus dead /api/params GET+POST. Biggest auth gap: POST /api/health/refusal and /api/audit/view_mode are unauthenticated, size-uncapped MQTT-publish primitives into control/#. settings_store.py and audit_log.py themselves are sound (thread-safe, atomic writes, never-raise logging).

### Controls / surface inventory
- **GET / (index page)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1002
  - action: Serves web/index.html; 303-redirects to /login when session cookie invalid
  - backend: NONE (static)
  - note: Blocking read_text() in async handler (trivial). Same pattern for /settings:1020, /audit:1028, /diagnostics:1036, /map:1045, /config:1055; /login:1015 is unauthenticated by design.
- **GET /api/audit** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1069
  - action: Tails last N (1..5000) lines of the audit JSONL and returns parsed JSON
  - backend: reads LIFETRAC_AUDIT_PATH / audit_log.DEFAULT_PATH (/var/log/lifetrac/audit.jsonl)
  - note: Auth: yes (Depends _require_session). Blocking chunked file read runs on the event loop (multi-MB file = brief stall). Called by web/audit.html:94.
- **WS /ws/telemetry** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1110
  - action: Fans every subscribed MQTT topic (telemetry/#, status/#, video/#, control/#, params/changed) to the browser as {topic,data} JSON; inbound WS text ignored (keepalive)
  - backend: MQTT subscribe -> WS push; no publishes
  - note: Unauthenticated (documented decision); cap 8 via _admit_ws, over-cap closed 4429. Note it also echoes web_ui's own retained control/encode_mode_override and control/radio_profile publishes to any LAN client. Caller: app.js:69.
- **WS /ws/image** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1124
  - action: Streams reassembled canvas blobs (binary) from MQTT lifetrac/v25/video/canvas
  - backend: MQTT lifetrac/v25/video/canvas -> ws.send_bytes
  - note: No frontend caller: canvas_renderer.js:24 uses /ws/state, and no file in web/ opens /ws/image. Unauthenticated, cap 4. Orphan route.
- **WS /ws/state** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1146
  - action: Pushes StatePublisher JSON snapshot on connect, then every 500 ms; also pushed from the MQTT thread on every canvas update via _publish_state_snapshot (line 929)
  - backend: in-process _image_publisher.snapshot(); no publishes
  - note: Unauthenticated, cap 8. Callers: canvas_renderer.js:124, map.js:232, diagnostics.js:226. Two send paths (handler loop + run_coroutine_threadsafe from MQTT thread) can interleave sends on one socket - starlette WS send is not concurrency-safe (low-probability corruption/exception).
- **POST /api/health/refusal** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1173
  - action: Accepts arbitrary JSON body, republishes it verbatim
  - backend: MQTT publish lifetrac/v25/control/badge_refusal, qos=1, not retained
  - note: NO AUTH, no body size cap, no schema. Any LAN client gets an MQTT-publish primitive into the control/# namespace + audit-firehose pollution. Caller: img/badge_renderer.js:51.
- **POST /api/audit/view_mode** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1192
  - action: Accepts arbitrary JSON body ({raw_mode,ts_ms} expected), republishes it verbatim
  - backend: MQTT publish lifetrac/v25/control/view_mode, qos=1, not retained
  - note: Same defect as /api/health/refusal: no auth, no size cap, no validation. Caller: img/raw_mode_toggle.js:44.
- **WS /ws/control (drive/hydraulics)** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1208
  - action: Receives JSON ControlMsg {lhx,lhy,rhx,rhy,buttons,flags} (pydantic, extra=forbid, ranges -127..127 / 0..0xFFFF / 0..0xFF; raw text >512 B dropped); packs via lora_proto.pack_control and publishes; server rate-limits to 20 Hz; 150 ms receive-timeout deadman publishes an all-zero halt frame; halt also sent on disconnect; frames suppressed while active_source is handheld/autonomy
  - backend: MQTT publish lifetrac/v25/cmd/control, qos=0, not retained (consumed by base_station/lora_bridge.py:193 -> LoRa)
  - note: Auth: session cookie checked manually (close 4401). Cap MAX_CONTROL_SUBSCRIBERS (BuildConfig ui.max_control_subscribers, default 4) - but there is NO single-writer arbitration: every admitted socket publishes with independent seq/hb to the same topic (see finding 4). Deadman depends on the event loop being free (see finding 1). Caller: app.js:44/178/181.
- **POST /api/estop** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1307
  - action: Software E-stop
  - backend: MQTT publish lifetrac/v25/cmd/estop, empty payload, qos=1 (lora_bridge.py:194 -> LoRa)
  - note: Auth-gated: an expired session makes the UI E-stop silently 401 (app.js:384/667 never checks the response); hardware E-stop is the documented auth-independent backstop. Callers: app.js:384 (button), app.js:667 (gamepad START, edge-triggered).
- **POST /api/bench/radio** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1383
  - action: Runs bench helper script (powershell run_radio_sleep.ps1 / run_radio_wake_rxcont.ps1 or python fallbacks) with subprocess.run(timeout=180) DIRECTLY in the async handler, then publishes the result
  - backend: MQTT publish lifetrac/v25/control/bench_radio {mode,ok,returncode}, qos=1, not retained
  - note: Auth: yes + LIFETRAC_ENABLE_BENCH_RADIO gate - but the gate DEFAULTS TO ENABLED ("1"). Body validated (mode sleep|wake). Blocks the entire event loop up to 180 s (finding 1). Caller: app.js:412.
- **GET /config/download** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1452
  - action: Builds and downloads the installer bundle of the active BuildConfig; records config_download audit event
  - backend: reads BUILD.source_path; audit_log record; no MQTT
  - note: Auth: yes. Caller: config.html:38 link.
- **GET /api/build_config/state** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1522
  - action: Polls ConfigWatcher (stat + possible reload), returns unit_id/sha/restart_pending/diff; audits non-noop events
  - backend: in-process watcher; audit_log; no MQTT
  - note: Auth: yes. Caller: config.html:99.
- **GET /api/build_config/source** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1622
  - action: Returns raw TOML of active build config
  - backend: reads BUILD.source_path
  - note: Auth: yes. Caller: config.html:122.
- **POST /api/build_config/preview-diff** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1640
  - action: Validates candidate TOML (text/plain body) via temp file + build_config.load, returns reload-class diff; no side effects
  - backend: NONE (validation only)
  - note: Auth: yes. No body size cap (whole body decoded to str). _load_body_for_validation (line 1591) mutates process-global env LIFETRAC_BUILD_CONFIG_PATH - safe only because there is no await inside; fragile. Caller: config.html:129.
- **POST /api/build_config/upload** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1674
  - action: Validates TOML, refuses unit_id mismatch and firmware_required diffs, atomic-writes over BUILD.source_path via installer_daemon._atomic_write, audits
  - backend: disk write of BUILD.source_path; audit_log; no MQTT
  - note: Auth: yes. No body size cap. Overwrites the live config file that the watcher then applies - correct per BC-10 contract. Caller: config.html:167.
- **POST /api/camera/select** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1743
  - action: Validates camera name against BuildConfig-filtered table (auto/front/rear/implement/crop), publishes 1-byte camera id
  - backend: MQTT publish lifetrac/v25/cmd/camera_select, bytes([id]), qos=1 (lora_bridge.py:195 -> FT_COMMAND/CMD_CAMERA_SELECT over LoRa)
  - note: Auth: yes. Pydantic body + allow-list. Confirmation is the tractor's active_camera telemetry echo (app.js:196). Caller: app.js:396.
- **POST /api/login** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1762
  - action: Constant-time PIN compare, per-IP lockout (5 fails -> 60 s, audited), issues 32-byte session token cookie (HttpOnly, SameSite=strict, 30 min TTL, no Secure - plain-HTTP LAN)
  - backend: in-process session dict; audit_log pin_failure/pin_lockout
  - note: PIN sources: .operator_pin file > LIFETRAC_PIN_FILE > LIFETRAC_PIN env > default "1234". X-Forwarded-For honored only from LIFETRAC_TRUSTED_PROXIES. Caller: login.html:52.
- **POST /api/logout** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1790
  - action: Drops session token, deletes cookie
  - backend: in-process session dict
  - note: No frontend caller anywhere in web/ - orphan route (works if called manually).
- **GET /api/session** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1800
  - action: Returns {authenticated: bool}
  - backend: NONE
  - note: No frontend caller - orphan route.
- **POST /api/settings/pin** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1805
  - action: Requires session + current PIN (constant-time); new PIN pattern ^\d{4,6}$; atomic-persists to PIN_STORE_PATH chmod 600; invalidates all other sessions
  - backend: disk write .operator_pin
  - note: Global LIFETRAC_PIN reassigned without a lock (benign). Caller: settings.html:183.
- **GET+POST /api/settings/accel, POST /api/settings/accel/refresh** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1847
  - action: GET returns accelerator state; POST {enabled:bool} persists image.coral_enabled via settings_store.SettingsStore.set (thread-safe atomic JSON write, default /var/lib/lifetrac/settings.json); refresh forces re-detection
  - backend: settings_store.py set(); image_pipeline.accel_select; no MQTT
  - note: Auth: yes on all three. refresh() probes hardware synchronously in async handler (brief block). Callers: settings.html:72/78/87.
- **GET /api/settings/encode_mode** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1883
  - action: Returns {current: <disk-persisted mode>, choices}
  - backend: reads .encode_mode_override file
  - note: Returns the DISK value, not the runtime override - after POST /api/encode_mode/cycle (persist=False) the settings page paints a stale 'Current pin'. Caller: settings.html:250.
- **GET /api/encode_mode/current** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1894
  - action: Returns runtime override + tractor ack (_encode_mode_ack, cached from retained lifetrac/v25/status/encode_mode at line 819) + rx_codec/rx_codec_name from link_stats (implicit over-the-air ack)
  - backend: in-process caches
  - note: Auth: yes. This is the ONLY place the encode-mode confirmation reaches a client; app.js polls it every 5 s (app.js:553/564). The settings page never consults it.
- **POST /api/settings/encode_mode** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1915
  - action: Validates mode (pydantic pattern, 6 choices), persists to disk, sets runtime override, publishes
  - backend: MQTT publish lifetrac/v25/control/encode_mode_override, payload {"mode":"<name>"}, RETAINED, qos=0 (consumed by lora_bridge.py:204 and image_rx_daemon.py:1021 -> 0xFB opcode 0x63)
  - note: Auth: yes. Response 'delivered' is only paho enqueue rc==0 (qos 0), not broker/tractor delivery; no ack is awaited or returned - settings.html:284 shows 'pinned (bridge will apply within ~1 s)' on enqueue alone. Retained pin also republished at startup from disk (line 534). Caller: settings.html:272.
- **POST /api/encode_mode/cycle** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1974
  - action: Rotates runtime mode through the 6-entry cycle order and publishes, WITHOUT persisting to disk
  - backend: MQTT publish lifetrac/v25/control/encode_mode_override {"mode":...}, RETAINED, qos=0
  - note: Retained-but-not-persisted: broker pin and disk store diverge until the next web_ui restart force-overwrites the retained value with the older disk mode (line 534) - which encoder is active then depends on which process restarts first. Callers: app.js:571 (pill), app.js:673 (gamepad BACK).
- **GET /api/settings/radio_profile** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1999
  - action: Returns {current mode (may be 'auto'), choices, labels, active_profile, acks.tx/rx (cached from retained lifetrac/v25/status/radio_profile/{tx,rx} at line 828), link_stats_age_s}
  - backend: in-process caches
  - note: Auth: yes. Ack loop for radio profile IS surfaced here (unlike encode mode's settings path). Caller: settings.html:360.
- **POST /api/settings/radio_profile** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:2025
  - action: Validates profile pattern ^(auto|0|1|2)$; persists selection; concrete pick publishes immediately; 'auto' instantiates AutoRadioPolicy seeded from _radio_profile_active and immediately publishes the seed
  - backend: MQTT publish lifetrac/v25/control/radio_profile, payload {"profile":N,"source":"operator"|"auto-start"}, RETAINED, qos=0 (image_rx_daemon.py:667 two-phase 0x65/0x66/0x67 to tractor)
  - note: Auth: yes. The retained pin is NEVER cleared (no empty-payload publish exists anywhere in web_ui.py) - selecting 'auto' still leaves a retained concrete {"profile":N}; the auto worker (line 559) publishes retained source:"auto" on every switch; startup (line 549/553) unconditionally overwrites the broker's retained value (finding 2). active_profile in the response read without the lock (line 2063). Caller: settings.html:379.
- **GET /api/params** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:2069
  - action: Returns cached tractor param snapshot fed from retained lifetrac/v25/params/changed
  - backend: in-process cache
  - note: params/changed is published by firmware/tractor_x8/params_service.py on the TRACTOR broker; post commit 3b1fb347 there is no LAN MQTT to the tractor, so the base-broker cache never populates - settings page shows 'no snapshot yet' forever. Caller: settings.html:120.
- **POST /api/params (incl. image.encode_quality / image.tile_quality)** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:2081
  - action: Size-caps body at 4096 B, allow-lists keys (image.coral_enabled, image.encode_quality, image.tile_quality, ui.theme, ui.language), scalar-only + 256-char string cap, forwards patch
  - backend: MQTT publish lifetrac/v25/params/set, JSON patch, qos=1, NOT retained
  - note: No base-side consumer: image_rx_daemon and lora_bridge do not subscribe to params/set; only tractor-side params_service.py:184 does, on the unreachable tractor broker. So the image QUALITY knobs currently go nowhere and no confirmation ever returns (settings.html:141 waits forever). Also no type/range validation on values (e.g. encode_quality: -5 or "banana" passes). Caller: settings.html:135.
- **POST /api/autonomy/calculate-sweep** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:2123
  - action: Runs planner.generate_sweep_coverage(polygon, swath) synchronously and returns waypoints
  - backend: in-process planner; no MQTT
  - note: Auth: yes. swath_width_m bounded (0.5..30) but polygon vertex list is UNBOUNDED and lat/lon values unchecked; CPU-bound planning runs on the event loop; bare except returns str(exc) to the client. Caller: map.js:350.
- **startup retained republish (module import)** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:531
  - action: On every boot re-publishes encode_mode_override (line 534) and radio_profile (line 549 concrete / 553 auto) from local disk stores
  - backend: MQTT retained publishes on lifetrac/v25/control/encode_mode_override and lifetrac/v25/control/radio_profile
  - note: Overwrites whatever the broker currently retains with disk-derived state; in auto mode the seed is the UNPERSISTED module default 2 (line 282) - see finding 2. Also _connect_mqtt_with_retry (line 501) blocks import up to 30 s.
- **keyframe self-heal publish** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:969
  - action: When Canvas.apply asks for a keyframe, publishes the reason string (64 B cap)
  - backend: MQTT publish lifetrac/v25/cmd/req_keyframe, qos=1 (image_rx_daemon.py:1022 + lora_bridge.py:198 -> 0xFB 0x60 REQ_KEYFRAME)
  - note: Runs on the paho thread, not the event loop - correct.
- **/static mount** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:495
  - action: Serves everything under base_station/web/ unauthenticated
  - backend: NONE (static)
  - note: All JS/HTML including settings.html markup is fetchable without a session (content only; the APIs they call are gated).

### Verified findings
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1391` — POST /api/bench/radio runs subprocess.run(timeout=180) synchronously inside an async handler, freezing the entire event loop
  - detail: _run_bench_radio_script (line 1349) is called directly from async api_bench_radio with no thread/executor offload. While it runs, NO coroutine executes: /ws/control cannot receive browser frames, and - worse - its 150 ms asyncio.wait_for deadman cannot fire either, so the base publishes neither fresh control frames NOR the zero halt frame. All WS fan-out and every other API stalls too. The route is reachable in production because LIFETRAC_ENABLE_BENCH_RADIO defaults to enabled (line 1387).
  - failure: Operator is driving via /ws/control and clicks the bench 'wake' button (app.js:412); the helper script hangs on a serial port for up to 180 s; the tractor keeps executing the last received drive command with no updates and no base-side halt until the tractor-side LoRa watchdog trips.
  - verifier correction: Claim is accurate as stated; one minor refinement: the helper does not open a local serial port directly — run_radio_wake_rxcont.ps1 pushes probe scripts over adb and runs them against /dev/ttymxc3 on the remote SoC, so the up-to-180 s hang comes from adb/remote-serial operations (device offline, USB flakiness, port contention). This does not change the defect or its impact.
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:553` — Radio-profile retained pin is never cleared and is unconditionally overwritten on every web_ui boot; in auto mode the boot value is the unpersisted default (2), re-commanding a stale profile mid-run - the exact prior-incident class
  - detail: _publish_radio_profile (line 305) always publishes retained {"profile":N,"source":...} on lifetrac/v25/control/radio_profile and nothing in the server ever publishes an empty payload to clear it. The disk store persists only the SELECTION mode ('auto'/'0'/'1'/'2'), never the last commanded profile; _radio_profile_active is a module literal 2 (line 281). On restart with mode='auto', line 553 publishes retained {"profile":2,"source":"auto-startup"} even if the pre-restart auto policy had degraded the fleet to profile 1 - and web_ui never reads the broker's existing retained value to reseed (its _on_mqtt_message has no branch for control/radio_profile despite subscribing control/#). Concrete-mode boots (line 549, source 'startup') likewise clobber any newer retained value from another writer. The auto worker (line 599) also makes every transient auto pick a permanent retained pin.
  - failure: Auto policy degrades the link to FHSS profile 1 during interference; web_ui restarts (crash/deploy); it publishes retained profile 2, image_rx_daemon immediately runs the two-phase switch back to DTS BW500 mid-run, causing a reconfig outage and pinning a profile the RF environment cannot sustain - a repeat of the stale-retained-pin incident.
  - verifier correction: Claim is accurate as stated, with two precisions: (a) "pinning a profile the RF environment cannot sustain" is guaranteed only for the auto policy's 60 s MIN_SWITCH_GAP_S dwell (plus unhealthy-detection latency) while web_ui stays alive — the auto worker will then re-degrade, at the cost of an additional reconfig-outage flap; the pin is indefinite only if web_ui is down/crash-looping or link_stats never arrive. (b) The missing reseed is doubly unreachable: besides `_on_mqtt_message` having no control/radio_profile branch, the startup publish (web_ui.py:549/553) runs before `_subscribe_mqtt_topics()` (line 912), so the broker's retained value is overwritten before web_ui could ever receive it.
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1174` — POST /api/health/refusal and POST /api/audit/view_mode (line 1193) are unauthenticated and publish arbitrary, unbounded client JSON onto the control/# MQTT namespace
  - detail: Neither route has Depends(_require_session), neither caps request size (await request.json() parses whatever arrives), and neither validates schema. The body is republished verbatim to lifetrac/v25/control/badge_refusal / control/view_mode at qos=1. That hands any unauthenticated LAN client a broker-publish primitive: audit-firehose pollution (audit_log --tail-mqtt records lifetrac/v25/#), garbage echoed to all /ws/telemetry subscribers (web_ui subscribes control/#), and memory pressure from multi-MB JSON bodies.
  - failure: Any device on the LAN loops POST /api/health/refusal with 50 MB JSON bodies without ever logging in: event-loop stalls parsing, broker traffic spikes, audit JSONL rotates real events away, and the operator's telemetry sidebar receives attacker-controlled payloads.
  - verifier correction: The defect stands as written; two impact clauses need narrowing.

(a) "publish onto the control/# namespace" is true only for two fixed leaf topics: `lifetrac/v25/control/badge_refusal` and `lifetrac/v25/control/view_mode`. The topic string is a literal at web_ui.py:1185 and :1201 — the attacker controls the payload, not the topic. Grepping the whole DESIGN-CONTROLLER tree for `badge_refusal|view_mode` finds no consumer other than the two firehose subscriptions (web_ui `control/#` and audit_log `lifetrac/v25/#`). So this is a pollution / log-eviction / resource-exhaustion primitive, NOT a control-injection primitive: it cannot reach `control/radio_profile`, the 0xFB command path, or any tractor actuation.

(b) "the operator's telemetry sidebar receives attacker-controlled payloads" — the payload does reach every `/ws/telemetry` socket (web_ui.py:879-887), but the browser handler at base_station/web/app.js:188-229 dispatches only on specific topic suffixes (`/engine`, `/battery`, `/hydraulics`, `/gps`, `/active_camera`, `/crop_health`, `/status/link`, `/source_active`, `/control/safe_mode_active`). Neither injected topic matches any of them, and diagnostics.js has no generic topic renderer. The browser-side cost is bandwidth plus a `JSON.parse` of the attacker blob per message, not rendered attacker text or DOM injection.

Everything else reproduces exactly: no `Depends(_require_session)` at web_ui.py:1174 and :1193, no body cap (contrast the IP-203 cap at web_ui.py:2090-2093 with MAX_PARAMS_BODY_BYTES at :56), no schema validation, qos=1 verbatim republish, and audit rotation at 10 MB x 5 in base_station/audit_log.py:29-30.

### Significant findings NOT adversarially verified (verify-cap overflow)
- (high) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1226` — No single-writer arbitration on /ws/control: up to 4 authenticated sockets all publish interleaved frames plus per-socket deadman halts to lifetrac/v25/cmd/control
  - detail: Every index.html tab auto-connects /ws/control on load (app.js:178) and streams the tab-local state (all zeros when idle) at 20 Hz (app.js:181-185). The server admits up to MAX_CONTROL_SUBSCRIBERS (default 4, line 686) and each socket independently packs frames with its own seq/hb counters and publishes to the same topic; additionally each idle/throttled socket's 150 ms timeout publishes a zero halt frame per timeout tick (~6.7 Hz). Browsers throttle background-tab timers to ~1 Hz, so a forgotten second tab becomes a constant halt-frame generator interleaved with the driver's real frames. Independent seq streams from one logical source may also confuse tractor-side replay windows.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:2109` — POST /api/params publishes to lifetrac/v25/params/set which has no base-side consumer post LoRa-only migration - the image encode/tile QUALITY knobs and the whole params editor are dead ends
  - detail: The only subscriber to params/set is firmware/tractor_x8/params_service.py:184, which runs against the tractor broker; since commit 3b1fb347 there is no LAN MQTT to the tractor, and neither image_rx_daemon.py nor lora_bridge.py subscribes to params/set to convert it into a 0xFB frame. Symmetrically, the retained params/changed snapshot never reaches the base broker, so GET /api/params (line 2069) returns {} forever. Additionally, allow-listed values get no type/range validation (ParamsBody line 127 accepts any scalar), so even once bridged, 'image.encode_quality': -5 or a string would be forwarded.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1993` — POST /api/encode_mode/cycle publishes a RETAINED encode_mode_override without persisting it, so broker pin and disk store diverge and restarts flip the active mode unpredictably
  - detail: _set_encode_mode_override(nxt, persist=False) still executes the retained publish at line 1964. After a pill/gamepad cycle, the broker retains e.g. {"mode":"mono_g4"} while .encode_mode_override on disk still says "full". If lora_bridge/image daemons restart, they adopt the retained mono_g4; if web_ui restarts first, its startup republish (line 534) stomps the retained value back to full. Which encoder is live after any restart depends on process restart order. GET /api/settings/encode_mode (line 1889) additionally reports the disk value, so the settings page shows a stale 'Current pin' after cycling.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1965` — Encode-mode POST responses report 'delivered' from paho's local qos-0 enqueue rc, and the settings page treats it as a confirmation; the real tractor ack never flows back on the settings path
  - detail: info.rc == 0 at qos 0 means 'queued in the client', not broker-accepted or tractor-applied. The genuine confirmation loop (retained lifetrac/v25/status/encode_mode cached at line 819-827, plus rx_codec from link_stats) is exposed only via GET /api/encode_mode/current, which only index.html polls (app.js:553). settings.html:284 prints 'pinned (bridge will apply within ~1 s)' purely off the enqueue flag. Same rc semantics apply to _publish_radio_profile's 'delivered' (line 313), though the radio GET at line 1999 does surface acks.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:2123` — /api/autonomy/calculate-sweep accepts an unbounded polygon and runs the CPU-bound planner on the event loop, with raw exception text returned to the client
  - detail: GeofencePlanRequest (line 2115) bounds swath_width_m but puts no length cap on polygon and no range check on lat/lon. planner.generate_sweep_coverage executes synchronously in the async handler; a large or degenerate polygon blocks all WS control/telemetry traffic for its full runtime. The except Exception -> HTTPException(400, str(exc)) leaks internal error details.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1387` — Bench-only radio control is enabled by default in production (LIFETRAC_ENABLE_BENCH_RADIO defaults to '1')
  - detail: The guard is opt-out, not opt-in: any deployment that forgets to set the env var exposes a PIN-gated route that runs local PowerShell/python helper scripts which reconfigure the base radio (sleep/wake RXCONT) out from under image_rx_daemon, on top of the 180 s event-loop block from finding 1.

### Low-severity notes
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1088` — Blocking disk I/O in async handlers (/api/audit chunked tail, page read_text, config bundle build, temp-file validation) briefly stalls the event loop — api_audit reads up to the whole audit file backwards in 64 KiB chunks on the loop thread; config preview/upload write temp files and mutate os.environ['LIFETRAC_BUILD_CONFIG_PATH'] (line 1608) relying on there being no await between set and restore. Individually small, but they add jitter to the 150 ms control deadman.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1124` — Orphan surfaces: /ws/image, POST /api/logout (line 1790), GET /api/session (line 1800) have no frontend callers — canvas_renderer.js consumes /ws/state, not /ws/image, so the binary canvas WS (and its video/canvas MQTT forwarding work on every frame) serves nobody; there is no logout button and no session probe in any web/ file. Conversely, every endpoint the frontend calls exists - no 404-target calls were found.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:1160` — /ws/state sockets are written from two contexts (handler 0.5 s loop + MQTT-thread run_coroutine_threadsafe snapshots), and starlette WS sends are not safe to interleave — _publish_state_snapshot (line 929) schedules ws.send_text onto the loop for every canvas update while ws_state's own while-loop is awaiting send_text on the same socket every 500 ms; two send coroutines on one websocket can interleave at internal await points and raise or corrupt framing under load.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:2063` — api_radio_profile_set reads the module global _radio_profile_active outside _radio_profile_lock when building its response — The value was just written under the lock inside _publish_radio_profile, but the unlocked read can race the auto worker's concurrent publish and report a different active_profile than what was actually commanded by this request.

---
## Layer review: base-daemon

Mapped base_station/image_rx_daemon.py (all 1124 lines) against lora_proto.py, frame_format.py, web_ui.py, and the tractor peer. SUBSCRIBES (on primary + optional LIFETRAC_CTRL_MQTT_HOST split-broker client, callbacks at 667/682/703, subscribed at 1020-1022): (1) control/radio_profile {\"profile\":0|1|2, validated} -> 0xFB 0x65 <profile_u8>, dedicated two-phase machinery NOT in _pending_cmds: send copies=2 + 1.5 s Phase-A resends for 12 s, cleared by matching 0x66 RADIO_PROFILE_ACK; then local apply, 45 s revert watchdog, proof-of-life -> one-shot 0x67 CONF (no ack coverage) + retained status/radio_profile/rx ack; late-0x66 split-heal follows the tractor. (2) control/encode_mode_override {\"mode\":name|int} -> 0xFB 0x63 <mode_u8>; strings validated via EncodeMode, ints UNVALIDATED (&0xFF); _set_pending(10 s/20 attempts), cleared by ANY 0x68 ENCODE_MODE_ACK (no mode match). (3) cmd/req_keyframe (payload ignored) -> 0xFB 0x60, gated by LIFETRAC_KF_REQUEST_DISABLE at the subscription choke point plus a 10 s min gap; _set_pending cleared by the implicit ack of a completed frame_kind==1 keyframe. PUBLISHES for the UI: video/tile_delta (reassembled frames, qos0), video/link_stats (2 s JSON incl. rx_codec implicit encode ack + base radio_profile, mirrored to ctrl broker), retained status/radio_profile/tx (synthesized from 0x66), retained status/radio_profile/rx, retained status/encode_mode (raw 0x68 JSON), plus a self-heal loopback publish to cmd/req_keyframe. Wire framing matches the air-verified contract (FB <op> <args>; the 0xFD/0xFE first byte in TX_FRAME_REQ is a local tx_id echoed in TX_DONE, not on-air). KEY DEFECTS: (H1) the RS-1.x window-aligned pump at line 898 is gated on the never-fed _ctrl_out queue — it never fires, so pending 0x60/0x63 retries only leave via idle-pass drain and starve to 'GAVE UP' under a saturated stream; (H2) _set_pending drops the new body when an opcode is already pending, so a rapid encode-mode change is silently lost and the stale mode's unmatched 0x68 ack ends retries; (M) numeric encode modes unvalidated end-to-end, 0x67 CONF loss splits the link with no base-side recovery, retained pins re-command on every restart (the recorded incident class, msg.retain never checked); plus RS-9: no drive/estop/camera/params transport exists in this daemon despite the LoRa-only topology — confirmed open in TODO.md, contradicting the assumption that commit 3b1fb347 implemented drive-over-0xFB. The referenced Method G doc contains no 0xFB contract; lora_proto.py is the de-facto contract and its 'shared back-channel ids' comment is wrong (legacy 0x60=PERSON_APPEARED, legacy REQ_KEYFRAME=0x62).

### Controls / surface inventory
- **SUB lifetrac/v25/control/radio_profile -> 0xFB 0x65 RADIO_PROFILE** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:667
  - action: MQTT callback _on_radio_profile_msg: JSON-parses {"profile": N}, validates N in (0,1,2), stores _pending_profile under lock. RX worker's _maybe_switch_profile (line 367) sends wire bytes FB 65 <profile_u8> (host framing: TX_FRAME_REQ 0x10 payload [0xFD tx_id, len, FB 65 pp]), copies=2 back-to-back, then Phase-A re-sends 1 copy-pair every 1.5 s until PROFILE_ACK_TIMEOUT_S=12 s.
  - backend: 0xFB opcode 0x65, payload = 1 byte profile id (0/1/2). Cleared by inbound 0x66 RADIO_PROFILE_ACK whose args[0] matches the awaited profile (line 561). NOT in _pending_cmds — uses its own dedicated Phase-A/Phase-B state machine (_await_ack_profile/_confirm_pending/_revert_deadline).
  - note: Subscribed qos=1 on both the primary and optional split-broker ctrl client (lines 1020, 1046). Retained pin re-triggers a full two-phase switch on every daemon restart (comment at 1019: intentional) — the exact stale-retained-pin incident class. On ack: applies locally (_apply_profile), arms 45 s revert; first non-command RX frame = proof-of-life -> sends 0x67 CONF + retained rx ack ok=true. Timeout -> retained rx ack ok=false 'no tractor ack'. Late 0x66 after window expiry -> base follows tractor (split-heal, line 577).
- **SUB lifetrac/v25/control/encode_mode_override -> 0xFB 0x63 ENCODE_MODE** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:682
  - action: MQTT callback _on_encode_mode_msg: JSON {"mode": X}. X in (None, "", "auto") -> silently dropped (auto is base-side policy, never transmitted). String -> EncodeMode[X.upper()].value (validated, KeyError caught). Int -> int(X) with NO range check. Registers via _set_pending(0x63, FB 63 <mode & 0xFF>).
  - backend: 0xFB opcode 0x63, payload = 1 byte mode id (contract: EncodeMode 0..7). Retry via _pending_cmds: timeout_s=10, max_attempts=20, 1 copy per attempt. Cleared by ANY inbound 0x68 ENCODE_MODE_ACK (line 598) — the acked mode is never compared to the commanded mode.
  - note: Two defects: (1) numeric payloads bypass validation — {"mode": 200} goes on air as 0xC8 and the tractor (image_tx_daemon.py:600-608) forwards any byte retained to camera_service; (2) _set_pending line 456-459 refreshes only the deadline when the opcode is already pending, so a mode change while the previous command is unacked is silently discarded and the STALE mode keeps retrying.
- **SUB lifetrac/v25/cmd/req_keyframe -> 0xFB 0x60 REQ_KEYFRAME** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:703
  - action: MQTT callback _on_req_keyframe_msg: payload ignored entirely (web_ui sends a reason string qos=1, the daemon's own KeyframeRequester sends b"\x01" qos=0). Gate 1: LIFETRAC_KF_REQUEST_DISABLE=1 drops at the subscription choke point (line 711; also independently gated inside KeyframeRequester.poke line 175). Gate 2: KEYFRAME_CMD_MIN_GAP_S=10 s rate limit. Then _set_pending(0x60, FB 60).
  - backend: 0xFB opcode 0x60, no args (2-byte frame FB 60). Retry via _pending_cmds (10 s / 20 attempts). Cleared by an IMPLICIT ack: a completed reassembled frame with frame_kind==1 (keyframe) at line 886-888 calls _clear_pending(0x60, "keyframe received").
  - note: The subscription-level env gate correctly chokes ALL request sources (web_ui button, web_ui I-frame recovery, self-heal poke). Retry delivery itself is impaired by the dead pump gate (see findings).
- **Window-aligned command pump (RS-1.x)** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:898
  - action: After a pass in which a frame COMPLETED (tractor's ~44 ms turnaround window open), send one command body from _next_ctrl_body with >=120 ms spacing between pumps.
  - backend: Should drain _pending_cmds (0x60/0x63) during an active stream. Gated on `frame_done and not self._ctrl_out.empty()`.
  - note: NOTHING ever enqueues into _ctrl_out (no .put() call exists in the file — only the constructor at 235, two get_nowait at 488/506, and this empty() check). The gate is therefore always false and the aligned pump NEVER fires. Pending commands are drained only by _drain_ctrl_idle (line 763), which runs only on empty read_frames passes — precisely the unaligned timing the RS-1.x work was built to avoid.
- **Idle-link command drain** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:492
  - action: On every empty ~0.25 s poll pass: sends one _pending_cmds retry (copies=1) via _next_ctrl_body, then flushes any legacy _ctrl_out entries (queue is永 empty in practice).
  - backend: 0xFB frames from _pending_cmds, oldest-t0 first; give-up caps enforced in _next_ctrl_body (line 473: attempts>=max_attempts or past deadline -> 'GAVE UP' log + drop).
  - note: At the ~0.25 s idle cadence, max_attempts=20 exhausts in ~5 s — half the nominal 10 s deadline. Oldest pending opcode monopolizes retries until cleared or capped (one body per pass, sorted by t0).
- **Inbound 0xFB 0x66 RADIO_PROFILE_ACK handler** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:544
  - action: Publishes retained JSON {"ok":true,"profile":args[0],"via":"lora","ts"} to lifetrac/v25/status/radio_profile/tx on BOTH clients, then drives the state machine: matching awaited profile -> local _apply_profile + arm 45 s revert; no await active but args[0] valid and != active -> late-ack split-heal (follow the tractor).
  - backend: MQTT publish lifetrac/v25/status/radio_profile/tx (retained, qos 0) — consumed by web_ui _radio_acks (web_ui.py:828).
  - note: The retained tx-ack publish does NOT validate args[0] against _PROFILE_CHOICES and hard-codes ok=true — a garbled ack byte publishes nonsense retained state to the UI.
- **Inbound 0xFB 0x68 ENCODE_MODE_ACK handler** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:596
  - action: Clears _pending_cmds[0x63] (any 0x68, no mode comparison), then republishes the raw args bytes (camera_service JSON, <=200 B, unvalidated) retained to lifetrac/v25/status/encode_mode on both clients.
  - backend: MQTT publish lifetrac/v25/status/encode_mode (retained, qos 0) — consumed by web_ui _encode_mode_ack (web_ui.py:819-824).
  - note: web_ui json-parses defensively so unvalidated bytes are tolerated; the no-mode-comparison clearing is folded into the _set_pending stale-body finding.
- **Outbound 0xFB 0x67 RADIO_PROFILE_CONF (proof-of-life)** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:611
  - action: _note_proof_of_life: first valid non-command RX frame after a local profile switch -> sends FB 67 <active_profile>, copies=2, disarms the base revert watchdog, publishes retained rx ack ok=true.
  - backend: 0xFB opcode 0x67, payload = 1 byte profile id. NO retry machinery, NO ack — pure fire-and-forget.
  - note: If both back-to-back copies miss the tractor's inter-fragment listening gap, the tractor's CONF watchdog reverts to the old profile while the base (revert already disarmed) stays on the new one — a link split with no automatic base-side recovery (only a fresh 0x66 late-ack could heal it, and the reverted tractor won't send one).
- **PUB lifetrac/v25/video/tile_delta (reassembled frames)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:346
  - action: Per completed reassembly (batched payloads publish each frame in the list, line 866-889): encode_tile_delta_frame(frame) bytes, qos 0, retain=False, primary client only.
  - backend: Consumed by web_ui.py (subscribes lifetrac/v25/video/#) — same slot as legacy bridge topic id 0x25.
  - note: Publish errors counted, never fatal. NOT mirrored to the split-broker ctrl client (by design: video stays on the local broker).
- **PUB lifetrac/v25/video/link_stats (2 s rolling sample)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:952
  - action: Every 2 s: JSON {bps, frames_per_s, rx_frames_seen, frames_published, rssi_dbm, snr_db, parity_reconstructions, timeouts, decode_errors, rx_codec, rx_codec_name, radio_profile, ts}, qos 0, not retained; mirrored to the ctrl client for the split-broker bench.
  - backend: Consumed by web_ui.py:806 (image panel + AutoRadioPolicy input); rx_codec doubles as the encoder-selector's implicit ack.
  - note: radio_profile field reports the BASE-side active profile — during a split it will disagree with reality on the tractor.
- **PUB lifetrac/v25/status/radio_profile/rx (base-side ack)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:652
  - action: _ack_profile publishes retained JSON {ok, profile, ts[, error]} on both clients at every terminal state of the two-phase switch: success (proof-of-life), 'no tractor ack', 'reverted: link silent', 'local apply failed'.
  - backend: Consumed by web_ui.py:828 (_radio_acks['rx']).
  - note: Retained so the UI's cache survives web_ui restarts.
- **PUB lifetrac/v25/cmd/req_keyframe (self-heal loopback)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:159
  - action: KeyframeRequester.poke on reassembler decode-error/timeout deltas: publishes b"\x01" qos 0 (min 5 s interval, LIFETRAC_KF_REQUEST_DISABLE gate) to the SAME topic the daemon subscribes — loops back through the broker into _on_req_keyframe_msg and becomes a 0x60 command.
  - backend: MQTT loopback -> 0xFB 0x60 (subject to the 10 s subscription-side gap).
  - note: Publishes on the primary client only; in the split-broker topology the loopback still works because the daemon subscribes on the primary client too.
- **UI topics with NO LoRa transport in the strict-path topology** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/base_station/web_ui.py:894
  - action: web_ui subscribes telemetry/# and params/changed and publishes cmd/control (drive), cmd/estop, cmd/camera_select, params/set — none of which this daemon subscribes to or has 0xFB opcodes for (_CMD_OPS = 0x60/0x63/0x65/0x66/0x67/0x68 only).
  - backend: NONE
  - note: Historically these rode lora_bridge.py (AEAD path, separate radio). TODO.md RS-9 (line 1046-1059) explicitly documents that image_rx_daemon carries 'only 0xFB video opcodes' and hydraulic control is absent from whichever process owns the UART — so the CONTEXT claim that commit 3b1fb347 moved drive commands onto 0xFB is NOT implemented in this tree; it is tracked open work. In a deployment where the image daemons own both L072s, drive/estop/camera-select/params UI paths are dead.

### Verified findings
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:898` — Window-aligned command pump is dead code: gated on the never-fed _ctrl_out queue instead of _pending_cmds
  - detail: The RS-1.x pump gate reads `if frame_done and not self._ctrl_out.empty():`, but no code path in the file ever calls _ctrl_out.put() (only the constructor at line 235 and two get_nowait() drains at 488/506 reference it). The gate is permanently false, so _next_ctrl_body is never invoked from the aligned pump. Pending ENCODE_MODE (0x63) and REQ_KEYFRAME (0x60) retries therefore go out only via _drain_ctrl_idle (line 763), which runs exclusively on EMPTY read_frames passes — unaligned timing, exactly what Run D measured at ~2/12 delivery. The fix is to gate on `self._pending_cmds or not self._ctrl_out.empty()`.
  - failure: Operator changes encode mode while the tractor streams at saturation (few/no empty 0.25 s polls): the 0x63 command is never transmitted during the stream, the 10 s deadline expires, the daemon logs 'GAVE UP', and the tractor keeps the old codec even though status topics and the UI show the command was issued.
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:458` — _set_pending discards the new command body when the opcode is already pending; combined with unmatched 0x68 clearing, a rapid encode-mode change is silently lost
  - detail: When _pending_cmds already holds the opcode, _set_pending only extends the deadline and returns (lines 456-459) — the freshly packed body (the NEW mode byte) is thrown away and the machinery keeps retrying the OLD mode. Additionally, _handle_command clears the 0x63 pending on ANY inbound 0x68 ENCODE_MODE_ACK (line 598) without comparing the acked mode against the commanded one, so the stale command's ack terminates retries for the newer intent.
  - failure: Operator selects MONO_G4 then corrects to BTC4_PER_TILE within the ack window: the second override callback extends the MONO_G4 entry's deadline, MONO_G4 keeps retrying, the tractor acks MONO_G4, the pending clears — BTC4_PER_TILE is never transmitted while web_ui's retained encode_mode_override topic claims it is active.
  - verifier correction: The discard site is the deadline-extend-and-return at lines 457-459 of LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py (claim cited 456-459/458 — same site). All other details are accurate as stated.
- **[CONFIRMED] (medium)** `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:693` — Numeric encode_mode_override payloads are accepted without range validation and sent on air masked &0xFF
  - detail: String modes are validated via EncodeMode[requested.upper()] (KeyError caught), but an integer payload takes `mode = int(requested)` with no membership check against EncodeMode (contract range 0..7), and line 701 masks it to a byte. The tractor side (firmware/tractor_x8/image_tx_daemon.py:600-608) is equally permissive: any args[0] byte is forwarded retained to camera_service as {"mode": N}. The invalid value also gets retried up to 20 times by the pending machinery.
  - failure: Any local MQTT client publishes {"mode": 200} to lifetrac/v25/control/encode_mode_override: the base airs FB 63 C8 repeatedly, the tractor publishes retained {"mode": 200} to camera_service, and the encoder's behavior on the undefined id (reject, crash, or clamp) decides whether video survives — a plaintext-command DoS the wire contract's SECURITY NOTE only anticipates for valid codec flips.
  - verifier correction: Confirmed as stated at the code level: base_station/image_rx_daemon.py:693 accepts any integer encode_mode_override with no range check against EncodeMode (0..7) and airs it masked &0xFF (line 701), retried by _set_pending up to 20 attempts / 10 s until ENCODE_MODE_ACK; firmware/tractor_x8/image_tx_daemon.py:600-608 forwards any byte retained to camera_service — unlike the adjacent radio-profile branch (line 610), which validates args[0] in _PROFILE_CHOICES. Impact correction: the encoder outcome is not undefined — camera_service._clamp_encode_mode (camera_service.py:447-453, applied at 1036) clamps unknown ids to Y_ONLY and acks requested-vs-effective, so video survives degraded rather than dying; the concrete harm is a forced quality downgrade plus a retained out-of-contract {"mode": 200} on the tractor's camera_service topic, an outcome broadly within the SECURITY NOTE's stated "video degradation/DoS" threat model but reachable via an unvalidated value the base should have rejected the same way it rejects unknown string modes and unknown radio profiles.

### Significant findings NOT adversarially verified (verify-cap overflow)
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:619` — RADIO_PROFILE_CONF (0x67) is fire-and-forget with no retry/ack coverage; a lost CONF splits the link with no base-side recovery
  - detail: _note_proof_of_life sends the CONF exactly once (copies=2 back-to-back via _send_command_frame) and simultaneously disarms the base's revert watchdog (_confirm_pending=False, _revert_to=None) and publishes the retained ok=true rx ack. 0x67 is not registered in _pending_cmds and no tractor response confirms receipt. The tractor's own CONF watchdog (image_tx_daemon.py:665-669) reverts to the previous profile if no CONF arrives.
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:1020` — Retained control-topic pins re-command the tractor on every daemon restart with no msg.retain discrimination
  - detail: The daemon subscribes control/radio_profile and control/encode_mode_override at qos=1 and treats retained deliveries identically to live operator actions (the comment at 1019 declares this intentional). _on_radio_profile_msg and _on_encode_mode_msg never inspect msg.retain, so every restart replays the last pin: a full two-phase profile switch volley and an ENCODE_MODE retry train. A stale retained {"profile": N} on this exact topic already caused a live incident (base re-commanded an old profile mid-run).
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:121` — No transport exists for web_ui's drive/estop/camera-select/params topics in the LoRa-only topology; the daemon bridges only three control topics
  - detail: This daemon — the sole owner of the base L072 in the strict path — subscribes only control/radio_profile, control/encode_mode_override, and cmd/req_keyframe, and _CMD_OPS caps the 0xFB namespace at the six video opcodes. web_ui also publishes cmd/control (hydraulic drive), cmd/estop, cmd/camera_select, and params/set, and subscribes telemetry/# and params/changed; those ride lora_bridge.py's AEAD path, which cannot share /dev/ttymxc3 (single-owner rule, header line 40-42). TODO.md RS-9 (lines 1046-1059) documents this exact gap, so it contradicts the working assumption that commit 3b1fb347 already moved drive commands onto 0xFB — that migration is tracked open work, not implemented.

### Low-severity notes
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:401` — RADIO_PROFILE (0x65) bypasses the aligned/spaced pump: direct sends with copies=2 back-to-back reproduce the Run-H same-window collapse — The initial Phase-A send (line 425-426) and the 1.5 s re-sends (line 399-403) call _send_command_frame directly with the default copies=2, transmitting both copies ~37 ms apart — the exact pattern Run H forensics showed collapses two shots into one stale window. The RS-1.x pump (>=120 ms spacing, frame-completion alignment) covers only _pending_cmds opcodes; the profile state machine predates it and was never migrated. It does have its own 12 s ack window and 1.5 s retry cadence, so delivery eventually succeeds, just at the unaligned ~1/12-per-volley rate.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:552` — Inbound 0x66 publishes a retained tx-ack without validating the profile byte and with hard-coded ok=true — The RADIO_PROFILE_ACK handler publishes {"ok": true, "profile": int(args[0]), "via": "lora"} retained to lifetrac/v25/status/radio_profile/tx for ANY 0x66 with len(args)>=1, before the _PROFILE_CHOICES check used by the state-machine branches below it. The 0xFB link is plaintext with only the PHY CRC guarding it.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/image_rx_daemon.py:451` — Idle-link retry cadence exhausts max_attempts=20 in ~5 s, halving the nominal 10 s pending-command deadline — _drain_ctrl_idle retries one pending command per empty poll pass and the RX loop's read timeout is 0.25 s, so attempts burn at ~4/s: 20 attempts ≈ 5 s, and _next_ctrl_body's attempts-cap check (line 480) drops the command well before its 10 s deadline. Secondary quirk: _next_ctrl_body serves the oldest-t0 entry exclusively, so a pending 0x60 starves a co-pending 0x63 until it clears or caps.
- `LifeTrac-v25/DESIGN-CONTROLLER/base_station/lora_proto.py:956` — 0xFB opcode-sharing comment is wrong: strict-path 0x60 collides with legacy CMD_PERSON_APPEARED, not legacy CMD_REQ_KEYFRAME (0x62) — The block comment (line 947) claims the 0xFB opcodes 'share the LORA_PROTOCOL.md back-channel ids 0x60/0x63/0x64', but in the legacy FT_COMMAND namespace CMD_REQ_KEYFRAME=0x62 and 0x60=CMD_PERSON_APPEARED (lines 38-40); CMD_OP_REQ_KEYFRAME=0x60 matches neither claim. The namespaces are disjoint on air (0xFB magic is checked before dispatch) so there is no wire ambiguity, and the daemon's implemented opcodes exactly match the air-verified contract (0x60/0x63/0x65/0x66/0x67/0x68). Note also the referenced wire-contract doc 'AI NOTES/2026-07-25_Method_G_Firmware_Analysis_Claude_v1_0.md' contains no 0xFB/opcode content at all — lora_proto.py is the de-facto contract.

---
## Layer review: tractor-side

Tractor 0xFB dispatch map (all inside image_tx_daemon's TX-worker thread): frames are parsed by parse_command_frame in _dispatch_command (image_tx_daemon.py:590-626), fed from four RX windows — idle poll (480-482), inter-train reverse-slot gap (464-469), post-fragment/post-burst drains (855-857, 1030-1032), and the RS-1.x mid-burst dispatch inside the v3 pipelined loop (1008-1019). Opcodes: 0x60 REQ_KEYFRAME -> local MQTT lifetrac/v25/tractor/req_keyframe -> camera_service force_key_evt (no air ack; keyframe arrival is the base's observable). 0x63 ENCODE_MODE -> retained local MQTT {\"mode\":N} on lifetrac/v25/tractor/encode_mode_override -> camera_service._apply_encode_mode sets the module-global ENCODE_MODE (clamped to implemented set {0,1,2,3,6,8}), forces a keyframe, THEN publishes the retained status ack, which image_tx_daemon forwards over the air as 0x68 — so the ack does follow the change taking effect, and the mode persists across frames and works identically for SyntheticCamera and real backends (mode logic is downstream of grab_rgb; nothing backend-specific overrides it). 0x65 RADIO_PROFILE -> two-phase: 0x66 ack x2 on the old grid, 0.7 s flush, retune via _apply_profile, 45 s revert watchdog awaiting 0x67 CONF. No hydraulic/drive opcodes exist anywhere in the contract or dispatcher. Key defects: (1) HIGH — the mid-burst RX dispatch fix lives only in the v3 pipelined loop, but TX_PIPELINE defaults to v2, whose wait_for_tx_done still silently discards RX_FRAME URCs during every fragment's time-on-air (the exact 1/12-delivery incident signature); (2) stale RETAINED encode acks are re-radiated as 0x68 on daemon reconnect and the base clears its ENCODE_MODE retry on ANY 0x68 without content checks — same stale-retained failure class as the radio_profile incident; (3) quality/byte-budget control (CMD_CAMERA_QUALITY 0x04, CMD_LINK_PROFILE 0x64) exists only in the KISS back-channel dispatcher, which is disabled under USE_LORA_BRIDGE — so over LoRa the base can change mode (with its fixed per-mode quality caps) but can never adjust WEBP_QUALITY or the frame byte budget at runtime; (4) a paho-thread/encode-loop race can ship one frame whose codec byte disagrees with its tiles. Enum values 0-7 match exactly base-to-tractor; btc4 modes 4/5 are UI-offered but clamped to Y_ONLY (surfaced via clamped:true), and RAWSTREAM=8 is tractor-only, unreachable through the base's name path or UI.

### Controls / surface inventory
- **0x60 REQ_KEYFRAME RX handler** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:596
  - action: _dispatch_command: on CMD_OP_REQ_KEYFRAME publishes b'\x01' (qos=0, not retained) to local MQTT
  - backend: MQTT lifetrac/v25/tractor/req_keyframe -> camera_service._on_msg (camera_service.py:1250) sets force_key_evt; next _build_frame is an I-frame
  - note: No explicit air ack by design: the base's RS-1.5 observable for this opcode is keyframe arrival. Works for synthetic and real cameras identically.
- **0x63 ENCODE_MODE RX handler** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:600-608
  - action: _dispatch_command: reads args[0] as mode int, publishes JSON {"mode": N} RETAINED to local MQTT (intra-board hop)
  - backend: MQTT lifetrac/v25/tractor/encode_mode_override -> camera_service._on_msg (camera_service.py:1253-1270) -> _apply_encode_mode (camera_service.py:1026)
  - note: Mode reaches camera_service via LOCAL MQTT publish (not file/env/direct call). Retained flag is deliberate: a restarted camera_service re-applies the last mode on subscribe (camera_service.py:1276-1278). Base sends exactly 1 arg byte (image_rx_daemon.py:701); tractor reads args[0] only — extra bytes ignored.
- **camera_service _apply_encode_mode (mode application + local ack)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/camera_service.py:1026-1069
  - action: Clamps via _clamp_encode_mode (implemented set {0,1,2,3,6,8}, camera_service.py:437-444), assigns module-global ENCODE_MODE (line 1037-1038), sets force_key_evt, THEN publishes retained JSON ack {requested, effective, effective_name, clamped, codec, source, ts}
  - backend: MQTT lifetrac/v25/status/encode_mode (retained) -> image_tx_daemon._on_enc_ack (image_tx_daemon.py:1081-1082)
  - note: Ordering is correct at this level: ENCODE_MODE is mutated BEFORE the ack publish, so the 0x68 only radiates after the change is in effect for the next _encode_tile call. Mode persists across frames (module global; no per-frame reset). Encoder output really changes: _encode_tile reads the global at line 572 (grayscale convert for modes 1-3, MONO_G4 branch at 573, RAWSTREAM strip at 588), per-mode quality caps applied at 583 via _apply_encode_mode_quality (MOTION_ONLY<=30, WIREFRAME<=20), and the wire codec byte follows via _codec_for_mode at line 977. Works identically for SyntheticCamera, LibcameraCamera, and V4l2FfmpegCamera — mode logic is entirely downstream of grab_rgb(); nothing backend-specific hardcodes a mode.
- **0x68 ENCODE_MODE_ACK air forwarder** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:645-649 (send) + 1081-1083,1090 (capture/subscribe)
  - action: _service_control_plane forwards the latest camera_service status JSON (truncated to 200 B) as pack_command_frame(CMD_OP_ENCODE_MODE_ACK), 1 copy, fire-and-forget
  - backend: 0xFB opcode 0x68 over the air -> base image_rx_daemon.py:596-608 clears the ENCODE_MODE retry and republishes retained on base lifetrac/v25/status/encode_mode
  - note: Because TRACTOR_ENC_STATUS_TOPIC is RETAINED and image_tx_daemon subscribes to it at every (re)connect, a daemon restart radiates a STALE 0x68 carrying the previous mode's ack; the base clears any pending 0x63 retry on ANY 0x68 without checking content (see findings). Also an unlocked read-then-clear on _enc_ack_out can drop an ack that lands in the window (latest-wins by design; base retry self-heals).
- **0x65 RADIO_PROFILE RX handler (two-phase switch)** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:609-622
  - action: _dispatch_command: validates args[0] in {0,1,2}, queues 0x66 RADIO_PROFILE_ACK into _cmd_out, sets _pending_profile
  - backend: 0x66 ack TXed x2 copies on the OLD grid by _service_control_plane (651-664, then 0.7 s flush dwell); switch applied by _maybe_switch_profile (680-707) -> _apply_profile (709-727) reconfigures the L072 (LIFETRAC_REG_PROFILE env, configure_regulatory_profile_if_needed, PHY contract verify, AirtimeBudget swap); 45 s revert watchdog armed awaiting 0x67
  - note: Ack deliberately PRECEDES the change (two-phase contract, ack on old grid). Failed apply auto-reverts to the previous profile; local retained log ack on lifetrac/v25/status/radio_profile/tx. Fragmentation happens at TX time (not prepare time), so a mid-queue profile switch re-fragments correctly (_prep_next_train docstring, line 488-492).
- **0x67 RADIO_PROFILE_CONF RX handler** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:623-626
  - action: Disarms the CONF-revert watchdog (_confirm_deadline/_revert_profile = None)
  - backend: NONE (terminal; no response frame)
  - note: Without CONF within 45 s, _service_control_plane (668-678) reverts to the previous profile and publishes a local not-ok ack.
- **Mid-burst RX dispatch inside the pipelined (v3) TX loop** [`partial`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:1008-1019
  - action: HOST_TYPE_RX_FRAME_URC events seen while polling for TX_DONEs are parse_rx_frame'd and fed to _dispatch_command (RS-1.x fix for the measured 1/12 mid-burst delivery loss)
  - backend: _dispatch_command -> per-opcode routes above
  - note: Only reachable when LIFETRAC_TX_PIPELINE=v3; the default is v2 (line 158), whose per-fragment wait_for_tx_done DISCARDS RX_FRAME URCs (see findings). Post-burst drain at 1030-1032 also dispatches.
- **Serial (v2) path RX windows** [`bug`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:855-857 (post-fragment), 480-482 (idle), 464-469 (inter-train gap)
  - action: _ensure_rxcont re-arms RXCONT (raw opmode write, invisible to firmware TX path which parks STANDBY after every TX), then _drain_rx_frames -> _dispatch_command
  - backend: _dispatch_command -> per-opcode routes above
  - note: The post-fragment drain uses timeout 0.0 and only runs on TX success; frames arriving DURING a fragment's time-on-air are consumed and dropped inside wait_for_tx_done (method_h_stage2_tx_probe_v2.py:2342-2343 'unrelated frame' branch). See high-severity finding.
- **KISS back-channel dispatcher (0x62 keyframe / 0x03 cam-select / 0x04 quality / 0x63 mode / 0x61 ROI / 0x64 link-profile)** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/camera_service.py:1072-1124
  - action: dispatch_back_channel mutates WEBP_QUALITY global (1094-1099), calls _apply_encode_mode (1100-1102), updates RoiPlanner/LinkBudget (1103-1119); forces a keyframe on any reconfig (1120-1124)
  - backend: M7 UART /dev/ttymxc1 KISS reader thread (1192-1229)
  - note: The reader thread is started ONLY when not USE_LORA_BRIDGE (1231-1235). In the LoRa-only deployment this whole dispatcher — including the ONLY runtime quality (0x04) and link-budget (0x64) mutators — is unreachable. No 0xFB opcode carries quality or link-profile (lora_proto _CMD_OPS = {0x60,0x63,0x65,0x66,0x67,0x68}).
- **camera_service MQTT control handler _on_msg** [`ok`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/camera_service.py:1246-1270
  - action: Topic dispatch: keyframe topics (legacy cmd/req_keyframe + tractor/req_keyframe) -> force_key_evt; tractor/encode_mode_override -> parses {"mode": int|name}, name path uses ENCODE_MODE_NAMES.index -> _apply_encode_mode('lora_cmd')
  - backend: subscriptions at 1274-1278 (retained override topic qos=1); boot ack _apply_encode_mode(ENCODE_MODE,'boot') at 1284
  - note: Only active when DEBUG_MQTT (forced true under USE_LORA_BRIDGE). Boot ack means a fresh camera_service radiates a proof-of-life 0x68 with source='boot'.
- **Hydraulic/drive opcodes** [`dead`] — LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:590-626
  - action: NOT PRESENT: no hydraulic/drive opcode exists in lora_proto's 0xFB command set (base_station/lora_proto.py:955-965) or the tractor dispatcher
  - backend: NONE
  - note: parse_command_frame drops unknown opcodes silently (returns None, lora_proto.py:987-988). If commit 3b1fb347's 'hydraulic/drive over 0xFB' contract is intended to land in these files, the tractor side has no handler yet — any such frame is silently ignored. Neither reviewed file references hydraulics.

### Verified findings
- **[CONFIRMED] (high)** `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:825` — Mid-burst 0xFB command dispatch fix only exists in the v3 pipelined loop; the DEFAULT v2 serial path still discards inbound command frames during every fragment's time-on-air
  - detail: TX_PIPELINE defaults to 'v2' (line 158). The v2 per-fragment path calls wait_for_tx_done (line 825-826), and that helper (firmware/x8_lora_bootloader_helper/method_h_stage2_tx_probe_v2.py:2342-2343) consumes HOST_TYPE_RX_FRAME_URC frames off the wire and drops them into the 'INFO: unrelated frame' branch — they are never re-queued. The RS-1.x fix that dispatches mid-burst RX (lines 1008-1019) lives only in _tx_fragments_pipelined, reachable only with LIFETRAC_TX_PIPELINE=v3. The v2 path's only mid-burst listen points are a 0-timeout drain AFTER each successful fragment (856) and the idle/inter-train gaps. _send_command_frame (636) also routes through wait_for_tx_done, so RX frames arriving while acks radiate are dropped too. This reproduces exactly the measured 1/12 command-delivery incident (runs D/F) whenever a deployment runs the default pipeline under sustained video TX; base-side RS-1.5 retries (20 attempts/10 s) partially mask it but REQ_KEYFRAME and RADIO_PROFILE delivery latency balloons.
  - failure: Deployment runs without LIFETRAC_TX_PIPELINE=v3 while streaming continuous video; operator taps the encode-mode pill; the base's 0x63 copies keep landing during fragment ToA windows and are logged as 'unrelated frame' and dropped; after 20 retries the base gives up and the tractor never changes mode.
  - verifier correction: The defect is real but the drop window is mis-attributed. During a fragment's actual time-on-air the tractor's SX1276 cannot receive at all (half-duplex; firmware parks the modem in STANDBY after each TX — see _ensure_rxcont, image_tx_daemon.py:554-571), so base command copies landing during ToA are lost at RF in v2 AND v3 alike. What the default v2 path uniquely discards are command frames received during the inter-fragment RXCONT gaps whose RX_FRAME_URCs surface on the UART while the host is blocked in wait_for_tx_done for the next fragment (image_tx_daemon.py:825-826) or the next command-ack copy (line 636): wait_for_tx_done (method_h_stage2_tx_probe_v2.py:2342-2343) logs them as "unrelated frame" and never re-queues them. The post-fragment _drain_rx_frames(link, 0.0) at line 856 performs no wire read (zero timeout) and only recovers frames incidentally stashed in urc_queue by _ensure_rxcont's register request() transactions. The RS-1.x dispatch fix (lines 1008-1019) is reachable only with LIFETRAC_TX_PIPELINE=v3 (line 804 gate, default "v2" at line 158). Base-side RS-1.5 retries (20 attempts / 10 s, image_rx_daemon.py:451) partially mask the loss, matching the claimed masking behavior.
- **[CONFIRMED] (medium)** `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:1090` — Stale RETAINED encode ack is re-radiated as 0x68 on every daemon (re)connect, and the base clears its ENCODE_MODE retry on ANY 0x68 without checking content
  - detail: camera_service publishes its encode ack RETAINED on lifetrac/v25/status/encode_mode (camera_service.py:1065-1066). image_tx_daemon subscribes to that topic at every MQTT connect (line 1090), so the broker immediately re-delivers the retained (possibly old) ack into _on_enc_ack (1081-1082), and _service_control_plane forwards it over the air as CMD_OP_ENCODE_MODE_ACK (645-649). On the base, image_rx_daemon.py:596-598 calls _clear_pending(CMD_OP_ENCODE_MODE) on ANY 0x68 — it never compares the ack's 'requested'/'effective' fields against the pending command — and republishes the stale JSON retained on the base status topic that the web UI renders. Same stale-retained-pin failure class as the radio_profile incident flagged in the task context.
  - failure: Operator selects mono_g4 while the tractor image_tx_daemon is restarting; on reconnect the daemon forwards the retained ack from the previous 'full' selection; the base sees a 0x68, cancels the mono_g4 retry, and the UI shows mode=full as freshly confirmed — the mode change is silently lost.
  - verifier correction: Claim is accurate as stated, with one precision: the stale ack is re-radiated not only on daemon restart but on ANY MQTT reconnect (paho re-fires on_connect and re-subscribes at image_tx_daemon.py:1090, triggering retained redelivery), so a broker blip mid-run can also emit a spurious 0x68. Fix surface exists on both sides: the tractor could skip forwarding when msg.retain is set (or compare ack ts), and the base could parse the 0x68 JSON and only _clear_pending when ack["requested"] (or ["effective"]) matches the pending command's mode byte, which is available in _pending_cmds[CMD_OP_ENCODE_MODE]["body"][-1].
- **[CONFIRMED] (medium)** `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/camera_service.py:1231` — Runtime quality and link-budget control are unreachable in the LoRa-only deployment — 0x63 changes mode only; CMD_CAMERA_QUALITY/CMD_LINK_PROFILE/CMD_ROI_HINT are dead code under USE_LORA_BRIDGE
  - detail: The only mutators of WEBP_QUALITY (dispatch_back_channel 1094-1099) and LinkBudget (1109-1119) live in the KISS back-channel dispatcher, whose reader thread is skipped when USE_LORA_BRIDGE=1 (1231-1235, deliberate to avoid UART contention). The LoRa 0xFB command set (lora_proto.py:955-965) has no quality/link-profile/ROI opcode, and image_tx_daemon._dispatch_command handles none. So in the strict-path deployment the base can change the encode MODE (whose fixed per-mode caps at camera_service.py:516-525 do bite) but can never adjust WEBP_QUALITY or the per-frame byte budget at runtime — LinkBudget.bytes stays frozen at its boot value from LIFETRAC_FRAGMENT_BUDGET (1169-1174).
  - failure: Link degrades mid-run; the base wants to shrink the per-frame fragment budget or drop WebP quality without a mode change; no wire path exists — the only lever is the coarse mode ladder, and a CMD_LINK_PROFILE-style adjustment silently cannot happen until the service is restarted with new env.
  - verifier correction: As stated, with one refinement: LinkBudget.bytes freezes at its boot value from _resolve_byte_budget() (camera_service.py:718-733) — that is LIFETRAC_FRAGMENT_BUDGET when set, otherwise (under USE_LORA_BRIDGE) the LIFETRAC_FRAGMENT_BUDGET_BRIDGE_DEFAULT of 12 fragments — not exclusively LIFETRAC_FRAGMENT_BUDGET. Additionally, the base-side sender for this lever (LinkProfileEmitter, base_station/link_monitor.py:457-534) exists but emits the legacy pack_command format and is instantiated only in tests, so the gap is on both ends of the wire.

### Significant findings NOT adversarially verified (verify-cap overflow)
- (medium) `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/camera_service.py:977` — Mode-change race can ship one frame whose codec header byte disagrees with how its tiles were encoded
  - detail: _apply_encode_mode mutates the module-global ENCODE_MODE from the paho network thread (1037-1038) while the main loop may be inside _build_frame. Each _encode_tile call reads the global independently (572), and the frame's codec byte is computed AFTER the tile loop via _codec_for_mode(ENCODE_MODE) (977). A flip landing mid-frame yields tiles encoded under the old mode (e.g. full-colour WebP) with a header codec byte for the new mode (e.g. CODEC_MONO_G4), or a mix of old/new-mode tiles under one codec byte. The encode-cache key (947) reads the global at yet another instant, so a mis-keyed blob can also be cached. The ack itself is correctly emitted only after the global is assigned — the race is about the frame in flight, not the ack ordering.

### Low-severity notes
- `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/camera_service.py:437` — Base UI offers btc4_per_tile(4)/btc4_per_frame(5) but the tractor clamps both to Y_ONLY(1) — two of six pill positions produce identical output with zero extra bandwidth savings — Enum VALUES match end-to-end for everything that overlaps: base EncodeMode (lora_proto.py:80-91) FULL=0..ADAPTIVE=7 aligns exactly with camera_service's constants 0-7 and ENCODE_MODE_NAMES; the base sends a single raw byte (image_rx_daemon.py:701) that camera_service interprets with the same numbering — no numeric mismatch. But _ENCODE_MODE_IMPLEMENTED (437-444) excludes 4, 5, and 7, so the web UI's choices/cycle order (web_ui.py:95-102, 1933-1940), which include btc4_per_tile and btc4_per_frame, are demoted to Y_ONLY on arrival. The ack does carry clamped:true and the requested vs effective pair, so the UI CAN render the mismatch — but the cycle still wastes two positions, and any auto-demotion ladder step through BTC4 (link_monitor ENCODE_MODE_LADDER) saves no bandwidth versus Y_ONLY until it reaches MONO_G4.
- `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/camera_service.py:429` — ENCODE_MODE_RAWSTREAM(8) is implemented tractor-side but absent from the base EncodeMode enum and UI — unreachable over the normal 0x63 path — camera_service implements mode 8 (RAWSTREAM: full-colour WebP, container stripped; _encode_tile 575-598, CODEC_WEBP_RAWSTREAM 491) and lists it in _ENCODE_MODE_IMPLEMENTED. The base's EncodeMode IntEnum stops at ADAPTIVE=7 (lora_proto.py:80-91), so a string request 'rawstream' raises KeyError in image_rx_daemon._on_encode_mode_msg (691) and is dropped as a bad payload; web_ui's choices exclude it too. Only a raw integer {"mode": 8} published directly to control/encode_mode_override (the int path at image_rx_daemon.py:693 does no enum validation) or boot env LIFETRAC_ENCODE_MODE=8 can reach it. Effectively dead over the operator-facing wire contract.
- `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:645` — Unlocked read-then-clear of _enc_ack_out can drop an encode ack that arrives in the window between the read and the None assignment — _on_enc_ack (1081-1082, paho thread) overwrites self._enc_ack_out; _service_control_plane (645-647, TX-worker thread) does 'ack = self._enc_ack_out; if ack is not None: self._enc_ack_out = None' with no lock. An ack published between the read and the clear is silently discarded. Latest-wins is the stated design, and the base's RS-1.5 retry re-triggers the whole chain (re-publish, re-apply, re-ack), so it self-heals — but each healing round costs a redundant forced keyframe and extra airtime.
- `LifeTrac-v25/DESIGN-CONTROLLER/firmware/tractor_x8/image_tx_daemon.py:590` — No hydraulic/drive opcode dispatch exists on the tractor despite the LoRa-only control-plane claim; unknown 0xFB opcodes are silently dropped — The task context (commit 3b1fb347) states hydraulic/drive commands travel base->tractor as 0xFB frames, but lora_proto._CMD_OPS contains only {0x60,0x63,0x65,0x66,0x67,0x68} and _dispatch_command handles only 0x60/0x63/0x65/0x67 (0x66/0x68 received by the tractor fall through every branch and are ignored — harmless since they are tractor->base). parse_command_frame returns None for any opcode outside _CMD_OPS (lora_proto.py:987-988), so a future hydraulic opcode sent by a newer base is dropped with no log at all — forward-compatible by design, but it means there is currently NO drive-command path in the reviewed tractor files, and no observability if one is attempted.

---
## Completeness critic (cross-check pass)

**Verdict:** The three reviews are individually high-quality and their headline confirmed findings all survive independent spot-checks: I re-verified estop fire-and-forget (app.js:383-385, bare fetch, no handlers), the controls-locked release latch (app.js:366 'if (controlsLocked) return;' swallows pointerup), the dead link-stats sidebar rows (app.js:216 matches '/status/link' which nothing publishes; real data is on control/link_airtime per lora_bridge.py:691 and video/link_stats per image_rx_daemon.py:112/1000), the missing Leaflet vendor file (glob for leaflet* across the whole repo returns nothing, so map.html:117 404s and map.js dies), the /ws/state snapshot lacking 'link' and 'gps' keys (state_publisher.py:81-102 - only link_power and link_stats exist), and the impossible params round-trip (params_service.py:184 subscribes params/set on the tractor-localhost client while web_ui.py:2109 publishes on the base broker). Both frontend reviews report refuted:[] and nothing I checked contradicts a confirmed item, so the visible verdicts are SOUND - however the 'two trace verdicts' I was explicitly asked to sanity-check were truncated out of the RESULTS payload and could not be checked. The material gaps are systemic rather than per-control: (1) the safe-mode plane - lora_bridge publishes control/safe_mode_active and link_monitor safe mode overrides the operator encode pin, while app.js:225-227 deliberately discards the signal into an empty setSafeMode stub, so the celebrated 'verified end-to-end' encode chain can be silently demoted with no UI trace; (2) lora_bridge is a second unmodeled subscriber/actor on control/encode_mode_override and cmd/req_keyframe, breaking the reviews' single-owner delivery model and permitting double air-commands; (3) several publishes (bench_radio, badge_refusal, view_mode, video/canvas) and one env-gated ingest backdoor (cmd/image_frame) have no counterparty and were never inventoried. Overall completeness grade: good on controls and routes (near-100% coverage of interactive elements and endpoints), incomplete on MQTT topology - the reviews inventoried topics from the UI outward but did not do the reverse sweep from the daemons inward, which is exactly where the five cross-check issues above live.

### Cross-check issues
- frontend-main's delivery cross-check ('image_rx_daemon subscribes only radio_profile, encode_mode_override, req_keyframe' vs 'lora_bridge.py:193-195 subscribes the cmd topics') is INCOMPLETE: lora_bridge.py also subscribes cmd/req_keyframe (:198) and control/encode_mode_override (:204). Consequence missed by all reviewers: encode_mode_override has TWO daemon-side subscribers that each drive a radio TX (image_rx_daemon 0xFB 0x63 and lora_bridge's encode_ctrl.command_frame at :544), so its conclusion 'encode_mode/radio_profile/req_keyframe are the only topics with a verified 0xFB path [via image_rx_daemon]' understates the topology - a mode change can be commanded twice, by two processes, on up to two serial ports.
- Server review claims '30 HTTP routes + 4 WebSockets'; the decorator grep of web_ui.py yields 34 HTTP routes + 4 WebSockets (lines 1002-2123). Four HTTP routes are unaccounted for in its count; since its inventory text is truncated I cannot tell which ones it dropped, but every route with a frontend caller IS covered somewhere across the three reviews, so the discrepancy is a counting/inventory-hygiene issue rather than a missed live control.
- Server review lumps '/api/params GET+POST' into its orphan list alongside /ws/image, /api/logout, /api/session, but /api/params is NOT an orphan - settings.html:120 (GET) and :135 (POST) call it. frontend-secondary correctly classifies it as called-but-dead-transport. The two reviews disagree on classification; frontend-secondary is right.
- No fabricated backends found in the other direction: every frontend fetch/WebSocket target found by grep across base_station/web/ (/api/settings/accel[+refresh], /api/params, /api/settings/pin, /api/settings/encode_mode, /api/settings/radio_profile, /api/build_config/{state,source,preview-diff,upload}, /api/audit, /api/autonomy/calculate-sweep, /api/estop, /api/camera/select, /api/bench/radio, /api/encode_mode/{current,cycle}, /api/health/refusal, /api/audit/view_mode, /api/login, /ws/{control,telemetry,state}) resolves to a real route in web_ui.py, confirming the server review's 'no frontend call targets a missing route' claim.
- frontend-main's sidebar inventory (app.js:188-229 handler) omits the '/control/safe_mode_active' branch at app.js:225 - the one branch whose behavior (unconditional setSafeMode(false) into an empty stub) hides an active bridge-side override of the operator's own encode-mode control that both frontend reviews declared 'verified end-to-end'. The end-to-end encode verdict is wire-correct for the 0x63 command path but wrong as a system claim: link_monitor safe mode can demote the effective mode after the ack, invisibly.

### Areas the layer reviews missed
- Safe-mode display chain entirely uninventoried: lora_bridge.py:445-465 publishes lifetrac/v25/control/safe_mode_active (initial state at :211, callback wired at :241), link_monitor.py:206-260 lets safe mode OVERRIDE the operator encode pin (test_link_monitor.py:44 'test_safe_mode_engages_and_overrides_operator_pin'), and app.js:225-227 receives the topic but deliberately calls setSafeMode(false) unconditionally while setSafeMode itself is an empty stub (app.js:544 'function setSafeMode(_active) {}'). Net effect no reviewer caught: the bridge can silently demote the operator's encode mode with zero UI indication, and the frontend comment mislabels an actively-published topic as 'legacy'.
- lora_bridge.py:513-546 is a second, independent actor on control/encode_mode_override: it parses the mode, force-mutates encode_ctrl (set_operator_ceiling, ._candidate, ._safe_mode_active=False at :542) and builds/queues its own command frame (encode_ctrl.command_frame at :544). No review examined this handler or its interaction with image_rx_daemon's 0xFB 0x63 path.
- web_ui.py:843-848 + :898-899: env-gated bench backdoor LIFETRAC_ALLOW_CMD_IMAGE_FRAME subscribes cmd/image_frame and feeds _ingest_tile_delta directly, letting a local synthetic publisher paint the operator canvas. Not in any inventory (the server review text is truncated, but its visible portions do not mention it).
- video/canvas chain is dead at BOTH ends: web_ui.py:792-800 forwards lifetrac/v25/video/canvas blobs to /ws/image subscribers, but grep finds no publisher of video/canvas anywhere in the repo (the comment credits image_pipeline/reassemble.py, which does not publish it) and no frontend client connects to /ws/image. The server review flagged /ws/image as an orphan route but nobody noticed the topic it forwards is also publisher-less.
- Audit-intent topics published into the void: control/bench_radio (web_ui.py:1394), control/badge_refusal (web_ui.py:1185), control/view_mode (web_ui.py:1201) have no consuming subscriber in any daemon; the only 'subscriber' is web_ui's own control/# loopback which just forwards them to /ws/telemetry where app.js ignores them. If these are meant to reach the audit trail via the broker, nothing writes them there. Reviews described the publishes but never checked for consumers.
- cmd/req_keyframe multi-writer/multi-reader topology uninventoried: publishers are web_ui.py:969 AND image_rx_daemon.py:184 (its own I-frame recovery); subscribers are image_rx_daemon.py:1022 AND lora_bridge.py:198. With two radio-owning subscribers a single request can be transmitted twice over the air; no review modeled this.
- The 'two trace verdicts' the task asks me to sanity-check are ABSENT from the RESULTS payload I received - it truncates mid-way through the server review's controls list. I could only sanity-check the per-review confirmed/refuted arrays (see verdict). Whoever assembles the final report should re-run this critic with the full trace output.
- Minor: no logout control exists anywhere in the UI (POST /api/logout web_ui.py:1790 and GET /api/session :1800 have zero frontend callers - server review noted the orphan routes but not the operator-facing consequence that sessions can only expire, never be ended deliberately).
