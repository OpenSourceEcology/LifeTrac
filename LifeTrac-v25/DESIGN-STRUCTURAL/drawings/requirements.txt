# Pinned so regenerated PDFs stay byte-identical until a part really changes.
# Bump deliberately; expect every PDF to be rewritten once when you do.
ezdxf==1.4.4
numpy==2.4.6
PyYAML==6.0.1
reportlab==5.0.1
