# LifeTrac v25 — Master Test Program

**Status:** living document, updated every implementation round.
**Last updated:** 2026-04-29 (Round 42).
**Owner:** every PR that adds, removes, or re-scopes a test MUST update
the relevant section of this file in the same commit. See
[§7 Update Protocol](#7-update-protocol).

This page is the **single index** for every test the LifeTrac v25
controller subsystem runs. It does NOT duplicate the implementation
plan, the HIL runbook, or the round-by-round status memo — it
links into them and answers three questions they don't:

1. **What tests exist today?** (catalog tables — §2, §3, §4)
2. **Which Implementation-Plan item (IP-XXX) does each test cover?** (§5)
3. **What is the gap between SIL coverage and the HIL bench?** (§4)

---

## Table of Contents

1. [Tier overview](#1-tier-overview)
2. [SIL test catalog](#2-sil-test-catalog)
3. [Compile-gate catalog](#3-compile-gate-catalog)
4. [HIL bench matrix](#4-hil-bench-matrix)
5. [Per-IP traceability](#5-per-ip-traceability)
6. [How to run](#6-how-to-run)
7. [Update protocol](#7-update-protocol)

---

## 1. Tier overview

The v25 controller is verified at three tiers. Each tier catches a
different class of regression — none of the three is sufficient on its
own.

| Tier | Where it runs | What it catches | Count (Round 20) |
|---|---|---|---|
| **SIL** (software-in-the-loop) | CI on every push, pure Python stdlib + project deps | Logic, protocol, state-machine regressions; cross-language byte-identity (C ↔ Python). **No hardware required.** | **581 tests** across [49 files](#2-sil-test-catalog) |
| **Compile gates** | CI on every push, `arduino-cli compile` against pinned cores | Toolchain drift, missing libraries, link errors, flash/RAM regressions, FQBN / sketch-folder convention breaks. | **5 jobs** (4 sketches × 1, plus stub/real-crypto on handheld) — [§3](#3-compile-gate-catalog) |
| **HIL** (hardware-in-the-loop) | Bench, manually, per [HIL_RUNBOOK.md](DESIGN-CONTROLLER/HIL_RUNBOOK.md) | Real-radio timing, valve-coil energise/release latency, RF link budget, IRQ jitter, on-air decode after cold-boot. | **12 procedures** — [§4](#4-hil-bench-matrix). The matrix opens with **W4-pre** (board bring-up sanity, no RF) and **W4-00** (dual-Portenta LoRa-stack bench) so board health is confirmed and the radio link is proven on real silicon before any valve-coil work is attempted. |

**Coverage rule:** every HIL bench item should have a SIL companion that
verifies the *logic* it depends on (state machine, threshold, ramp
shape, ladder transitions). The HIL run is then a *confirmation* that
the on-air / valve-coil reality matches the model — much faster to
diagnose when something fails. SIL coverage of a HIL item is documented
in [§4](#4-hil-bench-matrix).

---

## 2. SIL test catalog

All paths relative to [`LifeTrac-v25/DESIGN-CONTROLLER/base_station/tests/`](DESIGN-CONTROLLER/base_station/tests/).

| File | Tests | Scope | IP refs | W4 gate | Round |
|---|---|---|---|---|---|
| [test_accel_select.py](DESIGN-CONTROLLER/base_station/tests/test_accel_select.py) | 10 | Coral accelerator detection and state management. | — | — | — |
| [test_audit_log.py](DESIGN-CONTROLLER/base_station/tests/test_audit_log.py) | 5 | JSONL append-only audit log rotation and thread-safety. | — | — | — |
| [test_axis_ramp_sil.py](DESIGN-CONTROLLER/base_station/tests/test_axis_ramp_sil.py) | 10 | M7 proportional valve ramp timing + mixed-mode skip arbitration. Round 44 (BC-21): rewrote to operate on **logical** axes (`left_track`, `right_track`, `arms`, `bucket`) AFTER differential mixing; W4-05/W4-06 invariants preserved. | — | W4-05, W4-06 | 18, 44 |
| [test_track_mix_ramp_sil.py](DESIGN-CONTROLLER/base_station/tests/test_track_mix_ramp_sil.py) | 25 | BC-21 mix-then-ramp + K-A4 coordinated bilateral track stop: differential mixing math `leftTrack = clip(lhy + lhx, ±127)` + mirror across spin / smooth-curve / saturation cases (RT-A); per-side coil mapping energises correctly for spin-turns that pre-BC-21 produced zero coil activation (RT-B); smooth-curve turn (forward-pinned `lhy`, ramping `lhx` in) does NOT step the right track post-BC-21, with control-arm proving the pre-BC-21 short-circuit was visible (RT-C); K-A4 coordinated bilateral stop pins both track ramps to the larger starting magnitude's duration so the tractor doesn't pivot during release — symmetric, asymmetric, and same-tick zero-crossing all green (RT-D); single-track release does not falsely trigger K-A4 (RT-F); firmware source tripwire pins `BC-21` / `K-A4` / `clip_to_int8` markers, presence of new logical-axis globals, removal of pre-BC-21 stick-axis globals, and per-side coil-mapping pattern (RT-G). | — | (W4-05 augmented) | 44 |
| [test_reversal_brake_sil.py](DESIGN-CONTROLLER/base_station/tests/test_reversal_brake_sil.py) | 21 | BC-22 reversal brake: same-tick sign-flip on a still-energised axis converts the operator's reversal command into a decay-to-zero ramp using the standard release ladder, then holds the axis at zero for `REVERSAL_BRAKE_MS = 100` (2 × RAMP_TICK_MS) before the new direction is allowed to pass through. BR-A pins the decay phase shape (no snap to new direction; ramp uses release-ladder duration; magnitude is monotonically non-increasing across the decay window; brake window is armed at the deadline tick). BR-B pins the settle window invariants (effective stays at 0 for the full window even with the reversed stick still held; input changes during settle are ignored; constant equals 2 × RAMP_TICK_MS by design). BR-C pins post-brake resumption (new direction passes through immediately after window expiry; zero input post-brake leaves the axis at zero with no ghost ramp). BR-D pins the non-reversal cases that must NOT trigger BC-22 (same-sign magnitude jumps, plain release, sub-deadband flicker, mixed-mode-skip release). BR-E pins arm-axis reversals using the arm decay ladder while sharing the track settle window. BR-F is a firmware source tripwire for the `BC-22` marker, `REVERSAL_BRAKE_MS = 100` constant, `brake_until_ms` + `reversal_pending` struct fields, and the brake-arm / brake-clear code paths. | — | (extends W4-05 envelope) | 45 |
| [test_steering_priority_sil.py](DESIGN-CONTROLLER/base_station/tests/test_steering_priority_sil.py) | 24 | BC-23 preserve-steering + BC-24 spin-turn flow boost. SP-A pins the BC-23 helper math (`mix_tracks_preserve_steering` scales BOTH track intents by `127/max_mag` when either exceeds ±127): non-saturating identity, single-side saturation, both-sides saturation, sign preservation under negative saturation, mixed-sign saturation, just-above-threshold scaling, exact-127 boundary no-op, pure-throttle no-scale. SP-B pins the steering-ratio invariant end-to-end through `FourAxisArbiter`: across a sweep of saturating turns, the post-BC-23 differential is no further from the operator's commanded `lhx` than the pre-BC-23 independent-clip baseline; the saturating case `lhy=120, lhx=80` improves measurable differential authority. SP-C pins BC-24's spin-turn flow boost: pure spin (`lhy=0, lhx=120`) reaches the full 10000 mV flow set-point because the track contribution becomes `min(127, |left|+|right|) = 127`; partial spin uses sum-of-magnitudes; large spins clamp at int8 max. SP-D pins the non-spin cases that must NOT trigger the boost (forward / reverse / smooth-curve / arms-only — each must use the max formula). SP-E firmware source tripwire pins the `BC-23` + `BC-24` markers, the `mix_tracks_preserve_steering` helper, the absence of pre-BC-23 `clip_to_int8(left_track_raw)` pattern, and the `spin_turn` detection variable. | — | (extends W4-05 envelope) | 46 |
| [test_stick_curve_sil.py](DESIGN-CONTROLLER/base_station/tests/test_stick_curve_sil.py) | 20 | BC-25 / K-A2 per-stick response curve exponent. SC-A pins the pure `_apply_stick_curve(v, n)` math: identity at n=1.0 across the int8 range, zero is a fixed point at every supported exponent, full stick (±127) preserved at every exponent, n=2.0 maps 64→32 (square-law), n=1.5 lands strictly between linear and square at v=64, monotonic in input on [0, 127], sign preserved for non-trivial magnitudes, out-of-range inputs clamp rather than walk past the LUT. SC-B pins the default-identity contract: `FourAxisArbiter()` defaults to exponent 1.0 and produces the same byte-for-byte mixed outputs as the pre-BC-25 pipeline (legacy fixtures unaffected). SC-C pins the K-A2 motivating use case — at n=2.0 a 50%-stick input compresses to ~32 (vs 64 linear), and an arbiter run with `lhy=64` produces tracks of (32, 32). SC-D pins top-end authority: pegged sticks reach ±127 at n=1.0 / 1.5 / 2.0. SC-E pins that the curve applies **pre-mixing** — a pure `lhx=64` at n=2.0 produces tracks (32, -32), not the (16, -16) that post-mix curving would give. SC-F is a firmware/source tripwire pinning the `BC-25` marker, the `apply_stick_curve` symbol, the `init_stick_curve_lut` setup-time call, and the four call sites wrapping `cf.axis_lh_x` / `cf.axis_lh_y` / `cf.axis_rh_x` / `cf.axis_rh_y`; also pins the schema enum `[1.0, 1.5, 2.0]` and the `ui.stick_curve_exponent` row in `CAPABILITY_INVENTORY.md`. | — | (extends W4-05 envelope) | 48 |
| [test_ramp_shape_sil.py](DESIGN-CONTROLLER/base_station/tests/test_ramp_shape_sil.py) | 21 | BC-26 / K-A3 ramp interpolation shape selector. RS-A pins the `linear` default as byte-for-byte identical to the pre-Round-49 truncation formula `start * (duration - elapsed) / duration` across a representative spread of `start`, `duration_ms`, `elapsed` (positive, negative, quarter / half / three-quarter / endpoint elapsed); also pins that omitting the `shape` argument defaults to `linear`. RS-B pins the `scurve` endpoint contract: `elapsed=0 → start`, `elapsed≥duration → 0`, `duration=0 → 0`, monotonic non-increasing across the full ramp for positive `start`. RS-C pins the K-A3 closed-form quarter-points distinguishing `scurve` from `linear`: `t=0.25` smoothstep ≈ `0.854 × start` strictly higher than linear's `0.75`, `t=0.5` both shapes pass through `~0.5 × start`, `t=0.75` smoothstep ≈ `0.146 × start` strictly lower than linear's `0.25`. RS-D pins sign preservation for negative `start` and that the magnitude profile matches the positive case within 1 LSB of half-up rounding asymmetry. RS-E exercises `FourAxisArbiter(ramp_shape="scurve")` end-to-end: drive forward at `lhy=127`, release, capture every tick of the K-A4-coordinated 2 s release ramp; both shapes reach 0 at the deadline and `scurve` is strictly higher than `linear` at the quarter-point sample. RS-F pins the default-identity contract: `FourAxisArbiter()` with no `ramp_shape` argument produces byte-for-byte the same release trajectory as the explicit `linear` arbiter. RS-G is a firmware/source tripwire pinning the `BC-26` marker, the `ramp_interpolate` helper symbol, the `LIFETRAC_HYDRAULIC_RAMP_SHAPE_SCURVE` macro consumption, the helper call site replacing the pre-Round-49 inline interpolation, the schema `linear`/`scurve` enum, and the `hydraulic.ramp_shape` row in `CAPABILITY_INVENTORY.md`. | — | (extends W4-05 envelope) | 49 |
| [test_confined_space_sil.py](DESIGN-CONTROLLER/base_station/tests/test_confined_space_sil.py) | 18 | BC-27 / K-D2 confined-space mode (minimal version). CS-A pins the pure `ramp_duration_ms()` formula across the full ladder: `confined=False` matches the pre-Round-50 ladder byte-for-byte for both track (500 / 1000 / 2000 ms) and arm (250 / 500 / 1000 ms) magnitudes; `confined=True` multiplies by `3 / 2` (integer-exact: 250→375, 500→750, 1000→1500, 2000→3000); negative magnitudes treated as absolute. CS-B pins the default-identity contract: `FourAxisArbiter()` defaults `confined_space_mode_enabled=False` and produces byte-for-byte the same release-ramp trajectory as the explicit `False` arbiter. CS-C exercises a release ramp from `lhy=127` end-to-end: baseline last-non-zero tick ~38–40 (= 2000 ms / 50 ms tick), confined ~58–60 (= 3000 ms / 50 ms tick), and confined outlasts baseline by ≥ 18 ticks. CS-D pins K-A4 coordination under confined mode: drive `lhx=80, lhy=120` so the two pre-release magnitudes are asymmetric (left=127, right=25), then release; both tracks must read 0 at and beyond the K-A4 deadline tick (40 baseline / 60 confined) and stay there. CS-E composes BC-27 with BC-25 stick curve (`stick_curve_exponent=2.0`): both effects independently identifiable — curve compresses 64 → 32 at activation, confined-space mode stretches the subsequent release ramp. CS-F composes BC-27 with BC-26 scurve ramp shape: scurve still reaches 0 by the 3000 ms confined deadline (~tick 56–60), trajectory remains monotone non-increasing, and the quarter-point sample stays well above linear's `0.75 × start`. CS-G is a firmware/source tripwire pinning the `BC-27` marker, the `LIFETRAC_UI_CONFINED_SPACE_MODE_ENABLED` macro consumption, the `base * 3u` / `) / 2u` multiplier expression in `ramp_duration_ms()`, the schema `confined_space_mode_enabled` leaf, and the `ui.confined_space_mode_enabled` row in `CAPABILITY_INVENTORY.md`. | — | (extends W4-05 envelope) | 50 |
| [test_operator_profile_sil.py](DESIGN-CONTROLLER/base_station/tests/test_operator_profile_sil.py) | 21 | BC-28 / K-D1 operator profile preset (minimal). New enum leaf `ui.operator_profile` ∈ `{normal, gentle, sport}`, default `normal` = byte-for-byte identity (individual operator-feel leaves stand as authored). `gentle` overrides `ui.confined_space_mode_enabled = true` AND `hydraulic.ramp_shape = "scurve"`. `sport` overrides `ui.confined_space_mode_enabled = false`, `hydraulic.ramp_shape = "linear"` AND `ui.stick_curve_exponent = 1.0`. Overrides apply at config-load time inside `build_config.load()` via `_apply_operator_profile_overrides()`. OP-A pins the pure helper math: `normal` is a no-op (even when authored values would otherwise read as a bundle), `gentle` flips its two leaves regardless of authored values and leaves `stick_curve_exponent` alone, `sport` flips its three leaves regardless of authored values, sentinel fields under `ui`/`hydraulic`/`safety` stay untouched, an unknown profile is a defensive no-op (schema catches it upstream), and the public `OPERATOR_PROFILE_OVERRIDES` table is pinned exactly. OP-B exercises the loader integration via temp-TOML + `LIFETRAC_BUILD_CONFIG_PATH`: default `build.default.toml` yields `operator_profile == "normal"` with all three operator-feel leaves at identity values; a `gentle` toml that authors `confined_space_mode_enabled = false` still ends up `True` after load and `ramp_shape = "scurve"`; a `sport` toml that authors gentle settings ends up with all three sport overrides applied; `cfg.raw` reflects the post-override state so `config_sha256` is consistent across consumers (audit log, hot-reload diff, codegen). OP-C pins codegen integration: a `gentle` config emits `LIFETRAC_UI_CONFINED_SPACE_MODE_ENABLED 1` and `LIFETRAC_HYDRAULIC_RAMP_SHAPE_SCURVE 1` and `LIFETRAC_UI_OPERATOR_PROFILE "gentle"` + side macro; a `sport` config emits `..._CONFINED_SPACE_MODE_ENABLED 0`, `LIFETRAC_HYDRAULIC_RAMP_SHAPE_LINEAR 1`, `LIFETRAC_UI_STICK_CURVE_EXPONENT 1.0f`, and the sport side macro; a `normal` config (default toml) is byte-identity to the authored values. OP-D pins schema rejection of an unknown profile value (e.g. `"race_mode"`). OP-E is a source tripwire for the `BC-28` marker, `_apply_operator_profile_overrides` symbol + `OPERATOR_PROFILE_OVERRIDES` table in `build_config.py`, the schema enum, the `operator_profile = "normal"` line in `build.default.toml`, and the `ui.operator_profile` row in `CAPABILITY_INVENTORY.md`. | — | (extends W4-05 envelope) | 51 |
| [test_axis_deadband_sil.py](DESIGN-CONTROLLER/base_station/tests/test_axis_deadband_sil.py) | 23 | BC-29 / K-D3 configurable axis deadband. New int leaf `ui.axis_deadband` ∈ `[0, 32]`, default `13` (≈10% of int8 full scale) = byte-for-byte identity vs the pre-Round-52 hard-coded firmware constant. Firmware sources `AXIS_DEADBAND` from the codegen-emitted `LIFETRAC_UI_AXIS_DEADBAND` macro. DB-A pins schema validation: required-list update, in-range values 0/1/13/20/32 all accepted, out-of-range low (-1), out-of-range high (33), wrong type (string), and float-where-int-expected all rejected. DB-B pins default-value identity: default toml authors `axis_deadband = 13`, `cfg.ui.axis_deadband` is the int 13, codegen emits `LIFETRAC_UI_AXIS_DEADBAND 13`, and the leaf carries `reload_class = restart_required`. DB-C pins override round-trip: a temp toml authoring 5 / 0 / 32 round-trips through `build_config.load()` and the codegen header emits the corresponding macro value (5 case also pins that legacy `LIFETRAC_UI_AXIS_DEADBAND 13` does not appear). DB-D firmware tripwires pin: `BC-29` marker present, `AXIS_DEADBAND` initialised exactly as `(int8_t)LIFETRAC_UI_AXIS_DEADBAND`, the legacy `static const int8_t AXIS_DEADBAND = 13;` literal is gone, and the constant is still consumed in `axis_active()`, in the per-coil activation block (six track / arm patterns), in BC-24 spin-turn detection, and in the flow-set-point computation (both the threshold guard and the `(mag - AXIS_DEADBAND)` numerator). DB-E docs tripwires pin schema bounds (`"minimum": 0, "maximum": 32`), default-toml leaf + BC-29 doc comment, and the `ui.axis_deadband` row in `CAPABILITY_INVENTORY.md`. | — | (extends W4-05 envelope) | 52 |
| [test_ramp_heatmap_sil.py](DESIGN-CONTROLLER/base_station/tests/test_ramp_heatmap_sil.py) | 29 | K-E2 web UI ramp-state heatmap data model in [base_station/ramp_heatmap.py](DESIGN-CONTROLLER/base_station/ramp_heatmap.py). Per-axis, per-tick classification of the (raw, effective) pair into one of five states — `idle`, `matched`, `reversal`, `decay`, `mushy` — for the operator-visible "why did my stop feel mushy" overlay. RH-A pins the public `STATES` tuple shape so a regression that silently renames a state trips here. RH-B pins the signed `lag()` function (`raw - effective`). RH-C pins the precedence ladder of `classify_state()`: `idle` when both inputs are within `deadband`; `reversal` when both are active but opposite-sign (BC-22 territory — wins over `mushy` because reversal-brake lag is *expected*); `decay` when raw is idle but effective is still asserting (release ramp in flight); `mushy` when same-sign `|lag| > mushy_threshold`; `matched` otherwise. The boundary case (`|lag| == threshold`) classifies as `matched`; `|lag| == threshold + 1` classifies as `mushy`. Custom `deadband` and `mushy_threshold` overrides plumb through; both reject negative values with `ValueError`. RH-D pins `build_heatmap_row()` shape: five keys (`now_ms`, `raw`, `effective`, `lag`, `state`) all int-coerced (state is str). RH-E pins `build_heatmap()` top-level shape (`deadband`, `mushy_threshold`, `axes` keys), per-axis stream-order preservation, threshold override propagation, and a canonical pursuit-ramp scenario where a 127-floored stick climbs through 0 → 40 → 80 → 127 effective and produces `mushy / mushy / mushy / matched`. RH-F pins `DEFAULT_DEADBAND` to the BC-29 `ui.axis_deadband` config default, and pins `DEFAULT_MUSHY_THRESHOLD = 40` (~31% of int8 full scale) as the documented starting point. | — | (extends W4-05 envelope) | 53 |
| [test_motion_primitives_sil.py](DESIGN-CONTROLLER/base_station/tests/test_motion_primitives_sil.py) | 15 (1 skipped) | MP-01..MP-12 motion-primitive traceability. One test class per primitive in [DESIGN-KINEMATICS/MOTION_PRIMITIVES.md](DESIGN-KINEMATICS/MOTION_PRIMITIVES.md), each pinning the post-mix logical-axis signature through `FourAxisArbiter`. MP-01 / MP-02 (drive forward / reverse): both tracks equal sign, arms / bucket zero. MP-03 (skid turn): both tracks same sign, asymmetric, no spin-turn flow boost. MP-04 (pivot turn): one track exactly zero, other carries full mixed magnitude. MP-05 (spin in place): tracks opposite signs equal magnitude, BC-24 flow boost reaches 10000 mV. MP-06..MP-09 (arms / bucket only): single logical axis active, tracks zero. MP-10 (drive + arms): max-magnitude flow rule selects arms over track; single-EFC contention modelled. MP-11 (turn + bucket): pivot turn coexists with bucket, tracks and bucket independent; spin + bucket combined under spin-turn boost. MP-12 (float): build-gated by hydraulic.spool_type (BC-19); skipped with reason. Cross-cutting: sub-deadband inputs on all four sticks produce zero on every logical axis and zero flow_sp. | — | (extends W4-05 envelope) | 47 |
| [test_back_channel_dispatch.py](DESIGN-CONTROLLER/base_station/tests/test_back_channel_dispatch.py) | 12 | X8 back-channel dispatcher for camera control opcodes. | IP-104 | (W4-08 — partial) | — |
| [test_boot_phy_sil.py](DESIGN-CONTROLLER/base_station/tests/test_boot_phy_sil.py) | 11 | Boot-PHY first-frame decode: LADDER[0] + radio.begin() consistency across all nodes. | IP-006 | W4-07 | 20 |
| [test_build_config_loader_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_loader_sil.py) | 20 | Build-config loader + JSON Schema + canonical TOML round-trip: schema strict-object well-formedness, fallback-chain precedence (env→unit-id→default), strict validation (unknown / out-of-range / wrong-type / bad-enum / pattern / missing-section all raise), deterministic `config_sha256` (canonical-JSON formula, whitespace-invariant, leaf-change-sensitive), inventory-parity (schema↔CAPABILITY_INVENTORY.md no drift). | BC-01, BC-02 | — | 27 |
| [test_build_config_consumption_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_consumption_sil.py) | 11 | web_ui + lora_bridge consume BuildConfig at boot: `config_loaded` audit entries (unit_id / source_path / config_sha256), `_CAMERA_IDS` filtered against `cameras.{front,rear,implement,crop_health}_present` (canonical = auto+front, full = all five, count==0 = empty), `MAX_CONTROL_SUBSCRIBERS` reads from BUILD, graceful degradation when loader fails, source tripwires for BC-04 audit call in both daemons. | BC-04 | — | 28 |
| [test_build_config_delivery_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_delivery_sil.py) | 19 | BC-10 hot-reload contract: every schema leaf carries a `reload_class` annotation (enum-checked, `firmware_required` set pinned to `schema_version` + `safety.m4_watchdog_ms`); `diff_reload_classes` classifies live-only / restart-required / firmware-required changes and bubbles the strictest as `worst`; undeclared-leaf drift raises; `evaluate_quiescence` blocks on each of the four preconditions independently (parked seconds, control subscribers, M7 TX queue, engine load) with operator-facing reasons; loader/schema leaf-coverage parity; source tripwires for BC-10 helpers + `reload_class` keyword. | BC-10 | — | 29 |
| [test_build_config_installer_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_installer_sil.py) | 24 | BC-10 delivery (alpha): installer-bundle envelope (round-trip, filename pattern, body-tamper breaks SHA, version / unit_id / sentinel / filename mismatch all refused), `tools/lifetrac-config` CLI (validate / bundle / verify / diff with reload-class output), base-side `/config/download` route (PIN-gated, returns parseable bundle with documented Content-Disposition, emits `config_download` audit entry carrying the SHA), source tripwires for CLI subcommands + web route + `config_bundle` module. | BC-10 | — | 29b |
| [test_build_config_installer_daemon_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_installer_daemon_sil.py) | 30 | BC-10 delivery (beta): X8-side installer daemon (`apply_bundle` writes target atomically and returns `applied`; same SHA returns `noop` without touching the target; filename / body `unit_id` mismatch + schema violation + `firmware_required` diff all rejected without writing; `discover_bundles` filters by unit_id and ignores foreign files; `write_result_file` round-trips JSON), feedback contract (status table covers every install status; LED patterns for `applied` / `rejected` / `deferred` pinned verbatim; OLED line stays under 21 chars), watch-and-reload (`ConfigWatcher.poll` returns `noop` / `applied`-when-quiet / `deferred`-when-busy / `restart_pending` / `rejected`-on-schema-violation; `restart_pending` flag survives subsequent polls; base `/api/build_config/state` route surfaces watcher state + 401s without session), `push` CLI subcommand (local copy, local `--apply` invokes daemon, ssh dry-run prints plan), and source tripwires for the four new modules + web route + audit verb. | BC-10 | — | 29b |
| [test_build_config_admin_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_admin_sil.py) | 16 | BC-05 web admin form: `/config` page (303 redirect without session, widget IDs present when authed), `GET /api/build_config/source` returns the verbatim active TOML body and 401s without session, `POST /api/build_config/preview-diff` (identical / live / restart-required classification + schema-violation returns `ok:false` without writing the target), `POST /api/build_config/upload` (identical no-op / live atomic-write rewrites target byte-for-byte / schema-rejected without writing / unit_id-mismatch rejected / firmware-required rejected without writing / 401 without session), source + HTML tripwires for the four routes and `config_upload` audit verb. | BC-05 | — | 30 |
| [test_build_config_codegen_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_codegen_sil.py) | 22 | BC-03 firmware codegen: header shape (guard + identity macros + ASCII/LF/no-trailing-whitespace), leaf parity (every JSON-Schema scalar leaf appears as a `#define`), type formatting (int bare / bool 1-0 / float `f`-suffixed with decimal / string double-quoted / enum side-macros active=1 others=0), determinism (two emissions byte-identical + on-disk `firmware/common/lifetrac_build_config.h` matches a fresh emission against `build.default.toml` — the CI gate that fails when a schema edit forgets the regen step), legacy aliases (every entry in `_LEGACY_ALIASES` is emitted and resolves to a macro defined earlier in the file), CLI codegen (`--out` byte-identical to direct `emit_header`, `--check` clean / drift / canonical-firmware-header), and source tripwires for module + CLI + on-disk header carrying the Round 31 / BC-03 markers. | BC-03 | — | 31 |
| [test_build_config_variant_matrix_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_variant_matrix_sil.py) | 14 | BC-07 variant matrix: four representative fleet shapes (canonical / no-camera / no-IMU+GPS / single-axis) synthesised from the canonical default and round-tripped through loader (BC07_A), `web_ui` module-import + `_CAMERA_IDS` filter + `MAX_CONTROL_SUBSCRIBERS` substitution (BC07_B), Round 31 codegen header values (BC07_C: present flags zero / model strings persist / single-axis numbers emitted verbatim with `f`-suffix), BC-10 reload-class diff (BC07_D: canonical self-diff empty / camera diffs stay live / axis-count diffs bubble to restart_required), and pairwise-distinct `config_sha256` across the four variants so a single `config_loaded` audit line disambiguates the booted shape (BC07_E). | BC-07 | — | 32 |
| [test_build_config_aux_section_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_aux_section_sil.py) | 13 | BC-11 first-class auxiliary attachment ports: schema declares `[aux]` with `port_count` / `coupler_type` (enum incl. `none`) / `case_drain_present`, every leaf `restart_required` (BC11_A); `AuxConfig` dataclass + canonical default conservative (port_count=0, coupler_type="none", case_drain_present=false) + loader rejects missing section (BC11_B); codegen emits `LIFETRAC_AUX_*` macros + coupler_type enum side-macros (BC11_C); `iter_reload_classes` covers every aux leaf + `diff_reload_classes` classifies aux changes as `restart_required` (BC11_D); committed on-disk firmware header contains the aux block — drift gate that fails when an aux schema edit forgets the regen step (BC11_E). | BC-11 | — | 33 |
| [test_hil_dispatch_applicability_sil.py](DESIGN-CONTROLLER/base_station/tests/test_hil_dispatch_applicability_sil.py) | 13 | BC-06 hil/dispatch.ps1 build-config awareness: `gate_applicability.json` rules file shape + dispatcher-vs-rules gate-set parity + every predicate uses a known op + every dotted path resolves against canonical default (BC06_A); canonical default fleet is fully applicable (BC06_B); no-handheld marks W4-01 + W4-02 N/A while leaving W4-03..W4-10 applicable (BC06_C); zero cameras marks W4-08 N/A (BC06_D); bang-bang `proportional_flow=false` marks W4-05 N/A (BC06_E); `arm_axis_count=0` marks W4-06 N/A (BC06_F); `lifetrac-config dump-json` subprocess produces byte-identical output across two runs + payload carries the four documented top-level keys (BC06_G). | BC-06 | — | 34 |
| [test_build_config_doc_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_doc_sil.py) | 5 | BC-08 BUILD_CONFIG.md operator/integrator onboarding doc drift gate: every required schema section is named in the doc (BC08_A); every `sub.add_parser` registered CLI subcommand is named in the doc + no orphan subcommands appear in the synopsis table (BC08_B); every `reload_class` value the schema declares is named in the doc + schema vocabulary is exactly the documented three (BC08_C); every relative-path link target resolves on disk (BC08_D); every `estop_topology` enum value appears verbatim — representative drift gate covering safety-critical enum (BC08_E). | BC-08 | — | 35 |
| [test_hardware_bom_xref_sil.py](DESIGN-CONTROLLER/base_station/tests/test_hardware_bom_xref_sil.py) | 6 | BC-09 HARDWARE_BOM.md `## Capability cross-reference` appendix drift gate: every capability id listed in the appendix exists as a schema property (BC09_A no orphan IDs); every entry in the curated `_PHYSICAL_CAPABILITIES` set appears in the appendix and the curated set itself stays in lockstep with the schema (BC09_B bidirectional); every row's evidence cell carries a `Tier 1`/`Tier 2`/`Tier 3` token or a `planned` marker (BC09_C); every relative link in the appendix resolves on disk (BC09_D); appendix heading is present and self-references the enforcing test file (BC09_E). | BC-09 | — | 36 |
| [test_config_inventory_sil.py](DESIGN-CONTROLLER/base_station/tests/test_config_inventory_sil.py) | 14 | BC-14 `lifetrac-config inventory` fleet-wide audit-log aggregator: parser passes only `config_loaded` records and silently skips malformed / non-dict / missing-field lines and missing files (BC14_A); aggregator dedupes by `(unit_id, config_sha256)` so a reflashed unit produces two rows, with stable sort by unit_id then last_seen descending (BC14_B); CSV header matches `INVENTORY_FIELDS` + byte-identical across two runs + Markdown table has header + separator (BC14_C); directory argument picks up rotated `audit*.jsonl*` siblings and ignores stray files + `--format markdown` switches the renderer (BC14_D); ISO-8601 UTC timestamp formatting + zero/negative renders empty (BC14_E). | BC-14 | — | 37 |
| [test_cybersecurity_buildconfig_xref_sil.py](DESIGN-CONTROLLER/base_station/tests/test_cybersecurity_buildconfig_xref_sil.py) | 5 | BC-13 CYBERSECURITY_CASE.md `$9 Build-config attack surface` drift gate: $9 has the four expected sub-headings 9.1–9.4 (BC13_A); every `lifetrac-config <subcommand>` named in the section resolves through the live `_build_parser()` (BC13_B catches stale or typo'd subcommand names); every relative Markdown link target in $9 exists on disk (BC13_C); the $9.2 STRIDE table covers each Z-CONFIG asset row from $9.1 (BC13_D); $9 self-references this enforcing test file (BC13_E). | BC-13 | — | 38 |
| [test_boot_self_test_sil.py](DESIGN-CONTROLLER/base_station/tests/test_boot_self_test_sil.py) | 15 | BC-12 SIL half: `boot_self_test.run_self_test(config, hardware)` returns ok=True with no findings on a matching inventory (BC12_A); axis-count mismatches (track or arm) yield error findings and ok=False (BC12_B); proportional-flow asymmetry is one-sided — hardware-can't-do-what-config-commands fails, hardware-capable-but-config-bang-bang passes (BC12_C); IMU/GPS presence and pressure-sensor-count mismatches are errors (BC12_D); camera-count asymmetry (short or extra) is warning, ok stays True (BC12_E); LoRa region + aux port count + coupler type mismatches are errors (BC12_F); `emit_audit()` writes the canonical `boot_self_test` JSONL event with `{component, unit_id, config_sha256, ok, error_count, warning_count, findings[…], started_ts, finished_ts}` (BC12_G); 12-entry finding-code catalogue is byte-stable (BC12_H — catches silent renames before dashboards break). HIL half (real M4 Modbus probe) deferred. | BC-12 | — | 39 |
| [test_config_loaded_schema_version_sil.py](DESIGN-CONTROLLER/base_station/tests/test_config_loaded_schema_version_sil.py) | 5 | BC-14B follow-up to BC-14: `config_loaded` audit emitters in `web_ui` and `lora_bridge` now include `schema_version` so the BC-14 inventory column is populated. BC14B_A web_ui's `_audit_config_loaded` writes `schema_version` as int from `BUILD.schema_version`; BC14B_B lora_bridge inline block writes the same field; BC14B_C end-to-end aggregator surfaces the column for new lines + back-compat for legacy lines without it; BC14B_D source greps both call-sites for `schema_version=` so a refactor that drops the field fails the gate. | BC-14B | — | 40 |
| [test_config_self_test_cli_sil.py](DESIGN-CONTROLLER/base_station/tests/test_config_self_test_cli_sil.py) | 10 | BC-12C bench-side CLI half of BC-12: new `lifetrac-config self-test` subcommand composes Round 39's `boot_self_test.run_self_test` with Round 37's CLI. BC12C_A matching inventory passes in both text + JSON formats and reports zero findings; BC12C_B axis-count mismatches exit non-zero (EXIT_USAGE) with stable error codes `AXIS_COUNT_TRACK` / `AXIS_COUNT_ARM` and findings carry `severity`/`expected`/`observed`; BC12C_C warning-only mismatches (camera count) keep exit 0 and surface as `severity=warning`; BC12C_D bad inputs (missing file, non-object JSON, missing fields, unparseable JSON) all exit non-zero with diagnostic stderr; BC12C_E source-grep tripwire pins `"self-test"` parser, `cmd_self_test` definition, and `set_defaults(func=cmd_self_test)`. BUILD_CONFIG.md synopsis bumped eight→nine subcommands (BC-08 doc gate auto-validates). | BC-12C | — | 41 |
| [test_config_diff_cli_sil.py](DESIGN-CONTROLLER/base_station/tests/test_config_diff_cli_sil.py) | 10 | BC-15 machine-readable JSON form for the BC-10 `lifetrac-config diff` subcommand: `--format text` (default) is byte-identical to the pre-BC-15 output (BC15_A no-diff, live-class change, explicit `--format text` matches default); `--format json` emits the canonical sorted-keys compact form (consistent with `dump-json`) carrying `changed` / `classes` / `worst` / `is_empty` / `restart_required` / `firmware_required` (BC15_B no-diff empty payload, live-only / restart_required / firmware_required classification, `worst` promotion across multiple changes, sorted-compact output); argparse rejects unknown `--format` values with exit 2 and stderr (BC15_C). BUILD_CONFIG.md `diff` row updated; doc-coverage gate auto-validates. | BC-15 | — | 42 |
| [test_fleet_key_provisioning_sil.py](DESIGN-CONTROLLER/base_station/tests/test_fleet_key_provisioning_sil.py) | 24 | Fleet-key sanity: bridge `_load_fleet_key()` failure modes; firmware `#ifndef LIFETRAC_ALLOW_UNCONFIGURED_KEY` guard, OLED text, ESTOP magic write. | IP-008 | W4-10 | 22 |
| [test_hil_harness_completeness_sil.py](DESIGN-CONTROLLER/base_station/tests/test_hil_harness_completeness_sil.py) | 13 | W4-XX HIL harness contract: file existence per gate, harness honours `_common`/Write-GateHeader/Assert-Section0/Write-HilResult/HIL_RUNBOOK back-ref, JSONL schema invariants (W4-01..W4-10 pattern + PASS/FAIL/SKIP/ABORT enum + 5-key firmware-SHA bundle), dispatcher gate-set parity. | HIL-01 | (operationalises all W4-XX) | 26 |
| [test_keyframe_round_trip_sil.py](DESIGN-CONTROLLER/base_station/tests/test_keyframe_round_trip_sil.py) | 17 | Camera back-channel keyframe round-trip: bridge translation, encrypt/KISS air round-trip, M7 wrap → X8 dispatcher, SF7/BW250 < 100 ms LoRa-leg airtime model. | IP-103, IP-104 | W4-08 | 22 |
| [test_command_frame_fuzz.py](DESIGN-CONTROLLER/base_station/tests/test_command_frame_fuzz.py) | 5 | FT_COMMAND wire format CRC and layout invariants. | IP-108 | — | — |
| [test_control_frame_fuzz.py](DESIGN-CONTROLLER/base_station/tests/test_control_frame_fuzz.py) | 7 | FT_CONTROL packet structure, stick clipping, CRC validation. | IP-108 | — | 8 |
| [test_crypto_vectors.py](DESIGN-CONTROLLER/base_station/tests/test_crypto_vectors.py) | 2 | AES-GCM encryption vectors (Python vs. firmware C). | IP-003 | — | — |
| [test_followup_sweep.py](DESIGN-CONTROLLER/base_station/tests/test_followup_sweep.py) | 15 | Image-pipeline Coral inference logging and debounce. | — | — | — |
| [test_image_pipeline.py](DESIGN-CONTROLLER/base_station/tests/test_image_pipeline.py) | 18 | Canvas tile reassembly, delta encoding, grid mismatches. | — | — | — |
| [test_image_reassembly_fuzz.py](DESIGN-CONTROLLER/base_station/tests/test_image_reassembly_fuzz.py) | 12 | Image fragment reordering, timeouts, interleaved streams. | — | — | 13 |
| [test_link_monitor.py](DESIGN-CONTROLLER/base_station/tests/test_link_monitor.py) | 3 | LoRa heartbeat window classification for link quality. | — | — | — |
| [test_link_monitor_orchestrator.py](DESIGN-CONTROLLER/base_station/tests/test_link_monitor_orchestrator.py) | 2 | Orchestrator state machine for image encode-mode transitions. | — | — | — |
| [test_link_tune_sil.py](DESIGN-CONTROLLER/base_station/tests/test_link_tune_sil.py) | 16 | M7 adaptive SF ladder walk-down/up + 500 ms revert deadline + twice-back-to-back announce. | — | W4-02 | 19 |
| [test_lora_proto.py](DESIGN-CONTROLLER/base_station/tests/test_lora_proto.py) | 19 | CRC16, KISS encoding, replay window, CSMA, PHY routing. | IP-004 | — | — |
| [test_m4_safety_sil.py](DESIGN-CONTROLLER/base_station/tests/test_m4_safety_sil.py) | 11 | M4 watchdog trip, seqlock safety, E-stop latency < 100 ms. | IP-105, IP-106 | W4-01, W4-03 | 8 |
| [test_m7_tx_queue_sil.py](DESIGN-CONTROLLER/base_station/tests/test_m7_tx_queue_sil.py) | 6 | M7 non-blocking TX queue with priority preemption. | IP-107 | (W4-09 — partial) | — |
| [test_modbus_slave_sil.py](DESIGN-CONTROLLER/base_station/tests/test_modbus_slave_sil.py) | 17 | Opta Modbus watchdog, fail-count latch, ramp arbitration. | IP-205 | W4-04 | 10, 14 |
| [test_mqtt_retry_backoff_sil.py](DESIGN-CONTROLLER/base_station/tests/test_mqtt_retry_backoff_sil.py) | 13 | Web-UI MQTT connect: fake-clock retry/backoff schedule (`0.5→1→2→4→5→5`), 30 s monotonic deadline, env-var host, source-grep tripwire on contract literals. | IP-201 | — | 25 |
| [test_nonce_seq_threadthrough_sil.py](DESIGN-CONTROLLER/base_station/tests/test_nonce_seq_threadthrough_sil.py) | 13 | Bridge `_on_mqtt_message` reserves seq, stamps cleartext header, threads same seq into `_tx(nonce_seq=)` so GCM nonce seq == header seq across all four command arms; 100-message acceptance + replay window check + 16-bit wrap. | IP-102 | — | 24 |
| [test_protocol_constants_sil.py](DESIGN-CONTROLLER/base_station/tests/test_protocol_constants_sil.py) | 12 | Wave-3 polish constants tripwire: `TELEM_MAX_PAYLOAD` C↔Python pin, `REG_AUX_OUTPUTS` triplet (slave define + master enum + write-handler mask), handheld `s_btn_change_ms` boot-anchor in `setup()`. | IP-306, IP-303, IP-301 | — | 23 |
| [test_replay_window_invariant.py](DESIGN-CONTROLLER/base_station/tests/test_replay_window_invariant.py) | 2 | Cross-language ReplayWindow C/Python bit-for-bit match. | IP-101 | — | 9 |
| [test_telemetry_fragmentation.py](DESIGN-CONTROLLER/base_station/tests/test_telemetry_fragmentation.py) | 7 | Telemetry packet fragmentation and reassembly. | IP-204 | — | — |
| [test_telemetry_fragmentation_fuzz.py](DESIGN-CONTROLLER/base_station/tests/test_telemetry_fragmentation_fuzz.py) | 15 | Multi-stream telemetry reassembly, wrap-around, GC. | IP-204 | — | 11 |
| [test_web_ui_accel.py](DESIGN-CONTROLLER/base_station/tests/test_web_ui_accel.py) | 5 | Web UI Coral toggle and settings persistence. | — | — | — |
| [test_web_ui_auth.py](DESIGN-CONTROLLER/base_station/tests/test_web_ui_auth.py) | 8 | PIN auth, session cookies, lockout, IP rate-limiting. | IP-206, IP-207 | — | — |
| [test_web_ui_validation.py](DESIGN-CONTROLLER/base_station/tests/test_web_ui_validation.py) | 15 | Pydantic input validation, subscriber admission bounds. | IP-202, IP-203 | — | — |
| [test_ws_subscriber_concurrency.py](DESIGN-CONTROLLER/base_station/tests/test_ws_subscriber_concurrency.py) | 4 | WebSocket pool cap, concurrent admit/discard, MQTT fan-out. | IP-202, IP-208 | — | 12 |

**Totals:** 49 files, 581 tests, 1 skipped.

> **Test counts:** the "Tests" column counts `def test_*` methods, not
> parametrized cases. The skipped test in `test_accel_select.py` is
> Coral-hardware-gated and is expected to skip in CI.

---

## 3. Compile-gate catalog

CI workflow: [`.github/workflows/arduino-ci.yml`](.github/workflows/arduino-ci.yml).
All paths relative to [`LifeTrac-v25/DESIGN-CONTROLLER/firmware/`](DESIGN-CONTROLLER/firmware/).

| Job | FQBN | Sketch | Blocking? | Notes |
|---|---|---|---|---|
| `python-protocol-tests` | n/a | n/a | **Yes** | Runs the Python 3.11 unittest suite from §2. Must be green before any compile job is dispatched. |
| `firmware-compile-handheld` (stub) | `arduino:samd:mkrwan1310` | [`handheld_mkr/`](DESIGN-CONTROLLER/firmware/handheld_mkr/) | **Yes** | Stub-crypto build — 55 932 B / 21 % flash. (Round 15) |
| `firmware-compile-handheld` (real) | `arduino:samd:mkrwan1310` | [`handheld_mkr/`](DESIGN-CONTROLLER/firmware/handheld_mkr/) | **Yes** | Real-crypto build (`-DLIFETRAC_USE_REAL_CRYPTO`) — 59 940 B / 22 % flash. (Round 15) |
| `firmware-compile-opta` | `arduino:mbed_opta:opta` | [`tractor_opta/`](DESIGN-CONTROLLER/firmware/tractor_opta/) | **Yes** | 171 520 B / 8 % flash, 65 120 B / 12 % RAM. (Round 16) Requires `Arduino_Opta_Blueprint` lib install. |
| `firmware-compile-tractor-h7` | `arduino:mbed_portenta:envie_m7` | [`tractor_h7/`](DESIGN-CONTROLLER/firmware/tractor_h7/) | **Yes** | M7 build — 207 888 B / 9 % flash. Stages `lora_proto` + `shared_mem.h` to `src/`. (Round 17) |
| `firmware-compile-tractor-m4` | `arduino:mbed_portenta:envie_m7:target_core=cm4` | [`tractor_h7_m4/`](DESIGN-CONTROLLER/firmware/tractor_h7_m4/) | **Yes** | M4 watchdog — 87 032 B. Separate sketch folder per Portenta dual-core convention. (Round 17) |
| `firmware-compile-plan` | n/a | n/a | informational | Resolves the ESP32 / extra hardware sketches for inventory only — not gating. |

**5 of 5 production-firmware jobs are blocking** (no `continue-on-error`).
The Round 17 dual-core split documented why a single sketch with
`#if defined(CORE_CM4)` cannot work: arduino-cli scans `.ino` `#include`
directives regardless of `target_core`, so RadioLib + Modbus get pulled
into the M4 build. The two-folder layout matches Arduino IDE's stock
PortentaDualCore example.

---

## 4. HIL bench matrix

Procedures: [`DESIGN-CONTROLLER/HIL_RUNBOOK.md`](DESIGN-CONTROLLER/HIL_RUNBOOK.md).
"Bench-only residual" = the part of the procedure that no SIL test
can replace (timing on real silicon, RF link budget, valve-coil energise
latency, …).

| W4-XX | Goal | Pass criterion | SIL coverage | Bench-only residual |
|---|---|---|---|---|
| **W4-pre** | **Board bring-up sanity** — prove each Portenta + Max Carrier is healthy *before* the SX1276 PA is keyed. Sub-gates: (a) USB-CDC enumeration on the host (both boards as distinct serial ports, stable across re-plug); (b) Arduino blink + USB-Serial echo at 115200 8N1 (rules out brick / bad solder on the carrier); (c) carrier 3.3 V + 5 V rails within ±5 % under no-RF load (multimeter on Max Carrier test points); (d) M7↔M4 dual-core handshake — stock PortentaDualCore example flashes both cores and exchanges a counter via SRAM4; (e) flash + run the [`firmware/tractor_h7/`](DESIGN-CONTROLLER/firmware/tractor_h7/) image with `LIFETRAC_RADIO_DISABLE` (or equivalent stub) and verify boot-banner + heartbeat log on USB-CDC — the firmware reaches `loop()` without RF. | (a) Both boards enumerate; ports survive 10 re-plug cycles each; (b) blink visible + echo round-trip < 50 ms; (c) 3.3 V ± 5 %, 5 V ± 5 %; (d) M4 counter increments visible from M7 console for 60 s with zero gaps; (e) boot banner reaches steady-state `heartbeat` log line within 5 s of reset, no panics in 60 s. | None — these are pre-firmware checks that the SIL suite cannot model (real USB enumeration, real rail voltages, real dual-core SRAM4 link). | All of it. **Hardware required:** 2× Portenta + 2× Max Carrier + 2× USB-C cable + multimeter. **No antennas attached for this gate.** |
| **W4-00** | **LoRa stack dual-Portenta bench** — prove the full radio + framing + AES-GCM + replay-window stack on real silicon at short range, with both Max Carriers plugged into the same workstation so the operator (or VS Code + Copilot) can read both sides of every exchange over USB-CDC. Sub-gates: (a) carrier-class TX/RX at 1 m, (b) round-trip latency by SF, (c) on-air payload size matches `lp_frame_len` predictions, (d) keyframe / SSDV image-fragment delivery end-to-end. | (a) 1000-frame burst at SF7/SF8/SF9 with 0 lost frames at 1 m; (b) ControlFrame round-trip p50 within 5 % of `lora_time_on_air_ms` * 2 + 30 ms scheduler slop, p99 < p50 + 50 ms; (c) measured air-bytes within ±1 byte of `pack_command()` / `pack_telemetry()` predictions across all four PHY profiles; (d) one full keyframe (256×192 JPEG, ~3–5 kB) reassembled at the RX side via the SSDV reassembler with valid CRC and badge intact. | [test_lora_proto_sil.py](DESIGN-CONTROLLER/base_station/tests/test_lora_proto_sil.py) (frame pack/unpack, replay window, nonce builder) + [test_keyframe_round_trip_sil.py](DESIGN-CONTROLLER/base_station/tests/test_keyframe_round_trip_sil.py) (bridge → air → M7 wrap) + [test_link_tune_sil.py](DESIGN-CONTROLLER/base_station/tests/test_link_tune_sil.py) (PHY profiles + ladder timing) | Real SX1276 TX/RX timing, IRQ wiring, antenna front-end behaviour at 1 m, on-silicon AES-GCM throughput. **Hardware required:** 2× Portenta + 2× Max Carrier + 2× SMA whip antenna (separated ≥ 30 cm) + 2× USB-C cable. No 12 V supply, no valve coils, no Opta needed for this gate. **W4-pre must be green first.** |
| **W4-01** | Handheld E-stop latch < 100 ms across SF7/8/9 | 100/100 captures `latency_ms < 100`; p99 < 80 ms | [test_m4_safety_sil.py](DESIGN-CONTROLLER/base_station/tests/test_m4_safety_sil.py) | Real PSR-alive falling edge → relay terminal de-energise time |
| **W4-02** | Link-tune walk-down SF7→8→9 with < 1 % packet loss; revert deadline 500 ms | full ladder walk both directions; histogram monotonic; zero missed E-stop | [test_link_tune_sil.py](DESIGN-CONTROLLER/base_station/tests/test_link_tune_sil.py) | RF attenuator + < 1 % loss measurement |
| **W4-03** | M7↔M4 watchdog trip < 200 ms; `estop_request = 0xA5A5A5A5` magic on SRAM4 | 10/10 runs `t_trip - t_halt < 200 ms`; magic confirmed | [test_m4_safety_sil.py](DESIGN-CONTROLLER/base_station/tests/test_m4_safety_sil.py) | SRAM4 capture probe + JTAG halt of M7 |
| **W4-04** | Modbus failure → E-stop within 1 s; IP-205 counter + 10-failure latch | `t_apply_-1 - t_disconnect < 1.0 s`; audit log entry; no stuck motion | [test_modbus_slave_sil.py](DESIGN-CONTROLLER/base_station/tests/test_modbus_slave_sil.py) | Physical RS-485 cable disconnect mid-run |
| **W4-05** | Proportional valve ramp-out: track 2 s, arm 1 s; coil stays engaged | track 2.00 ± 0.10 s; arm 1.00 ± 0.05 s; linear R² > 0.98 | [test_axis_ramp_sil.py](DESIGN-CONTROLLER/base_station/tests/test_axis_ramp_sil.py) | A0602 output probe + valve-coil voltage capture |
| **W4-06** | Mixed-mode skip: sibling-active release drops coil < 50 ms (no ramp) | 10/10 runs drop < 50 ms | [test_axis_ramp_sil.py](DESIGN-CONTROLLER/base_station/tests/test_axis_ramp_sil.py) | Same probe rig as W4-05 |
| **W4-07** | Cold-boot first-frame decode; no `bad_header` events | 20/20 alternate power-on orders; zero `bad_header` in 10 s window | [test_boot_phy_sil.py](DESIGN-CONTROLLER/base_station/tests/test_boot_phy_sil.py) | Audit-log capture of first 10 s after each cold boot |
| **W4-08** | Camera back-channel `CMD_REQ_KEYFRAME` < 200 ms end-to-end | 30/30 runs < 200 ms; LoRa leg < 100 ms | [test_back_channel_dispatch.py](DESIGN-CONTROLLER/base_station/tests/test_back_channel_dispatch.py) (X8 dispatcher) + [test_keyframe_round_trip_sil.py](DESIGN-CONTROLLER/base_station/tests/test_keyframe_round_trip_sil.py) (bridge translation, air round-trip, M7 wrap, airtime budget model) | Real H747 Serial1 baud stability + camera-encoder I-frame produce time on real X8 silicon |
| **W4-09** | Async M7 TX state machine: `startTransmit()` + `isTransmitDone()` IRQ timing | Phase 1 time-on-air within 5 % of theory + IRQ→flag p99 < 2 ms; Phase 2 W4-03 + alive_tick jitter p99 < 5 ms | [test_m7_tx_queue_sil.py](DESIGN-CONTROLLER/base_station/tests/test_m7_tx_queue_sil.py) (queue logic only) | Real SX1276 IRQ wiring on real H747 silicon — **hard-blocked, no SIL substitute possible** |
| **W4-10** | Fleet-key provisioning sanity: missing key halts M7 + bridge at startup | compile fail (missing header); OLED `FLEET KEY NOT PROVISIONED`; container exit ≠ 0 | [test_fleet_key_provisioning_sil.py](DESIGN-CONTROLLER/base_station/tests/test_fleet_key_provisioning_sil.py) (bridge loader failure modes; source-parse of handheld + tractor `#ifndef` guard, OLED string, ESTOP magic write) + handheld stub/real compile gates | OLED visual confirmation on real bench; systemd-unit restart behavior on real X8 |

**Tier status (Round 53):**
- **W4-pre** is the new earliest gate — board health (USB enumeration,
  rails, dual-core handshake, blink-equivalent firmware boot) confirmed
  with no RF activity. No antennas attached. Cannot be SIL-substituted.
- **W4-00** is the LoRa-stack gating phase — every later W4-XX gate
  that exercises the radio assumes W4-00 (a)–(d) have already passed.
  W4-00 has full SIL coverage of the logic surface (lora_proto,
  keyframe round-trip, link-tune); the bench run is a real-silicon
  confirmation only.
- 9 of 10 prior gates (W4-01, W4-02, W4-03, W4-04, W4-05, W4-06,
  W4-07, W4-08, W4-10) have SIL coverage of the *logic surface* and
  need only a bench-confirmation pass.
- 1 of 10 prior gates (W4-09) has *partial* SIL — the M7 TX queue
  logic is covered, but the IRQ-driven async startTransmit /
  isTransmitDone on real SX1276 silicon is hard-blocked from SIL
  substitution.

---

## 5. Per-IP traceability

Every Implementation-Plan item (IP-001 … IP-309) from
[`AI NOTES/2026-04-28_Controller_Code_Review_Implementation_Plan_v1_0.md`](AI%20NOTES/2026-04-28_Controller_Code_Review_Implementation_Plan_v1_0.md)
mapped to its verifying test. **"—" in the test column means the IP is
landed but has no automated verification** — those are the cells that
should drive future SIL rounds.

### Wave 0 — blockers / repo hygiene

| IP | Severity | Title | Verified by |
|---|---|---|---|
| IP-001 | BLOCKER | Rename sketch folders / `.ino` files | All compile gates (§3) |
| IP-002 | BLOCKER | Wire `LIFETRAC_PIN`, `LIFETRAC_MQTT_HOST`, `LIFETRAC_LORA_DEVICE` env vars | (env-var contract — manual) |
| IP-003 | BLOCKER | Fix `lp_decrypt(ct_len)` contract mismatch | [test_crypto_vectors.py](DESIGN-CONTROLLER/base_station/tests/test_crypto_vectors.py) |
| IP-004 | BLOCKER | Fix M7 CSMA frequency bug (hop index vs Hz) | [test_lora_proto.py](DESIGN-CONTROLLER/base_station/tests/test_lora_proto.py) |
| IP-005 | BLOCKER | Resolve `LIFETRAC_SETTINGS_PATH` env-var drift | (env-var contract — manual) |
| IP-006 | BLOCKER | Match boot PHY to `LADDER[0]` (BW250) | [test_boot_phy_sil.py](DESIGN-CONTROLLER/base_station/tests/test_boot_phy_sil.py) + W4-07 |
| IP-007 | BLOCKER | CI must build firmware and run all suites | [arduino-ci.yml](.github/workflows/arduino-ci.yml) (§3) |
| IP-008 | BLOCKER | Provisioned-or-fail fleet-key loading | [test_fleet_key_provisioning_sil.py](DESIGN-CONTROLLER/base_station/tests/test_fleet_key_provisioning_sil.py) + handheld compile gates + W4-10 |

### Wave 1 — protocol / pipeline

| IP | Severity | Title | Verified by |
|---|---|---|---|
| IP-101 | HIGH | Wire `NonceStore` into the LoRa bridge | [test_replay_window_invariant.py](DESIGN-CONTROLLER/base_station/tests/test_replay_window_invariant.py) |
| IP-102 | HIGH | Thread `nonce_seq` through bridge | [test_nonce_seq_threadthrough_sil.py](DESIGN-CONTROLLER/base_station/tests/test_nonce_seq_threadthrough_sil.py) |
| IP-103 | HIGH | Subscribe and forward `cmd/req_keyframe` | [test_back_channel_dispatch.py](DESIGN-CONTROLLER/base_station/tests/test_back_channel_dispatch.py) + [test_keyframe_round_trip_sil.py](DESIGN-CONTROLLER/base_station/tests/test_keyframe_round_trip_sil.py) |
| IP-104 | HIGH | Wire X8 `camera_service` to M7 UART | [test_back_channel_dispatch.py](DESIGN-CONTROLLER/base_station/tests/test_back_channel_dispatch.py) + [test_keyframe_round_trip_sil.py](DESIGN-CONTROLLER/base_station/tests/test_keyframe_round_trip_sil.py) + W4-08 |
| IP-105 | HIGH | M4 read of SRAM4 must be seqlock-protected | [test_m4_safety_sil.py](DESIGN-CONTROLLER/base_station/tests/test_m4_safety_sil.py) |
| IP-106 | HIGH | Watchdog wrap on M4: require `0xA5A5A5A5` magic | [test_m4_safety_sil.py](DESIGN-CONTROLLER/base_station/tests/test_m4_safety_sil.py) |
| IP-107 | HIGH | M7 LoRa TX async (un-block M4 watchdog) | [test_m7_tx_queue_sil.py](DESIGN-CONTROLLER/base_station/tests/test_m7_tx_queue_sil.py) + W4-09 |
| IP-108 | HIGH | Fix CRC asymmetry on `FT_COMMAND` | [test_command_frame_fuzz.py](DESIGN-CONTROLLER/base_station/tests/test_command_frame_fuzz.py), [test_control_frame_fuzz.py](DESIGN-CONTROLLER/base_station/tests/test_control_frame_fuzz.py) |

### Wave 2 — base-station hardening

| IP | Severity | Title | Verified by |
|---|---|---|---|
| IP-201 | MED | MQTT connect retry/backoff | [test_mqtt_retry_backoff_sil.py](DESIGN-CONTROLLER/base_station/tests/test_mqtt_retry_backoff_sil.py) |
| IP-202 | MED | WebSocket fan-out admission cap | [test_web_ui_validation.py](DESIGN-CONTROLLER/base_station/tests/test_web_ui_validation.py), [test_ws_subscriber_concurrency.py](DESIGN-CONTROLLER/base_station/tests/test_ws_subscriber_concurrency.py) |
| IP-203 | MED | Pydantic input validation | [test_web_ui_validation.py](DESIGN-CONTROLLER/base_station/tests/test_web_ui_validation.py) |
| IP-204 | MED | Decode telemetry through `TelemetryReassembler` | [test_telemetry_fragmentation.py](DESIGN-CONTROLLER/base_station/tests/test_telemetry_fragmentation.py), [test_telemetry_fragmentation_fuzz.py](DESIGN-CONTROLLER/base_station/tests/test_telemetry_fragmentation_fuzz.py) |
| IP-205 | MED | Surface Modbus failures (counter + 10-fail latch) | [test_modbus_slave_sil.py](DESIGN-CONTROLLER/base_station/tests/test_modbus_slave_sil.py) + W4-04 |
| IP-206 | MED | Honor `X-Forwarded-For` for PIN lockout | [test_web_ui_auth.py](DESIGN-CONTROLLER/base_station/tests/test_web_ui_auth.py) |
| IP-207 | MED | Forward auth failures to `AuditLog` | [test_web_ui_auth.py](DESIGN-CONTROLLER/base_station/tests/test_web_ui_auth.py) |
| IP-208 | MED | `camera_service.py` thread race + clamp WEBP_QUALITY | [test_ws_subscriber_concurrency.py](DESIGN-CONTROLLER/base_station/tests/test_ws_subscriber_concurrency.py) |
| IP-209 | MED | Lockfile Python deps (`requirements.lock`) | (build-time — manual review of lockfile) |

### Wave 3 — firmware polish

| IP | Severity | Title | Verified by |
|---|---|---|---|
| IP-301 | MED | Initialize `s_btn_change_ms` at boot | [test_protocol_constants_sil.py](DESIGN-CONTROLLER/base_station/tests/test_protocol_constants_sil.py) |
| IP-302 | MED | Fix axis dead-band scaling | [test_axis_ramp_sil.py](DESIGN-CONTROLLER/base_station/tests/test_axis_ramp_sil.py) (deadband constant pinned) |
| IP-303 | MED | Implement `REG_AUX_OUTPUTS` or remove it | [test_protocol_constants_sil.py](DESIGN-CONTROLLER/base_station/tests/test_protocol_constants_sil.py) |
| IP-304 | MED | Stable PYTHONPATH for X8 services | (deployment — manual) |
| IP-305 | LOW | `lp_kiss_encode` over-conservative bounds | [test_lora_proto.py](DESIGN-CONTROLLER/base_station/tests/test_lora_proto.py) |
| IP-306 | LOW | Reconcile `TELEM_MAX_PAYLOAD` C(118) vs Python(120) | [test_protocol_constants_sil.py](DESIGN-CONTROLLER/base_station/tests/test_protocol_constants_sil.py) |
| IP-307 | LOW | Per-source-active OLED status threshold | — |
| IP-308 | LOW | Boot self-test PIN ordering | — |
| IP-309 | LOW | Camera tile-diff numpy vectorization | — |

### Wave 4 — bench-bring-up tooling and build-configuration

| IP | Severity | Title | Verified by |
|---|---|---|---|
| HIL-01 | INFRA | W4-XX HIL harness completeness (per-gate PowerShell skeleton, JSONL schema, dispatcher coverage) | [test_hil_harness_completeness_sil.py](DESIGN-CONTROLLER/base_station/tests/test_hil_harness_completeness_sil.py) |
| BC-01 | INFRA | Capability inventory (every optional / parameterised v25 capability enumerated with default + range + consumer) | [`CAPABILITY_INVENTORY.md`](DESIGN-CONTROLLER/CAPABILITY_INVENTORY.md) + [test_build_config_loader_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_loader_sil.py) (BC_E inventory parity) |
| BC-02 | INFRA | Build-config schema + canonical TOML + strict loader (`load(unit_id) -> BuildConfig`) with `LIFETRAC_BUILD_CONFIG_PATH`→per-unit→default fallback chain and deterministic `config_sha256` | [test_build_config_loader_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_loader_sil.py) |
| BC-04 | INFRA | web_ui + lora_bridge consume BuildConfig at boot, audit-log `config_sha256`, gate `_CAMERA_IDS` on present-flags, drive `MAX_CONTROL_SUBSCRIBERS` from `ui.max_control_subscribers` | [test_build_config_consumption_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_consumption_sil.py) |
| BC-03 | INFRA | Firmware codegen: emit a deterministic C header (`firmware/common/lifetrac_build_config.h`) from the canonical TOML so the M4/M7 firmware shares the same single source of truth (every scalar leaf becomes a `#define`, enum side-macros allow `#if` branches, legacy aliases preserve existing `LIFETRAC_M4_WATCHDOG_MS` etc.); `lifetrac-config codegen --check` drift gate. | [test_build_config_codegen_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_codegen_sil.py) |
| BC-07 | INFRA | Variant-matrix SIL: four representative fleet shapes (canonical / no-camera / no-IMU+GPS / single-axis) round-tripped end-to-end through loader, `web_ui` consumer surfaces, codegen, and BC-10 reload-class diff; pairwise-distinct SHAs so a single `config_loaded` audit line disambiguates the booted shape. | [test_build_config_variant_matrix_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_variant_matrix_sil.py) |
| BC-11 | INFRA | First-class auxiliary attachment ports: `[aux]` schema section (`port_count` / `coupler_type` / `case_drain_present`); `AuxConfig` dataclass; codegen emits `LIFETRAC_AUX_*` macros + enum side-macros so M4 can `#if LIFETRAC_AUX_PORT_COUNT > 0` to gate aux-PWM init; every aux leaf annotated `restart_required` (hardware capability → driver re-init); on-disk firmware header drift gate. | [test_build_config_aux_section_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_aux_section_sil.py) |
| BC-06 | INFRA | Per-build HIL harness gating: `hil/dispatch.ps1` reads the active build config (via `lifetrac-config dump-json`) and applies `gate_applicability.json` rules to flag inapplicable Wave-4 gates as `N/A` instead of `NOT-STARTED`; recommended-next-gate skips N/A; rules file is the single source of truth shared between PowerShell consumer and Python SIL gate. | [test_hil_dispatch_applicability_sil.py](DESIGN-CONTROLLER/base_station/tests/test_hil_dispatch_applicability_sil.py) |
| BC-08 | DOC | Operator/integrator onboarding doc [`BUILD_CONFIG.md`](DESIGN-CONTROLLER/BUILD_CONFIG.md): nine sections covering what a build-config is, where files live, the eight required sections, the three reload classes, the seven CLI subcommands, the deterministic SHA-256 identity, the audit trail, common pitfalls, and a where-to-read-next pointer table; SIL drift gate pins the doc to schema + CLI + reload-class + link-target reality. | [test_build_config_doc_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_doc_sil.py) |
| BC-09 | DOC | `## Capability cross-reference` appendix in [`HARDWARE_BOM.md`](DESIGN-CONTROLLER/HARDWARE_BOM.md): maps every physical-shape build-config leaf to the BOM section + part(s) that realise it (15 rows today); SIL drift gate pins bidirectional schema parity, evidence-cell tier-token discipline, link-target resolution, and self-reference to the enforcing test. | [test_hardware_bom_xref_sil.py](DESIGN-CONTROLLER/base_station/tests/test_hardware_bom_xref_sil.py) |
| BC-14 | TOOL | Fleet-wide config inventory: new `lifetrac-config inventory` subcommand aggregates `config_loaded` audit-log events across one or more `audit.jsonl` files into CSV / Markdown table, deduplicated by `(unit_id, config_sha256)` so a reflashed unit shows one row per distinct build; directory args auto-expand to rotated siblings; ISO-8601 UTC timestamps; byte-stable rendering. Composes naturally with Round 31 audit-log identity + Round 34 `dump-json`. | [test_config_inventory_sil.py](DESIGN-CONTROLLER/base_station/tests/test_config_inventory_sil.py) |
| BC-13 | DOC | `$9 Build-config attack surface` section in [`CYBERSECURITY_CASE.md`](DESIGN-CONTROLLER/CYBERSECURITY_CASE.md): treats the build-config subsystem as an explicit IEC-62443 asset class with Z-CONFIG zone enumeration ($9.1), 9-row STRIDE matrix mapping each asset's threats to existing mitigations ($9.2), defence-in-depth narrative tying BC-09 BOM cross-reference + `codegen --check` + BC-14 inventory + three-watchdog safe-state ($9.3), and SIL-pinned self-reference to the enforcing drift gate ($9.4). | [test_cybersecurity_buildconfig_xref_sil.py](DESIGN-CONTROLLER/base_station/tests/test_cybersecurity_buildconfig_xref_sil.py) |
| BC-12 (SIL half) | RUN | New [`boot_self_test.py`](DESIGN-CONTROLLER/base_station/boot_self_test.py) module compares the loaded `BuildConfig` against an injected `HardwareInventory` (the M4 Modbus probe will live in the HIL half, deferred) and emits a structured `SelfTestReport` with stable finding-code catalogue. Safety-significant mismatches (axis counts, proportional flow, sensor counts, presence flags, LoRa region, aux plumbing) fail boot; cosmetic camera-count mismatches degrade to warnings. `emit_audit()` writes one `boot_self_test` JSONL event per boot with flat findings so jq / Pandas can pivot on `code`. HIL half deferred. | [test_boot_self_test_sil.py](DESIGN-CONTROLLER/base_station/tests/test_boot_self_test_sil.py) |
| BC-14B | RUN | Closed Round 37 / BC-14 follow-up gap: `web_ui._audit_config_loaded` and `lora_bridge.Bridge.__init__` now include `schema_version` in the `config_loaded` audit event so the BC-14 inventory's `schema_version` column is no longer always empty. Aggregator gracefully handles legacy lines (rotated logs from before this change) by leaving the cell blank. | [test_config_loaded_schema_version_sil.py](DESIGN-CONTROLLER/base_station/tests/test_config_loaded_schema_version_sil.py) |
| BC-12C | RUN | Bench-side CLI half of BC-12: new `lifetrac-config self-test <toml> <inventory.json>` composes the Round 39 boot_self_test comparator with the Round 37 CLI. Operator captures what the M4 reports as a JSON file and runs the same self-test before flashing (or post-incident, against a captured inventory dump) without booting a tractor. Exit non-zero on any error finding, EXIT_USAGE on malformed inventory; warning-only findings keep exit 0. | [test_config_self_test_cli_sil.py](DESIGN-CONTROLLER/base_station/tests/test_config_self_test_cli_sil.py) |
| BC-15 | TOOL | Machine-readable JSON form for `lifetrac-config diff`: new `--format text|json` flag (text default is byte-identical to pre-BC-15 output). JSON payload carries `changed` / `classes` / `worst` / `is_empty` / `restart_required` / `firmware_required` in canonical sorted-keys compact form (consistent with `dump-json`) so non-Python consumers (notably `hil/dispatch.ps1` and ad-hoc shell pipelines) can decide reload-class without re-implementing the schema-aware comparator. | [test_config_diff_cli_sil.py](DESIGN-CONTROLLER/base_station/tests/test_config_diff_cli_sil.py) |
| **BC-18 (planned)** | RUN | **Hydraulic valve-centre settling timer.** New `LIFETRAC_HYDRAULIC_VALVE_SETTLING_MS` macro in `firmware/common/lifetrac_build_config.h` (default 100 ms, range 50–250); `tractor_opta.ino` valve-drive loop gates solenoid de-energise behind a settling timer that starts when each `REG_FLOW_SP_*` reaches zero; E-stop bypasses the timer. Prerequisite for using D1VW00*8*CNKW tandem-centre spool in field hardware (see [DESIGN-HYDRAULIC/SOFT_STOP_STRATEGY.md](DESIGN-HYDRAULIC/SOFT_STOP_STRATEGY.md), [DESIGN-CONTROLLER/DECISIONS.md](DESIGN-CONTROLLER/DECISIONS.md) §D-HYD1). | `tests/test_valve_settling_sil.py` (planned, 4 cases: axis→zero starts timer; EFC reaches zero before timer; valve de-energises only after timer; E-stop cancels timer) |
| **BC-19** | INFRA | **Hydraulic build-variant schema (Round 43).** Three new `[hydraulic]` schema leaves: `spool_type` (enum tandem/float/closed/open, `restart_required`), `load_holding` (enum spool_inherent/po_check/counterbalance/none, `restart_required`), `valve_settling_ms` (uint 0..250, `live`). Extends `build.default.toml`, `HydraulicConfig` dataclass, codegen header emitter (auto-emits enum side macros via schema-driven generator), `CAPABILITY_INVENTORY.md` rows, and a new in-loader cross-leaf compatibility validator `_validate_hydraulic_compatibility` (refuses tandem/closed + load_holding=none; refuses float/open + load_holding=spool_inherent; refuses non-zero `valve_settling_ms` for venting spools). BC-18 firmware reads the new spool-type and settling-ms macros instead of hard-coding tandem (see [DESIGN-CONTROLLER/DECISIONS.md](DESIGN-CONTROLLER/DECISIONS.md) §D-HYD2). | [test_hydraulic_compatibility_sil.py](DESIGN-CONTROLLER/base_station/tests/test_hydraulic_compatibility_sil.py) (BC19_A canonical loads, BC19_B four reference builds load — OSE-legacy, v25-canonical, high-performance, closed+counterbalance, BC19_C six invalid pairings rejected, BC19_D rejection-at-load tripwire); existing `test_build_config_loader_sil.py` / `test_build_config_codegen_sil.py` / `test_build_config_variant_matrix_sil.py` exercise the three new leaves at canonical values via schema-driven sweeps. |
| **BC-20 (planned, three sub-rounds)** | RUN | **IMU-adaptive ramp tuning.** Use BNO086 jerk telemetry to tune ramp seconds + valve-settling-ms toward smoother trajectories. **BC-20A passive jerk telemetry** — M7 computes `d(accel)/dt` per axis and publishes per-transition peak; web UI diagnostics surface; no control change. **BC-20B advisory recommendation engine** — base-station accumulates per-build histograms over operator-confirmed clean transitions, recommends ramp-leaf edits via existing `lifetrac-config validate/bundle/verify/push` flow (no autonomous writes). **BC-20C closed-loop online tuning (gated, opt-in)** — new schema leaf `hydraulic.adaptive_ramp_tuning` (bool, default false, `restart_required`); ±5 % step adjustments per accepted clean transition, schema-bounded, audit-logged, operator panic revert. Disabled by default; requires BC-12 HIL bench validation before enabling. See [DESIGN-HYDRAULIC/SOFT_STOP_STRATEGY.md](DESIGN-HYDRAULIC/SOFT_STOP_STRATEGY.md) §"IMU-adaptive ramp tuning". | `test_jerk_telemetry_sil.py` (planned, BC-20A); `test_ramp_recommendation_sil.py` (planned, BC-20B); `test_adaptive_ramp_tuning_sil.py` (planned, BC-20C) |
| BC-05 | INFRA | Operator-facing build-config admin form on the base UI: `GET /config` page + `GET /api/build_config/source` (verbatim TOML) + `POST /api/build_config/preview-diff` (no-write reload-class classification) + `POST /api/build_config/upload` (schema-validate, refuse `firmware_required`, atomic write via `installer_daemon._atomic_write`, audit `config_upload`); watcher then routes per BC-10 contract | [test_build_config_admin_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_admin_sil.py) |
| BC-10 | INFRA | Hot-reload contract: schema-leaf `reload_class` annotations + `diff_reload_classes` classifier + `evaluate_quiescence` predicate + installer-bundle envelope + `tools/lifetrac-config` CLI (validate / bundle / verify / diff / push) + base-side `/config/download` + `/api/build_config/state` routes + X8-side installer daemon (`apply_bundle` + `discover_bundles` + `process_mount` + `write_result_file`) + OLED + LED feedback contract + `ConfigWatcher` watch-and-reload helper (production X8 systemd-unit wrapper around `process_mount` deferred to next bench round) | [test_build_config_delivery_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_delivery_sil.py), [test_build_config_installer_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_installer_sil.py), [test_build_config_installer_daemon_sil.py](DESIGN-CONTROLLER/base_station/tests/test_build_config_installer_daemon_sil.py) |

**Coverage gaps (drive future SIL rounds):**

As of Round 42 every "—" cell that has a natural SIL surface is
closed. The remaining "—" cells (IP-002, IP-005, IP-209, IP-304,
IP-307, IP-308, IP-309) are deployment / manual-review items that
don't have a natural SIL surface. Document them in the relevant
deployment runbook
instead of forcing test coverage.

---

## 6. How to run

### Run all SIL tests (CI-equivalent)

```powershell
cd c:\GitHub\LifeTrac\LifeTrac-v25\DESIGN-CONTROLLER\base_station
$env:LIFETRAC_ALLOW_UNCONFIGURED_KEY="1"
python -m unittest discover -s tests
```

Expected: `Ran 278 tests in ~0.9s` `OK (skipped=1)`.

### Run a single SIL file

```powershell
python -m unittest tests.test_link_tune_sil -v
```

### Run a single test method

```powershell
python -m unittest tests.test_axis_ramp_sil.W4_05_RampOutTests.test_TR_A_track_full_speed_two_second_ramp -v
```

### Compile gates locally (per-sketch)

```powershell
# Stage shared sources (one-time per build):
cd c:\GitHub\LifeTrac\LifeTrac-v25\DESIGN-CONTROLLER
.\tools\stage_firmware_sketches.ps1 -LoraProto common\lora_proto\

# Then compile, e.g. handheld:
arduino-cli compile --fqbn arduino:samd:mkrwan1310 firmware\handheld_mkr
```

For the M4 build, use `arduino:mbed_portenta:envie_m7:target_core=cm4`
on the [`firmware/tractor_h7_m4/`](DESIGN-CONTROLLER/firmware/tractor_h7_m4/)
sketch (NOT a separate `envie_m4` FQBN — that doesn't exist).

### HIL pre-flight

Per [HIL_RUNBOOK.md](DESIGN-CONTROLLER/HIL_RUNBOOK.md) §0:
1. Confirm SIL suite + all compile gates green on the commit under test.
2. Confirm fleet key provisioned on every node (no `0x00…` placeholder).
3. Confirm the [BUILD-CONTROLLER first-power-up checklist](BUILD-CONTROLLER/06_bringup_and_testing.md)
   has passed for the bench rig.

---

## 7. Update protocol

Every implementation round MUST update this file in the same PR as the
test/code change. Specifically:

1. **New SIL test file** → add a row to [§2](#2-sil-test-catalog),
   update the totals at the bottom of §2, update the test count in
   the [§1 tier overview](#1-tier-overview), and tick the relevant IP
   row(s) in [§5](#5-per-ip-traceability).
2. **New SIL coverage of a HIL item** → fill in the "SIL coverage"
   cell in [§4](#4-hil-bench-matrix) and link both ways.
3. **New / changed compile-gate job** → update [§3](#3-compile-gate-catalog)
   AND the count in [§1](#1-tier-overview).
4. **New IP added to the implementation plan** → add a row to the
   relevant Wave subsection of [§5](#5-per-ip-traceability) with `—`
   in the test column; that's the trigger for the next SIL round.
5. **Bump the "Last updated" line at the top of this file** to the
   round number that landed the change.

If you need to deviate (e.g. a deferred-coverage SIL is intentional),
document the rationale in the relevant cell with a footnote rather
than leaving the cell ambiguous.
